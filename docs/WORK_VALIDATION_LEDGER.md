# Work Validation Ledger

This file is the durable, topic-neutral record for terminal-guided work, validation-heavy work, and operator-run command chunks. It is not limited to mbsync.

Use this ledger when Codex gives command chunks for the owner to run on another machine, when the owner returns terminal output through `codex-input/pasted-text.txt`, or when a task needs a clear record of what succeeded, failed, was skipped, or was superseded.

## Rules

- Do not store passwords, app passwords, tokens, private keys, or unredacted configs.
- Do not paste massive terminal logs into this file. Reference the source path and summarize the important result.
- Record failed chunks and the fix that replaced them. Do not erase failures.
- Mark obsolete work as `superseded`, not deleted.
- Treat `codex-input/pasted-text.txt` as temporary input. This ledger is the durable record.

## Result Values

- `success`: command/action completed and matched expectations.
- `failed`: command/action did not complete or produced an unsafe/unusable result.
- `partial`: command/action produced useful output but required a workaround or follow-up.
- `skipped`: command/action was intentionally not run.
- `superseded`: command/action was valid earlier but later replaced by a better or final path.

## Future Session Template

For each future command chunk or validation step, add a row to the active ledger table.

| Date | Topic | Chunk/Step | Command or Action | Output Source | Result | Notes | Follow-up |
|---|---|---|---|---|---|---|---|
| YYYY-MM-DD | Short topic name | Chunk N or named step | Brief command/action summary | `codex-input/pasted-text.txt`, log path, or doc path | success/failed/partial/skipped/superseded | Key observed result, redacted if needed | Next command, fix, or none |

Recommended minimum fields for each topic:

- Work topic
- Environment
- Operator machine
- Command chunk ID
- Pasted output filename or log path
- Result value
- Decision or fix applied

## Active Ledger

Add new work here first. Move or summarize old rows only when the table becomes too large to scan.

| Date | Topic | Chunk/Step | Command or Action | Output Source | Result | Notes | Follow-up |
|---|---|---|---|---|---|---|---|
| 2026-06-28 | General workflow | Add validation ledger | Create topic-neutral ledger and wire it into project memory | `docs/WORK_VALIDATION_LEDGER.md` | success | Ledger added for all future terminal-guided work; historical exact per-chunk reconstruction remains unavailable. Path inspection, `git diff --check`, secret-oriented scan, and unit tests passed. | Use this ledger in future sessions. |
| 2026-06-30 | Betterbird conversion | Fedora source discovery Batch 1 | Inspect Fedora system, Python, Betterbird process state, candidate roots, lock markers, and shallow `cur/new/tmp` structure | `codex-input/pasted-text.txt` | partial | Fedora output showed Fedora 44 with Python 3.14.6, no Betterbird/Thunderbird process, source root `/home/atiq/Betterbird-Email/betterbird-maildir`, Betterbird markers `prefs.js` and `global-messages-db.sqlite`, and many maildir-style `cur/new/tmp` folders under `Mail/`. Lock marker files `.parentlock` and `lock` still exist, so they must be inspected before packing or copying. | Run lock-marker and source inventory batch before any transfer/conversion. |
| 2026-06-30 | Betterbird conversion | Fedora source discovery Batch 2 | Inspect source markers, lock file details, maildir directory counts, message counts, size, and filename samples | `codex-input/pasted-text.txt` | partial | Confirmed no Betterbird/Thunderbird process, source exists, `.parentlock` is an empty regular file, `lock` is a broken symlink to `192.168.199.51:+2`, `prefs.js` and 324M `global-messages-db.sqlite` exist, source size is 59G, and counts are 28 `cur` dirs, 19 `new` dirs, 28 `tmp` dirs, 48,720 `cur` files, 0 `new` files, and 4 `tmp` files. Output ended before the `new` sample and mixed-storage check. | Run a shorter follow-up for tmp contents, mixed mbox candidates, folder distribution, and representative samples. |
| 2026-06-30 | Betterbird conversion | Fedora source discovery Batch 3 | Inspect tmp files, per-folder message distribution, mixed-storage candidates, and sample RFC822 headers | `codex-input/pasted-text.txt` | success | Confirmed 48,720 message files, 0 `new` files, 4 `tmp` files in provider INBOX, no non-metadata files outside `cur/new/tmp`, and 200/200 sampled messages parsed with Message-ID, From, Date, and Subject. `tmp` includes one 2,045,376-byte file and three 201-byte files that the converter would skip by design. | Check whether migration scripts are available on Fedora, then run script unit tests and a representative sample conversion outside the source tree. |
| 2026-06-30 | Betterbird conversion | Fedora script availability Batch 4 | Search Fedora for converter/test scripts and compile available converter | `codex-input/pasted-text.txt` | partial | Found `/home/atiq/codex-runs/betterbird_maildirlite_to_maildirpp.py` and `/home/atiq/codex-runs/test_betterbird_maildirlite_to_maildirpp.py`; converter is executable, 30K, and `python3 -m py_compile` succeeded under Python 3.14.6. A full hash check and unit test have not yet been run. | Verify SHA256 values and run Fedora unit tests before any representative sample conversion. |
| 2026-06-30 | Betterbird conversion | Fedora script validation Batch 5 | Verify SHA256, compile scripts, show converter help, and run unit test | `codex-input/pasted-text.txt` | partial | SHA256 hashes matched the prepared Windows repo copies exactly, both scripts compiled, and converter help rendered. Unit test failed only because the standalone test file expected repo layout `../src/betterbird_maildirlite_to_maildirpp.py` and was run directly from `/home/atiq/codex-runs`, causing lookup of `/home/atiq/src/...`. | Create a temporary `src/` and `tests/` layout under `~/codex-runs` and rerun unit tests from that layout. |
| 2026-06-30 | Betterbird conversion | Fedora unit test Batch 6 | Create temporary repo-style layout under `~/codex-runs`, verify hashes, compile, and run converter tests | `codex-input/pasted-text.txt` | success | Corrected layout kept exact SHA256 values, `py_compile` exited 0, and Fedora unit tests ran all 3 tests successfully, including the Linux-only `cur` copy and `:2,S` flag preservation test. | Run a representative copied-sample conversion against temporary data outside the real Betterbird source. |
| 2026-06-30 | Betterbird conversion | Fedora sample conversion Batch 7 | Build 60-message representative copied sample, run converter `--copy --verify-hash --resume`, then run `--verify --verify-hash` | `codex-input/pasted-text.txt` | success | Sample copied 60 real messages from five folders plus 2 `tmp` files for skip testing. Converter copy exited 0, verify exited 0, target contained 60 `cur` files, 0 `new` files, and 0 `tmp` files. Summary showed all 60 candidates copied with zero skipped/errors; `errors.tsv` and `skipped.tsv` contained headers only. Folder mapping matched Maildir++ dot-folder expectations. | Run a full-source dry-run against the real Fedora source to audit all folders without copying or modifying source mail. |
| 2026-06-30 | Betterbird conversion | Fedora full-source dry-run Batch 8 | Run converter `--dry-run` against the full 59G Betterbird source and inspect copied log directory | `codex-input/pasted-text.txt`, `codex-input/log/full-dryrun-logs-20260630-091345` | success | Dry-run exited 0 in 755 seconds. Logs show 24 populated target folders, 48,720 source candidates, 48,720 dry-run planned copies, 0 copied, 0 skipped, 0 errors, and 62,359,127,853 message bytes. `errors.tsv` and `skipped.tsv` contain headers only. Duplicate audit recorded 18,571 duplicate Message-ID rows and 2,734 duplicate SHA256 rows; duplicates are warnings and will be kept by design. Dry-run did not create the target placeholder. | Inspect/decide treatment for four source `tmp` files and stale Betterbird lock markers before full profile transport. |
| 2026-06-30 | Betterbird conversion | Fedora tmp and lock audit Batch 9 | Recheck process/lock markers and classify the four source `tmp` files against dry-run hashes and Message-IDs | `codex-input/pasted-text.txt` | partial | Confirmed no Betterbird/Thunderbird process. `.parentlock` is still an empty regular file and `lock` is still a broken symlink to `192.168.199.51:+2`. Of four `tmp` files, three are 201-byte files with no Message-ID/From/Date/Subject headers; one 2,045,376-byte file has Message-ID/From/Date/Subject and its Message-ID is already present in planned `cur/new`, but its SHA256 is not an exact duplicate. | Locate the matching `cur/new` Message-ID path and compare non-private metadata before deciding whether to quarantine/salvage the valid-looking tmp file. |
| 2026-06-30 | Betterbird conversion | Fedora tmp match audit Batch 10 | Locate `cur/new` records matching each `tmp` file by Message-ID using dry-run logs | `codex-input/pasted-text.txt` | partial | Three 201-byte `tmp` files have no Message-ID and no matching `cur/new` records. The 2,045,376-byte `tmp` file has a Message-ID that matches three `cur` messages of about 8,555,695-8,555,699 bytes; none are SHA256-equal. This suggests the `tmp` file may be an incomplete/partial duplicate, but one more non-private content-shape check is needed before deciding to ignore or quarantine it. | Run a read-only MIME/content-shape comparison for the 2MB `tmp` file and its three `cur` matches. |
| 2026-06-30 | Betterbird conversion | Fedora tmp content-shape audit Batch 11 | Compare MIME/content shape and byte relationship between the 2MB `tmp` file and its three matching `cur` messages | `codex-input/pasted-text.txt` | success | The 2,045,376-byte `tmp` file is `multipart/related` with 4 MIME parts, 0 attachment-like parts, and 2 parser defects. The matching `cur` messages are about 8.55MB each, have 11 MIME parts, 7 attachment-like parts, and 0 defects. One complete `cur` message starts with and contains the full `tmp` byte sequence, while its final bytes differ. This confirms the `tmp` file is an incomplete prefix/partial duplicate of a complete `cur` message already included in conversion. | Keep converter behavior unchanged: ignore `tmp`. Preserve the original profile archive if needed, but do not include `tmp` files in canonical Maildir++ import. Next validate the transport script/sample behavior around stale lock markers and symlinks. |
| 2026-06-30 | Betterbird transport | Fedora transport script validation Batch 12 | Locate `betterbird_profile_transport.py`, copy to `~/codex-runs`, verify SHA256, compile, and show help | `codex-input/pasted-text.txt` | success | Found the script under VMware drag-and-drop cache, copied it to `~/codex-runs`, SHA256 matched `55568019cabc88e21bb5edaffd6f2d3b91cb6f15f075ad6351b1534c2ebfd094`, `py_compile` exited 0, and general plus `pack` help rendered. | Run a copied sample transport validation, including default refusal with lock markers and successful pack/verify/unpack/verify-tree after explicit manual override. |
| 2026-06-30 | Betterbird transport | Fedora sample transport Batch 13 | Attempt sample transport pack/verify/unpack/verify-tree with stale lock marker simulation | `codex-input/pasted-text.txt` | partial | Output stopped immediately after setup variables and before source inventory. This likely came from the `find ... | head -1` command under a wrapper with `set -e`/`pipefail`, where `find` can receive SIGPIPE after `head` exits. No transport validation result was produced. | Rerun the sample transport with `find ... -print -quit` and explicit status messages. |
| 2026-06-30 | Betterbird transport | Fedora sample transport Batch 13B | Rerun sample transport with `find -print -quit`, then test default refusal for simulated lock markers | `codex-input/pasted-text.txt` | partial | Sample source was created with one real message, `prefs.js`, empty `.parentlock`, and a `lock` symlink to `192.168.199.51:+2`. Default `pack` refused the active-looking lock markers as expected. The wrapper stopped immediately after the expected nonzero exit, so override pack/verify/unpack/verify-tree did not run. | Rerun with the expected refusal inside an `if` block so the script can continue to explicit override validation. |
| 2026-06-30 | Fedora Evolution conversion | Fedora Evolution/disk preflight Batch 14 | Verify Fedora Evolution Flatpak installation, process state, source health, disk space, and candidate target paths | `codex-input/pasted-text.txt` | partial | Confirmed Fedora 44, Evolution Flatpak `org.gnome.Evolution` version 3.60.2 installed system-wide from Fedora origin, source `/home/atiq/Betterbird-Email/betterbird-maildir` exists with 48,720 `cur` files, 0 `new`, 4 `tmp`, and `/home` has 219G free. Evolution background services are running. Flatpak permissions do not show access to a future `/home/atiq/Evolution-Mailstore` archive path, so a filesystem override will be needed before Evolution can read that directory. | Stop Evolution services, create/check a dedicated target path, grant Flatpak read/write access to that path, then run a non-destructive Flatpak access proof before real conversion. |
| 2026-06-30 | Fedora Evolution conversion | Fedora Evolution target prep Batch 15 | Stop Evolution services, create dedicated mailstore root, grant Evolution Flatpak access, and prove sandbox read access | `codex-input/pasted-text.txt` | success | Evolution-related processes stopped. Created `/home/atiq/Evolution-Mailstore` with mode 700. Confirmed target `/home/atiq/Evolution-Mailstore/betterbird-archive-maildirpp` does not exist. `flatpak override --user --filesystem=/home/atiq/Evolution-Mailstore:rw org.gnome.Evolution` exited 0, updated permissions show the path, Flatpak read proof succeeded, and `/home` still has 219G free. | Run real converter copy into the dedicated Fedora Evolution-friendly Maildir++ target, then verify hashes before adding it to Evolution. |
| 2026-06-30 | Fedora Evolution conversion | Real Maildir++ conversion Batch 16 | Convert full Betterbird maildir-lite source into dedicated Fedora Evolution Maildir++ target | `codex-input/pasted-text.txt` | success | Real copy exited 0 in 2,875 seconds. Copied 48,720 source candidates into 48,720 target `cur` files, 0 `new`, 0 target `tmp`, with target size 59G. `errors.tsv` and `skipped.tsv` contain headers only. `/home` free space decreased from 219G to 179G. Folder map created canonical Maildir++ dot folders under `/home/atiq/Evolution-Mailstore/betterbird-archive-maildirpp`. | Run full converter `--verify --verify-hash` against the copied target and inspect verification logs before opening Evolution. |
| 2026-06-30 | Fedora Evolution conversion | Real Maildir++ verify Batch 17 | Attempt full converter `--verify --verify-hash` against the copied target | `codex-input/pasted-text.txt` | partial | Verification did not run because the safety recheck found `evolution-source-registry` running. Source and target paths were detected, but the batch exited before hash verification. | Stop Evolution background services and rerun full verification with the same state database and target. |
| 2026-06-30 | Fedora Evolution conversion | Real Maildir++ verify Batch 17B | Stop Evolution background services and rerun full converter `--verify --verify-hash` | `codex-input/pasted-text.txt` | success | Evolution processes stopped, source/target/state checks passed, source and target counts matched at 48,720 `cur` files and 0 `new` files, target `tmp` count was 0, and full hash verification exited 0 in 328 seconds. Verification summary shows 48,720 copied records, 0 skipped, and 0 errors; `errors.tsv` and `skipped.tsv` contain headers only. `/home` remains 179G free. | Prove Evolution Flatpak can see the verified target, then add it in Evolution as `Maildir-format mail directories`. |
| 2026-06-30 | Fedora Evolution conversion | Evolution Flatpak visibility Batch 18 | Verify host and Evolution Flatpak can see the converted Maildir++ archive | `codex-input/pasted-text.txt` | success | Host and Flatpak both see `/home/atiq/Evolution-Mailstore/betterbird-archive-maildirpp`, top-level canonical Maildir++ dot folders are visible, and both count 48,720 `cur` message files. `FLATPAK_CHECK_EXIT=0`. | Add the archive to Evolution using `Maildir-format mail directories`, then validate folder visibility, message opening, and counts/search behavior. |
| 2026-07-01 | Fedora Evolution conversion | Owner GUI validation | Owner reported Evolution opened the converted archive account and validated key user-visible behavior | Conversation report | success | Owner reported `mail_tagindustries_com_sg.Inbox` opens with 14,279 emails, messages open, HTML renders well, and no error popup text is shown. This is owner-reported GUI validation, not independently reproduced from terminal logs in this workspace. | Transfer the verified converted Maildir++ archive to antiX using `src/maildirpp_transport.py`. |
| 2026-07-01 | Converted Maildir++ transport | Local implementation validation | Compile scripts, run full unittest discovery, check docs for bad `--manifest inventory.jsonl` references, inspect HTML copy-button wiring, show CLI help, and run `git diff --check` | Local PowerShell output | success | `py_compile` passed for `src/maildirpp_transport.py`, `src/betterbird_profile_transport.py`, and `src/betterbird_maildirlite_to_maildirpp.py`. `python -m unittest discover -s tests -v` ran 19 tests successfully with 2 expected Windows skips. Maildir++ guide HTML has 13 copy buttons, no missing copy targets, and no external resources. Manifest examples all point to `manifest.json`. `git diff --check` reported only unrelated line-ending warnings for unstaged `.gitignore` and `existing_DIY-Guide` files. | Commit and push the converted Maildir++ transport utility, tests, and docs. |
| 2026-07-01 | Converted Maildir++ transport | Fedora Chunk 1 | Check Fedora OS/Python, expected repo/script paths, and converted source root before transport | `codex-input/pasted-text.txt`, Fedora log `~/codex-runs/codex-batch-20260701-211319.log` | partial | Fedora is release 44 with Python 3.14.6. Converted source `/home/atiq/Evolution-Mailstore/betterbird-archive-maildirpp` exists and root `cur`, `new`, and `tmp` directories are present, with expected top-level Maildir++ dot folders visible. The expected repo path `/home/atiq/antiX_VM_Laptop` was absent, so repo-based script execution was not available. | Use the manually copied scripts in `~/codex-runs` instead of cloning the repo, then verify script hashes and imports. |
| 2026-07-01 | Converted Maildir++ transport | Fedora Manual Chunk 2A | Verify manually copied `maildirpp_transport.py` and `betterbird_profile_transport.py`, compile, show help, and confirm import locations | `codex-input/pasted-text.txt`, Fedora log `~/codex-runs/codex-batch-20260701-214251.log` | success | `maildirpp_transport.py` SHA256 matched `7af4d83d25556a39623d06d0d8de321b9569748e6da3cbe2b90d050dce3189e3`; `betterbird_profile_transport.py` SHA256 matched `55568019cabc88e21bb5edaffd6f2d3b91cb6f15f075ad6351b1534c2ebfd094`. `py_compile`, help commands, and import-location checks all exited 0, and both modules loaded from `/home/atiq/codex-runs`. | Stop Evolution/background services and run Maildir++ source inspection. |
| 2026-07-01 | Converted Maildir++ transport | Fedora Manual Chunk 2B | Stop Evolution services, check disk/source safety, and run `maildirpp_transport.py inspect` against the converted archive | `codex-input/pasted-text.txt`, Fedora log `~/codex-runs/codex-batch-20260701-214548.log` | success | Evolution source registry, alarm notify, calendar factory, and addressbook factory were stopped; remaining `bwrap` lines were `glycin-image-rs` loader processes, not Evolution mail services. `/home` had 178G available; source size was 59G. Source inspection reported 29 Maildir folders, 28 dot folders, 48,720 `cur` files, 0 `new`, 0 `tmp`, 1 metadata file, 62,430,783,277 regular bytes, no symlinks, no special files, `inspect_exit=0`, and `status=ok`. | Run Fedora Manual Chunk 2C to pack the converted Maildir++ archive into split verified parts. |
| 2026-07-01 | Converted Maildir++ transport | Fedora Manual Chunk 2C | Pack the converted Maildir++ archive into split verified gzip tar parts using the manually copied transport script | `codex-input/pasted-text.txt`, Fedora log `~/codex-runs/codex-batch-20260701-215204.log` | success | Final pre-pack check showed no Evolution mail processes. Source inspect repeated successfully with 48,720 `cur`, 0 `new`, 0 `tmp`, and `status=ok`. Pack wrote `/home/atiq/maildirpp-archive-export-20260701-215204` in 2,539 seconds with `pack_exit=0`, 21 parts, export size 38G, archive size 40,613,836,495 bytes, archive SHA256 `23292983c9ffb3590197a534aeddd03590a2b8d9f913732573fa1637b71045fe`, inventory SHA256 `97332ffac27c85372d91486ceeb8d49dde2cc036d6b0d889cd73243b1e06067d`, and manifest format `maildirpp-archive-transport-v1`. `/home` free space after pack was 141G. | Run Fedora-side `verify-archive` against `/home/atiq/maildirpp-archive-export-20260701-215204/manifest.json` before transferring to antiX. |
| 2026-07-01 | Converted Maildir++ transport | Fedora Manual Chunk 2D | Verify all split archive parts and the reconstructed archive stream on Fedora before transfer | `codex-input/pasted-text.txt`, Fedora log `~/codex-runs/codex-batch-20260701-224817.log` | success | Export `/home/atiq/maildirpp-archive-export-20260701-215204` still contained `manifest.json`, `inventory.jsonl`, and 21 part files. Manifest format was `maildirpp-archive-transport-v1`, archive SHA256 was `23292983c9ffb3590197a534aeddd03590a2b8d9f913732573fa1637b71045fe`, and inventory SHA256 was `97332ffac27c85372d91486ceeb8d49dde2cc036d6b0d889cd73243b1e06067d`. `verify-archive` returned `status=ok`, 21 parts, 40,613,836,495 archive bytes, `verify_archive_exit=0`, and completed in 183 seconds. | Transfer the full export folder to antiX, then run antiX-side `verify-archive` before unpacking. |
| 2026-07-01 | Converted Maildir++ transport | Fedora Manual Chunk 2E | Print final Fedora transfer checklist and exact files to copy to antiX | `codex-input/pasted-text.txt`, Fedora log `~/codex-runs/codex-batch-20260701-231043.log` | success | Export `/home/atiq/maildirpp-archive-export-20260701-215204` had `manifest_exit=0`, `inventory_exit=0`, `part_count=21`, and `export_size=38G`. File list showed `inventory.jsonl` at 12,422,512 bytes, `manifest.json` at 5,251 bytes, parts `0001` through `0020` at 1,992,294,400 bytes each, and final part `0021` at 767,948,495 bytes. | Copy the full export folder to antiX with filenames unchanged, then report the antiX destination path for antiX-side verification. |
| 2026-07-01 | Converted Maildir++ transport | USB guide implementation validation | Update the Maildir++ archive guide with Fedora USB and antiX staging transfer chunks, then check HTML copy buttons, manifest examples, and exact staging path references | Local PowerShell output | success | `docs/MAILDIRPP_ARCHIVE_TRANSPORT_GUIDE.html` now has 15 copy buttons, no missing copy targets, and no external resources. Search found no `--manifest inventory.jsonl` examples and no remaining generic `/path/to/maildirpp-archive-export` placeholders in the Maildir++ guide. `git diff --check` exited 0 with only unrelated Windows line-ending warnings. | Use the guide to copy the verified export by USB to `/mail/import-staging/maildirpp-archive-export-20260701-215204`, then validate antiX-side `verify-archive`. |

## Historical Backfill

These rows summarize confirmed milestones from existing project memory. They are not a complete verbatim reconstruction of every command chunk and pasted terminal response.

| Date | Topic | Chunk/Step | Command or Action | Output Source | Result | Notes | Follow-up |
|---|---|---|---|---|---|---|---|
| 2026-06-23 | Project memory | Initial setup | Create repository memory, workflow docs, and end-of-session commit/push policy | `docs/CHANGELOG.md`, `docs/DECISIONS.md` | success | Markdown memory system established. | Keep memory current after meaningful work. |
| 2026-06-24 | Betterbird conversion | Converter implementation | Add standard-library Betterbird maildir-lite to Maildir++ converter and tests | `docs/CHANGELOG.md`, `tests/` | success | Converter remains dry-run-first and dependency-free. | Validate on antiX with staged Betterbird data. |
| 2026-06-25 | Betterbird profile transport | Transport implementation | Add pack, split, verify, unpack, and verify-tree utility plus guide | `docs/CHANGELOG.md`, `docs/BETTERBIRD_PROFILE_TRANSPORT_GUIDE.txt` | success | Tool uses split gzip tar parts and manifest/inventory verification. | Run real Fedora to antiX transfer. |
| 2026-06-26 | Evolution Flatpak | GUI mail client setup | Validate Evolution Flatpak 3.60.2, `/mail` app data, launcher, and Maildir test account | `docs/PROJECT_STATE.md`, `docs/EVOLUTION_FLATPAK_ANTIX_GUIDE.txt` | success | Evolution reads Maildir with `BackendName=maildir`; user-level IceWM launcher documented. | Use Evolution as GUI path for validated Maildir trees. |
| 2026-06-27 | mbsync INBOX test | Discovery/install/layout/config | Install/use APT `isync`/`mbsync 1.5.1`, create isolated INBOX test layout, and write deletion-safe config | `docs/PROJECT_STATE.md`, `docs/MBSYNC_ANTIX_GUIDE.txt` | success | Layout used `/mail/Mailstore/mbsync/provider-inbox-test`; config used `Sync PullNew`, `Remove None`, `Expunge None`. | Keep INBOX test tree as validation account. |
| 2026-06-27 | mbsync INBOX test | Dry-run | Run `mbsync --dry-run -V provider-inbox` on empty test Maildir | `docs/PROJECT_STATE.md` | partial | isync 1.5.1 hit an assertion on the empty test Maildir. Safety was reviewed before continuing. | Proceeded with logged real pull after confirming target was empty and deletion policy was safe. |
| 2026-06-27 | mbsync INBOX test | First real pull and Evolution read test | Pull INBOX, inspect counts/files/logs, then read/search in Evolution | `docs/PROJECT_STATE.md`, `docs/MBSYNC_ANTIX_GUIDE.txt` | success | 144 INBOX messages pulled; Evolution read the tree as `BackendName=maildir`; post-Evolution sync had no duplicate flood. | Snapshot config/logs and plan production promotion. |
| 2026-06-27 | IMAP cleanup | Remove unwanted folders | Verify empty `FARSUK` and `BASUNDHARA`, then remove them from server | `docs/PROJECT_STATE.md`, `docs/CHANGELOG.md` | success | Folder cleanup made exact seven-folder production promotion practical. | Use explicit production folder list. |
| 2026-06-27 | mbsync production | Provider-live setup | Create `/mail/Mailstore/mbsync/provider-live`, state/log paths, and explicit folder patterns | `docs/PROJECT_STATE.md`, `docs/MBSYNC_ANTIX_GUIDE.txt` | success | First provider-live pull downloaded 163 messages; second pull had zero delta and no duplicate flood; `tmp` stayed empty. | Add Evolution production account and SMTP. |
| 2026-06-27 | Evolution production | SMTP and GUI validation | Read/search provider-live in Evolution, send SMTP tests to Gmail, pull replies | `docs/PROJECT_STATE.md` | success | After SMTP/Gmail replies, provider-live reached 170 messages; Evolution used `BackendName=maildir`. | Configure Sent upload. |
| 2026-06-27 | mbsync Sent upload | Sent-only push channel | Add `provider-live-sent-upload` with `Sync PullNew PushNew` only for Sent | `docs/PROJECT_STATE.md`, `docs/DECISIONS.md` | success | Controlled Sent upload changed remote `INBOX.Sent` from 4 to 5; second Sent sync had zero delta; later auto upload reached 6. | Keep normal folders receive-only. |
| 2026-06-27 | mbsync auto-sync | Loop/startup validation | Add 180-second provider-live loop, control commands, and IceWM startup block | `docs/PROJECT_STATE.md`, `docs/MBSYNC_ANTIX_GUIDE.txt` | success | Final audit showed loop running, `provider-live-group`, exit 0, no lock left behind, and empty `tmp` directories. | Monitor with `mbsync-provider-live-control status`. |
| 2026-06-27 | mbsync log retention | Cleanup proof | Add log retention and prove compression/delete/keep behavior with fake logs | `docs/PROJECT_STATE.md`, `docs/DECISIONS.md` | success | Auto logs compress after 2 days, delete after 30 days; manual logs kept for 90 days; status shows log usage. | Use `logs` and `cleanup-logs`. |
| 2026-06-27 | mbsync slow-sync safety | Timeout and stale-lock proof | Add timeout wrapper, sync-age visibility, stale lock clearing, and stale loop PID protection | `docs/PROJECT_STATE.md`, `codex-input/pasted-text.txt` | success | Fake stale lock showed `sync_age_seconds=600` and `sync_pid_alive=no`; clear succeeded; sync-now and restarted loop exited 0. | Use `clear-stale-lock` only when pid is not alive. |
| 2026-06-28 | mbsync guide hardening | Start Here section | Add clear fresh setup command sequence and reference-only config labels | `docs/MBSYNC_ANTIX_GUIDE.txt`, `docs/MBSYNC_ANTIX_GUIDE.html` | success | Future readers start from INBOX validation, then production, Evolution checks, and auto-sync validation. | Follow guide from `Start Here: Fresh Setup`. |
