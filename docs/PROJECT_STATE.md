# Project State

Last updated: 2026-07-27

## Project Identity

- Repository name: `antiX_VM_Laptop`
- Project type: local mail migration utilities plus a read-only Go notmuch browser service for antiX
- Primary purpose today: durable project memory, safe Betterbird/Maildir++ migration utilities, validated Evolution and production mbsync workflows, an installed approval-gated provider-live local archive/threshold controller, and a validated localhost-only Go notmuch browser using templ/Bun-built assets and repaired per-user runit supervision
- Portability target: Windows 11 and Debian Linux
- Git synchronization: connected GitHub remote with owner-approved end-of-session commit and push

## Current Phase

Planning/setup plus first broader application implementation.

The owner approved Python standard-library utilities for transporting a full Betterbird profile tree, converting Betterbird/Thunderbird maildir-lite to canonical Maildir++, transporting the already-converted Maildir++ archive from Fedora to antiX, and selecting/staging post-main-archive Betterbird aggregate deltas, plus small antiX setup helpers for launching Evolution Flatpak and testing deletion-safe mbsync pulls. On 2026-07-06, the owner accepted the first broader application objective: a read-only Go notmuch browser service for antiX, bound to localhost and reached from Windows 11 through SSH tunneling. On 2026-07-09, the owner accepted chi routing with compiled local Tailwind CSS; on 2026-07-12, capability-gated decoded attachment export and confirmation-gated image rendering; and on 2026-07-25, the final frontend/build/supervision stack of templ, HTMX, Tailwind via Bun, chi, and the existing per-user runit supervisor. Alpine.js, Alpine AJAX, Pines, Node, npm, and root-level runit are excluded.

## Current Objective

Maintain the project memory, mail utilities, and beginner-first fresh-VM reconstruction path for the fully validated templ/HTMX/Tailwind/chi browser, repaired per-user runit supervision, exact historical archive indexing, automatic incremental refresh, and the guarded multi-source search rollout.

## Current Multi-Source Rollout

- The repository implementation exposes All Mail and these approved GUI sources when their directories exist: `mbsync/provider-live`, `mbsync/provider-inbox-test`, `evolution/local-maildir`, `evolution/betterbird-delta-maildirpp-20260704`, `evolution/provider-live-archive`, and `evolution/test-maildir`. An empty approved source remains selectable; only nonempty discovered Maildir subfolders are added.
- Search pagination is driven by `notmuch search --output=messages --sort=newest-first`, so each unique Message-ID is one row and thread grouping cannot change the order. Metadata is fetched in bounded batches and reordered to the authoritative ID list. A source-specific result opens the duplicate file from that source when the same Message-ID has copies in multiple trees.
- Result dates use the notmuch message timestamp and antiX local time: under 60 seconds is `Now`; 1-59 minutes is `N min ago`; 60-119 minutes is `1 hour ago`; older same-day/yesterday rows include 12-hour time with seconds; older rows include weekday, full date, and time.
- `scripts/notmuch_browser_source_enroll.sh` provides read-only inspection, exact current-inventory acknowledgement, sequential enrollment, per-source path parity, full config/tag/database backup, automatic failure rollback, explicit rollback, and final validation. It pauses mbsync/stops the index loop, requires both synchronization locks to quiesce, checksum-verifies the complete backup before returning its pointer or starting mutation, and keeps raw refresh/index output only in private mode-600 logs whose byte counts and hashes are safe for operator evidence. Enrollment is not declared complete until the previously running browser/index/mbsync loops and prior mbsync pause state are observed restored; a failed restoration remains inside the rollback boundary. The unavailable `betterbird-post-main-archive-maildirpp-20260704-205827` remains the only intended `new.ignore` value.
- Current production source enrollment completed in Gate 07; browser replacement has not run. The remaining rollout proceeds one reviewed operator chunk at a time. Each gate records its exact batch, complete privacy-limited log, hashes, outcome, and ledger summary before the next gate.
- Local validation on 2026-07-26 passed deterministic templ/Tailwind regeneration, Go unit tests, `go vet`, Go race tests, all 75 Python tests, every shell syntax check, the 87,110-byte embedded asset budget, and a stripped 8,147,209-byte binary with SHA256 `a87276c5c17936489fe8ccad4de80eec3ba79cadb466ed2260825cdf4b5d19d6`. Recovery regressions prove that failure to observe a formerly running service after enrollment triggers database/config rollback and an active synchronization lock blocks before backup; raw mock refresh/index details do not reach operator output.
- Multi-source Gate 01 passed against clean pushed commit `7935502...`. The exact static inventories/manifests, XFS/free-space gate, safe ignored baseline, current 1,659-message/2,872-file index, installed `c7d14e0e...` release, user-runit services, provider loop, localhost health, locks, and temp state were valid. No acknowledgement, indexing, lifecycle action, or mail/index mutation ran. The exact batch and complete privacy-reviewed log are committed under `docs/validation/notmuch-browser-multisource/`.
- Gate 02 created the exact private acknowledgement marker `e17e3b45...` after matching every Gate 01 source manifest before and after. Config, ignore scope, health, locks, and services remained safe, and no indexing or lifecycle action ran. The gate is recorded as partial because its greedy `sv status` parser labeled svlogd PIDs as browser/index PIDs; direct review identified the actual service PIDs. A corrected read-only identity audit is mandatory before enrollment.
- Gate 03 resolved the Gate 02 evidence defect without writes: it proved the exact marker/sources/config, browser PID 3761, index PID 3812, their distinct svlogd processes and runsv parents, provider loop, localhost health, free candidate port, and absent locks.
- Gate 04 passed without production writes. It proved 104,973,672 KiB free, a 36,700 KiB/six-file database, zero prior enrollment backups/pointer, safe destinations, exact tag/path/database backup commands, and a conservative 1,158,676 KiB capacity requirement. Its complete 68-line privacy-reviewed log is committed. A final read-only audit of the newly hardened service-restoration contract is required before enrollment.
- Gate 05 passed without production writes against pushed commit `1297797...`. It verified the exact Gate 04 evidence, helper restoration order, marker/config, all five acknowledged source manifests, 104,973,632 KiB free, zero prior enrollment backups, running browser/index/mbsync services with mbsync unpaused, read-only 1,659/2,872 health, and absent locks. Its complete 78-line privacy-reviewed log is committed. The next gate may perform the guarded source enrollment.
- Gate 06 ran the guarded enrollment for about 17 minutes. The helper and all three `notmuch new` stages returned zero, the backup verified, exact prefix-filtered parity passed for 48,564 local, 247 delta, 148 provider-test, four Evolution-test, and zero provider-archive files, the final one-value ignore/config was correct, and all services were restored. The outer harness then failed because `notmuch count --output=files path:evolution/local-maildir/**` returned 49,037: the installed manual confirms this mode counts every file associated with a matching logical message, including duplicate copies outside the selected source. Automatic rollback returned zero. Direct review proved the old config `9fc4a9bd...`, marker `e17e3b45...`, live 1,659-message/2,872-file database, browser/index/mbsync services, unpaused state, and absent locks restored. Backup `source-enrollment-20260726-231044` remains intact; the successfully enrolled database is preserved as a 741,256 KiB/six-file failed-state directory with manifest `5a5fc006...`. Gate 06 is retained as failed evidence; a corrected retry must use the helper's prefix-filtered file-list method.
- Gate 07 completed the corrected enrollment retry in about 17 minutes. It first verified the committed Gate 06 failure, original backup inventory `7c539d54...`, and preserved enrolled-database manifest `5a5fc006...`. The three new index stages returned zero; exact prefix-filtered source parity and post-counts were local 48,564, delta 247, provider-test 148, Evolution-test four, provider-archive zero, and provider-live 2,625. The final index is 31,418 unique messages/51,588 files. Config hash `50adc22c...` has only the unavailable post-main archive ignored and restores `inbox unread` live defaults; marker `e17e3b45...` remains exact. Backup `source-enrollment-20260726-233309` passed inventory verification and is the current rollback pointer. Browser/index/mbsync are running, mbsync is unpaused with last exit zero, locks are absent, health is read-only/non-mutating, rollback was not invoked, and enrollment did not mutate Maildir files. The next rollout phase is isolated validation of the new multi-source GUI binary before production replacement.
- Gate 08 attempted the clean build and isolated port-8876 validation, but stopped in 0.29 seconds at its first production listener assertion. Repository/Gate 07/build/config/marker/ignore checks and the free-port check passed; the single-quoted `awk` program incorrectly contained backslash-escaped double quotes and `awk` rejected it. The gate stopped before creating the candidate root, running the build, starting a process, or writing a candidate pointer. Direct review confirmed zero 8876 listeners, healthy production on 8765, and the unchanged 31,418-message/51,588-file index. The exact 460-line batch and complete 36-line log are retained as failed, privacy-reviewed evidence; the retry changes only this harness quoting and pins Gate 08's committed failure record.
- Gate 09 corrected the listener assertions and completed the clean build: deterministic templ/Tailwind generation, Go unit/vet/race checks, all 75 Python tests, the 87,110-byte asset budget, and candidate binary `a87276c...` at 8,147,209 bytes passed. Candidate health on 8876 was read-only at 31,418/51,588; provider-live, provider-inbox-test, local-maildir, betterbird-delta, and empty provider-live-archive source contracts passed. The test-maildir check exposed a real default-query defect: its four logical messages and four exact indexed paths exist, but scoped search changed notmuch's special `*` query into `(*) and path:...`, which returned zero for those untagged messages. Failure cleanup identity-stopped the candidate, freed 8876, and published no pointer; production remained unchanged. Gate 09's exact batch and complete 182-line privacy-reviewed log are retained. The application must special-case default scoped search to use only the path term, add regression coverage, rebuild, and retry.
- The Gate 09 source-search defect is fixed in the repository: blank and `*` searches scoped to a selected source now execute the source `path:".../**"` term directly, while explicit user queries retain `(query) and path:".../**"`. A focused regression covers blank, `*`, and explicit-query behavior. The complete reproducible build passed deterministic generation, Go unit/vet/race, all 75 Python tests, shell/asset checks, and produced an 8,147,209-byte candidate with SHA256 `26967a5d9e7456ecfdc9350a57aaee1f736e048bfbed9c72820adc157d9f21e1`. Production was not changed.
- Gate 10 passed the complete clean-build and isolated validation against pushed commit `56a6d49...`. Candidate `26967a5d...` started as PID 7522 only on `127.0.0.1:8876` with read-only health at 31,418 messages/51,588 files. The GUI catalog exposed all six approved source groups; provider-live, provider-inbox-test, local-maildir, betterbird-delta, and test-maildir returned individual unique newest-first rows with the complete date contract, test-maildir returned exactly all four indexed messages, and the empty provider-live archive correctly returned no results. Invalid-folder rejection, security headers, static assets, status/method guards, and zero download-temp files passed. Production remained healthy on 8765 with exact old binary `c7d14e0e...`; mail/tags/index/config were unchanged. Mode-600 pointer `d8e111ca...` recorded the retained candidate; by the later layout diagnostic PID 7522 had exited and 8876 was free, so that pointer is now preserved as stale Gate 10 evidence rather than reused.
- Manual antiX review rejected Gate 10's result-row layout because the required full date/time painted beyond the old 122px date column and overlapped the sender. Local Brave renders at 1365x768, 1024x768, and 620x768 reproduced the defect. The responsive fix reserves 218px for the desktop date column, uses compact 12px tabular numerals with overflow containment, and changes results at 620px or narrower into stacked cards with date, sender, and subject on separate lines. Post-fix renders at all three sizes showed no overlap; the 1024px sender uses a controlled ellipsis and the 620px card preserves the full sender. A CSS regression contract and the complete reproducible suite passed; embedded assets are 87,968 bytes and the 8,147,209-byte candidate SHA256 is `5269c53393d96db1c3fe37f1160280ef9778fd3a7c795b1cf3db0a07c9c9649f`. The temporary diagnostic server on 8877 was stopped cleanly; production was not changed.
- Gate 11 passed its complete automated clean-build and isolated validation against pushed commit `a1ec21a...`. Exact binary `5269c533...` was retained only on `127.0.0.1:8876` with read-only health; all six approved source groups, newest-first/unique/date contracts, exact four-row test-maildir result, empty archive, invalid-folder handling, security/assets/routes/temp, and unchanged production passed. Its exact batch/log/record are privacy-reviewed and committed as successful automated evidence. The owner's later new-tab screenshot rejected a separate behavior: after scrolling a long full-message page, the black sidebar ended partway down the viewport. Gate 12 later identity-stopped only this rejected process and preserved its files and pointer.
- The new-tab sidebar defect is reproduced and fixed locally. Gate 11 computed geometry changed from `position: relative`, `top: 0`, `bottom: 768` before scroll to `top: -238`, `bottom: 530` after a 238px document scroll, exactly explaining the exposed lower-left background in the screenshot. The desktop rule now uses `position: sticky`, `top: 0`, `align-self: start`, and `height: 100dvh`, while the existing mobile rule remains a fixed off-canvas drawer. On the corrected diagnostic binary, the same actual indexed full-message page stayed `top: 0`, `bottom: 768`, and `height: 768` after the full available 238px scroll. The captured scrolled render showed the black sidebar and its bottom privacy card continuously filling the viewport. Deterministic generation, Go unit/vet/race, all 75 Python tests, shell checks, and the 87,989-byte asset gate passed; the 8,147,209-byte diagnostic binary SHA256 is `4f33c40777b3dc49abdecb59775c8b8aa4d207dd7933349766edae4d7e5bc105`. Diagnostic port 8877 and its headless browser were stopped; production, Gate 11, mail, tags, index, and configuration were not changed.
- Gate 12 passed the exact operator clean-build and isolated-validation gate against pushed commit `532c783...`. It pinned the privacy-reviewed Gate 11 evidence, verified and gracefully stopped only rejected PID 32185, preserved its files/pointer, and built exact 8,147,209-byte binary `4f33c407...`. Repository and served CSS SHA256 `3c07b0c4...` both proved desktop `sticky/top:0/100dvh` and mobile `fixed`. Candidate PID 79328 is retained only on `127.0.0.1:8876` under mode-600 pointer `ccfc7295...`; health is read-only/non-mutating at 31,420 messages/51,590 files. All six approved source groups, unique/newest-first/date contracts, exact four test-maildir rows, empty archive, invalid-folder rejection, security/assets/routes, and zero temp files passed. Production stayed exact on 8765 with binary `c7d14e0e...`; mail, tags, index, and configuration were untouched. The exact 643-line batch and complete 248-line log passed automated and manual privacy review. The owner then explicitly reported `antiX Gate 12 sticky sidebar GUI: PASS` after hard-refreshing, opening a long message in a new tab, scrolling through top/middle/bottom, and checking collapsed/expanded states. A post-report identity check reconfirmed exact PID/executable/start ticks, sole 8876 listener, read-only 31,420/51,590 health, and zero temp files. This is the durable session stopping point; Windows SSH-tunnel GUI acceptance is next.
- After the owner rebooted antiX and resumed, the repository gained the approved small message-reader address enhancement: From, To, Cc, and nonempty Bcc values are parsed with Go's standard mail parser; every valid individual `Name <address>` entry is a query-free `mailto:` link with an adjacent raw-address copy control; and malformed fields remain escaped plain text. Search-result senders remain plain text. Deterministic generation, Go unit/vet/race, all 75 Python tests, shell checks, the 88,348-byte asset gate, and an 8,188,169-byte stripped local binary with SHA256 `b37b3b633188e5c1c9c34b6dbb9c58f5e2c391982437ded6f0d7efeecf68ed5b` passed. The reboot made Gate 12's retained-process pointer stale and freed 8876; production auto-restarted on 8765 with the previously installed binary. A new isolated operator candidate is required before any production replacement.
- Gate 13 passed its clean-build and isolated validation against pushed commit `86cc59b...`. It verified Gate 12's exact accepted batch/log/record and reboot-stale mode-600 pointer, preserved all prior files, and built exact 8,188,169-byte candidate `b37b3b63...`. Deterministic generation, Go unit/vet/race, all 75 Python tests, shell checks, 88,348-byte assets, desktop sticky/mobile fixed/address-wrapping CSS, all six source/date/search contracts, exact four test-maildir rows, invalid-folder rejection, security/routes, zero temp files, and read-only 31,551-message/51,783-file health passed. A private real-message response contained ten query-free mailto links and ten exactly matching raw-address copy values; Bcc remains covered by the clean-build fixture because recent real mail need not contain that header. The exact 617-line batch and complete 228-line log contain no addresses or Message-IDs and passed automated/manual privacy review. Candidate PID 48951 is retained only on `127.0.0.1:8876` under pointer `5d386a49...`; production remains exact and unchanged on 8765. AntiX GUI acceptance is next.
- The owner reported `antiX Gate 13 address links GUI: PASS`, accepting mailto opening, raw-address copying, wrapping, and the preserved browser behavior, then requested one visual refinement before Windows review: make only the per-address copy controls smaller while keeping them noticeable. The repository now uses an 18px address-only control with an 11px icon, persistent subtle border/muted surface/sapphire color, stronger hover/focus, and preserved green copied feedback; subject and Message-ID controls remain 22px. The focused regressions and complete deterministic suite passed, including Go unit/vet/race, 75 Python tests, shell checks, 88,863-byte assets, and an 8,192,265-byte stripped binary with SHA256 `c53c95e28989c3d9543394c8142b7b2fb199f8ebe4cbf28e13923a0444ab0692`. Gate 13 remains accepted evidence but its running candidate must be superseded by a new isolated Gate 14 candidate for this final styling review.
- Gate 14 passed against pushed commit `df6aac6...`. It pinned Gate 13's exact batch/log/record/pointer, verified the accepted PID/executable/start ticks and read-only health, stopped only that process, and preserved its files and pointer. The clean build reproduced exact 8,192,265-byte candidate `c53c95e2...`; deterministic generation, Go unit/vet/race, 75 Python tests, shell checks, 88,863-byte assets, exact 18px/11px/resting/hover/copied CSS, all six source/date/search contracts, exact four test-maildir rows, real-message query-free mailto/raw-copy parity, security/routes, zero temp, and read-only 31,552-message/51,785-file health passed. The complete 644-line batch and 246-line log passed automated/manual privacy review without addresses or Message-IDs. PID 70088 is retained only on `127.0.0.1:8876` under mode-600 pointer `0fcdeffb...`; production remains unchanged on 8765. Final antiX compact-control GUI acceptance is next, followed by Windows tunnel review.
- The owner reported `antiX Gate 14 compact address copy GUI: PASS`, explicitly accepting the smaller control plus its hover/focus behavior, but found the green copied feedback too subtle. The address-only copied state now lasts 2.2 seconds and uses a brighter green fill, dark green border/icon, visible focus-like ring, and slight scale pulse; other copy controls retain their existing 1.2-second behavior and size. Focused regressions plus deterministic generation, Go unit/vet/race, all 75 Python tests, shell checks, and the 89,072-byte asset gate passed. The resulting stripped 8,192,265-byte local binary has SHA256 `7f4099ed7cc5882928b5f9785689cd3adfa60993af83f0f8a17f325fcb7f1f1d`. Gate 14 remains retained on 8876 until Gate 15 verifies and supersedes only that exact process.
- Gate 15 passed against pushed commit `71251c4...`. It pinned Gate 14's exact batch/log/record/pointer, verified its accepted live PID/executable/start ticks and read-only health, stopped only PID 70088, and preserved all prior files and pointer. The clean build reproduced exact 8,192,265-byte binary `7f4099ed...`; deterministic generation, Go unit/vet/race, all 75 Python tests, shell checks, 89,072-byte assets, repository and served CSS `4600ac8b...`, repository and served JavaScript `792d1141...`, bright/ring/pulse CSS, and the 2,200ms real-message markup contract passed. All six source/date/search contracts, exact four test-maildir rows, six private mailto/raw-copy pairs, security/routes, zero temp files, and read-only 31,552-message/51,785-file health also passed. The exact 684-line batch and complete 260-line log passed automated and manual privacy review without addresses or Message-IDs. PID 92012 is retained only on `127.0.0.1:8876` under mode-600 pointer `6ab743ea...`; production remains exact and unchanged on 8765. The owner explicitly reported both `antiX Gate 15 strong copied feedback GUI: PASS` and Windows 11 SSH-tunnel `127.0.0.1:8876` PASS. An immediate post-Windows-report identity/health recheck confirmed the exact candidate binary, sole 8876 listener, read-only 31,552/51,785 state, separate production listener, and unchanged installed production binary `c7d14e0e...`. Cross-platform GUI acceptance is complete; the next guarded action is production replacement, which has not run.
- Gate 16 attempted the guarded production replacement against clean pushed commit `1d33105...` but stopped after 0.06 seconds at a documentation-text assertion. Repository identity plus Gate 15's exact batch `84fa9b90...`, log `b89e9d83...`, and record `9de34205...` passed; the batch then searched for one contiguous phrase not present in the differently worded project-state sentence. It stopped before checking the candidate pointer, creating a backup/rollout directory or pointer, stopping runit, staging/installing a binary, or entering the rollback boundary. Direct review reconfirmed exact old production binary `c7d14e0e...`, absent new production/rollback pointers and rollout root, and healthy read-only production/candidate endpoints at 31,552 messages/51,785 files. The exact 884-line batch and complete 27-line log passed automated/manual privacy review. Gate 16 is retained as failed evidence; the retry must replace only the brittle text check with independent exact antiX and Windows acceptance markers and pin this failure record.

## What Exists Now

- Git repository on branch `atiqte/branch-codex`, with a connected `origin`.
- `README.md` with project overview and start instructions.
- `AGENTS.md` with future Codex operating rules.
- `docs/` project memory folder.
- `docs/BETTERBIRD_PROFILE_TRANSPORT_GUIDE.html`, an offline browser DIY guide with copy buttons for the full Fedora-to-antiX transport and conversion flow.
- `docs/BETTERBIRD_PROFILE_TRANSPORT_GUIDE.txt`, a terminal-friendly plain text version of the same guide.
- `docs/MAILDIRPP_ARCHIVE_TRANSPORT_GUIDE.html`, an offline browser DIY guide with copy buttons for transferring the already-converted Fedora Maildir++ archive to antiX, including the USB transfer workflow, Win11 host-share copy path for the verified export, and the validated all-in-one antiX restore/verify/inspect chunk.
- `docs/MAILDIRPP_ARCHIVE_TRANSPORT_GUIDE.txt`, a terminal-friendly plain text version of the same converted Maildir++ archive guide, USB transfer workflow, Win11 host-share copy path, and validated all-in-one antiX restore/verify/inspect chunk.
- `docs/BETTERBIRD_DELTA_MAILDIRPP_TRANSFER_GUIDE.html`, an offline browser DIY guide with separate copy buttons for the dynamic post-main-archive Betterbird aggregate delta workflow from Fedora to antiX Evolution.
- `docs/BETTERBIRD_DELTA_MAILDIRPP_TRANSFER_GUIDE.txt`, a terminal-friendly plain text version of the same dynamic post-main-archive aggregate delta workflow, including audit, mail-process shutdown, staging, converter dry-run, copy/hash verification, pack/verify, transfer, antiX restore, and Evolution GUI validation.
- `docs/EVOLUTION_FLATPAK_ANTIX_GUIDE.html`, an offline browser DIY guide with copy buttons for installing and preparing Evolution Flatpak 3.60.2 on antiX runit/IceWM.
- `docs/EVOLUTION_FLATPAK_ANTIX_GUIDE.txt`, a terminal-friendly plain text version of the same Evolution Flatpak guide.
- `docs/MBSYNC_ANTIX_GUIDE.html`, an offline browser DIY guide with copy buttons for the first deletion-safe mbsync INBOX test and the validated production `provider-live` setup with narrow Sent upload on antiX.
- `docs/MBSYNC_ANTIX_GUIDE.txt`, a terminal-friendly plain text version of the same mbsync guide.
- `docs/PROVIDER_LIVE_ARCHIVE_GUIDE.html`, a chronological offline guide with copy buttons for discovery, checksummed no-clone installation, Evolution/notmuch enrollment, threshold monitoring, two-stage archive approval, cleanup canary, rollback, and snapshot retirement.
- `docs/PROVIDER_LIVE_ARCHIVE_GUIDE.txt`, the terminal-friendly companion guide for the same provider-live archive workflow.
- `docs/NOTMUCH_BROWSER_ANTIX_GUIDE.html`, an offline browser DIY guide with visible embedded command blocks and copy buttons for retiring Astroid, installing the single-email read-only notmuch browser viewer, daily start/stop, CLI search, tag backup/restore, and pilot-safe reindexing.
- `docs/NOTMUCH_BROWSER_ANTIX_GUIDE.txt`, a terminal-friendly plain text version of the same browser-only notmuch guide.
- `docs/NOTMUCH_GO_BROWSER_SERVICE_GUIDE.html`, the beginner-first offline blank-VM guide with 24 ordered sections and 19 copy-button command blocks covering antiX/VMware, XFS `/mail`, packages, verified Go/Bun, GitHub clone/update, mbsync, exact archive restore, the long first index, full browser build/install, user runit, SSH, automatic new-mail indexing, reboot proof, and troubleshooting.
- `docs/NOTMUCH_GO_BROWSER_SERVICE_GUIDE.txt`, the terminal-friendly companion containing the same commands and stop/expected-result guidance.
- `docs/NOTMUCH_ASTROID_ANTIX_GUIDE.html` and `.txt`, retained only as retired historical guides with warnings pointing to the active browser-only guide.
- `docs/WORK_VALIDATION_LEDGER.md`, a topic-neutral ledger for command chunks, pasted-output review, validation outcomes, failures, fixes, and superseded steps.
- `src/` utility folder for approved Python standard-library mail migration tools.
- `.gitignore` with language-neutral local, cache, and generated-file exclusions.
- `.gitattributes` enforcing LF line endings for shell and Python scripts.
- `src/betterbird_profile_transport.py`, a Python standard-library tool to pack, split, verify, unpack, and verify a whole Betterbird profile tree for transfer.
- `src/maildirpp_transport.py`, a Python standard-library tool to inspect, pack, split, verify, unpack, and verify an already-converted canonical Maildir++ archive.
- `src/betterbird_maildirlite_to_maildirpp.py`, a dry-run-first Python converter for staged Betterbird maildir-lite profiles.
- `src/betterbird_post_archive_delta.py`, a Python standard-library helper that audits and stages all current Betterbird source messages absent from the main converted archive baseline.
- `src/provider_live_archive.py`, a Python standard-library transaction engine for UID/hash manifests, hard-link snapshots, split external backup verification, monthly Maildir++ archive copies, approval gates, cleanup canary, rollback, snapshot retirement, mbsync policy audit, and notmuch scope audit.
- `scripts/provider_live_archive_control.sh`, the POSIX antiX monitor/service controller for the 15,000-message threshold and explicit archive transaction commands.
- `scripts/provider_live_archive_setup.sh`, the checksummed offline payload bundler and antiX installer/validator; no antiX repository clone is required.
- `tests/test_provider_live_archive.py`, focused tests for threshold selection, UID/flag/month mapping, mbsync policy refusal, external corruption, resumable archive copy, hash gates, canary behavior, cleanup, rollback, and retirement.
- `tests/test_betterbird_profile_transport.py`, standard-library tests for split archive transport safety.
- `tests/test_maildirpp_transport.py`, standard-library tests for converted Maildir++ transport safety.
- `tests/test_betterbird_maildirlite_to_maildirpp.py`, standard-library tests for folder mapping and portable validation.
- `tests/test_betterbird_post_archive_delta.py`, standard-library tests for dynamic post-main-archive delta selection, fallback matching, and staging safety.
- `scripts/evolution_flatpak_icewm_launcher_setup.sh`, a user-level antiX helper for adding Evolution Flatpak to IceWM menu/taskbar launch surfaces and starting `gnome-keyring-daemon`.
- `scripts/mbsync_provider_inbox_setup.sh`, an antiX helper for inspecting package/layout state, installing isync/mbsync, creating the isolated `/mail` mbsync test layout, writing a pull-only INBOX config, production `provider-live` layout/config/status, Sent-only upload channel setup, and provider-live auto-sync loop/startup setup with log retention, timeout wrapping, persistent runtime-adjustable intervals, stale-lock proofing, and stale loop PID protection.
- `scripts/notmuch_browser_control.sh`, the source control script intended to be installed on antiX as `~/.local/bin/notmuch-browser-control`; it starts/stops the Go browser service, reports status/logs, refreshes the notmuch index with lock safety, and delegates lifecycle/status/log operations to the approved per-user runit service when active while retaining the direct-process rollback path.
- `scripts/notmuch_browser_index_control.sh`, a user-level helper that keeps the notmuch index fresh through the locked refresh path, skips active mbsync/notmuch locks, and delegates lifecycle/status/log operations to the approved per-user runit service when active.
- `scripts/notmuch_browser_build.sh`, the Bun 1.3.14 and Go build orchestrator for frozen dependency preparation, templ/Tailwind generation, reproducibility checks, tests/vet/race/shell validation, and stripped binary output.
- `scripts/notmuch_browser_fresh_vm_setup.sh`, the state-aware fresh antiX assistant with read-only `inspect`/`status`/`next`, exact archive acknowledgement, safe notmuch configuration, resumable full historical indexing, reproducible build/install, user-runit enablement, final validation, and rollback-protected `main` updates.
- `scripts/notmuch_browser_runit_setup.sh`, a fail-closed helper that manages only `~/.runit/usersv/notmuch-browser{,-index}` and their links under `~/.runit/service`; it stages disabled definitions, records checksummed rollback evidence, activates with health checks, removes only the two legacy IceWM blocks after success, and restores the legacy path after a failed or explicit rollback.
- `scripts/notmuch_browser_operator_run.sh`, the committed Bash runner for one project-local reviewed gate at a time. The ignored active batch is `codex-output/notmuch-browser-operator/current.sh`; private millisecond-stamped combined stdout/stderr logs and `latest-log.txt` remain beside it for direct Codex review.
- `scripts/notmuch_browser_strict_validator.py`, a Python standard-library validator for signed attachment/ZIP/inline-image capabilities, duplicate isolation, exact decoded bytes, image CSP/reset behavior, safe Store-mode ZIP entries, and capability rejection. It treats the audit-selected inline MIME part separately from the genuine attachment parts exposed by the accepted browser.
- `scripts/generate_notmuch_browser_service_guide.py`, the Python standard-library source for deterministic matching HTML/TXT reconstruction guides.
- `cmd/notmuch-browser/main.go`, the Go service entrypoint.
- `internal/notmuchbrowser/`, the Go service package, including chi v5.3.1 routing, notmuch command execution, MIME/JSON parsing, HMAC capabilities, bounded attachment/ZIP/inline-image handlers, structured email HTML sanitization, templ v0.3.1020 source/generated components, compiled local Tailwind CSS 4.3.3, local application JavaScript, a pinned Lucide icon subset/license, vendored local HTMX 2.0.10, and focused tests.
- `package.json` and `bun.lock`, exact Bun-managed build dependencies for `tailwindcss` and `@tailwindcss/cli` 4.3.3. Generated CSS and templ Go are committed; production needs neither Bun nor templ.
- Local combined-migration validation on 2026-07-25 passed frozen Bun install, Go module verification, two consecutive templ/Tailwind generations with identical hashes, Go unit tests, vet, race, 65.5% generated-code-inclusive coverage, all 46 Python tests, every shell syntax check, diff checks, an isolated graceful-SIGTERM proof, and a stripped 8,093,961-byte Linux build with SHA256 `c7d14e0ee792595cd168a131cb05307214ae1129dc449f982fa48d30aa5bfed9`. Embedded static assets total 86,332 bytes, below the 90 KiB budget.
- The old modular `.html` templates were deleted only after typed templ render parity tests passed. HTMX is configured with evaluation and script-tag processing disabled plus no history snapshot cache; the parent CSP remains `script-src 'self'` with no `unsafe-eval`.
- The accepted combined-migration browser and controls are installed under `/mail/AppData/notmuch-browser` and `~/.local/bin`, with browser and index supervision cut over to the existing per-user runit tree. The HTML/TXT reconstruction guides now describe this final release and its repaired reboot path.
- Beginner fresh-VM guide regeneration completed on 2026-07-25 from a standard-library content module. Matching ASCII HTML/TXT now use 24 ordered sections and 19 command blocks. The helper and guide require the exact verified 48,720-file/62,430,783,277-byte archive, reject the drifted 48,564-file tree, require 80 GiB free, keep `local-maildir` out of `new.ignore`, run the first index without the routine timeout, verify path parity, preserve the full browser, and explain the 180-second receive plus 60-second incremental-index flow.
- Local implementation validation passed frozen Bun 1.3.14 installation, module verification, byte-stable templ/Tailwind generation, Go unit/vet/race, all 65 Python tests, every shell syntax check, the 86,332-byte embedded-asset budget, and the exact 8,093,961-byte stripped binary SHA256 `c7d14e0e...`. This validates repository behavior; the full blank-VM operator pilot remains a separate acceptance task.
- The first operator preflight log `/home/atiq/codex-runs/codex-batch-20260725-150547.log` passed repository commit/cleanliness, Go/Bun/dependency pins, `/mail` mount/capacity, active per-user runsvdir, absent candidate runit links/pointer, exact installed artifact hashes, one browser/index process, localhost-only 8765, and free 8876. It then stopped at an indentation-damaged embedded Python parser, so health/config/lock/temp assertions were not completed. This was a read-only harness failure, not an application failure. The replacement full preflight is prepared in the new project-local operator workspace.
- Project-local FIX1 log `notmuch-browser-operator-20260725-160822-406.log`, SHA256 `9f8c531c...`, passed through exact production artifact identities, then reported two `pgrep -x notmuch-browser` matches. A direct read-only audit proved PID 2296 is `/usr/bin/dash` running only `notmuch-browser-index-control loop`, while PID 2374 is the sole exact production browser executable/listener. Linux `comm` truncation made both long names appear as the same 15-character value. Production health remained read-only at 1,620 messages/2,817 files and 8876 was free. The corrected gate matches the full browser command line and `/proc/PID/exe`.
- Project-local FIX2 log `notmuch-browser-operator-20260725-161114-564.log`, SHA256 `6adabdfb...`, completed the entire read-only preflight with exit 0. Commit/branch/cleanliness, Go/Bun/templ/chi/x-net/Tailwind pins, `/mail` mount/capacity, user runsvdir, absent candidate services/pointer, installed hashes, exact browser executable, one browser/index process, 15,804 KiB browser RSS, loopback-only 8765, free 8876, structured read-only health at 1,620/2,817, notmuch paths/settings, absent locks, zero download/Maildir temp, and singular IceWM markers all passed. No production or mail/index state changed.
- Clean-build attempt 1 log `notmuch-browser-operator-20260725-161620-299.log`, SHA256 `4e9a62b1...`, stopped before run-directory creation because its expected production hash accidentally omitted one `6f` pair. Actual installed SHA256 was the exact durable `50e6b1ad...`; the candidate pointer and build directory remained absent, PID 2374 stayed the sole 8765 listener, 8876 stayed free, and health remained OK/read-only/non-mutating at 1,620/2,817. The corrected batch changes only that expected constant.
- Clean-build FIX1 log `notmuch-browser-operator-20260725-161836-320.log`, SHA256 `8f4e13cb...`, passed from exact clean commit archive through frozen Bun install, module verification, deterministic templ/Tailwind generation, Go unit/vet/race, 46 Python tests, shebang-aware shell syntax, asset budget, stripped build, checksummed evidence, and unchanged production. The safe 110-member source tar is 2,232,320 bytes, SHA256 `4d0b6d9c...`; candidate `c7d14e0e...` is 8,093,961 bytes; assets total 86,332 bytes; manifest/report hashes are `57d6e360...`/`7f13bd3d...`; pointer hash is `47a65fdb...`. Independent audit reverified every artifact, approved Go module/build metadata, glibc-only dynamic linkage, private containment, 46 MiB total footprint, zero candidate processes, free 8876, and exact healthy production.
- Isolated-runtime log `notmuch-browser-operator-20260725-172108-312.log`, SHA256 `e3f4b036...`, passed with exact retained build/pointer evidence and started candidate PID 110502 only on `127.0.0.1:8876`. Candidate and production both reported OK/read-only/non-mutating health at 1,628 messages/2,825 files. Root, HTMX search/message, status, three exact static assets, HTMX hardening, CSP/security headers, POST 405, missing-route 404, empty isolated temp, unchanged production PID/start ticks/hash, and 20,404 KiB candidate RSS all passed. The mode-600 runtime pointer has SHA256 `e32b23c9...`; the candidate was intentionally retained after exit.
- A later antiX reboot gracefully stopped that retained candidate before manual review: its log appended only `shutdown=begin` and `shutdown=complete`, the recorded boot ID `71f26ff6...` differs from current boot `dce3319f...`, PID 110502 is gone, exact candidate process count is zero, port 8876 is free, and the isolated runtime temp is empty. Production restarted normally on localhost 8765. The stale pointer, HTTP evidence, log, and build artifacts remain preserved; the next gate uses fresh identity-bound runtime paths and deletes nothing.
- Isolated-runtime restart log `notmuch-browser-operator-20260725-175149-547.log`, SHA256 `1158c7e2...`, preserved and revalidated every prior runtime artifact, confirmed the reboot boundary, and passed the complete automated gate again with exit 0. Exact candidate PID 15740/start ticks 65744 is retained only on `127.0.0.1:8876`, pointer SHA256 is `d6e02a4d...`, candidate RSS was 16,052 KiB at the gate, and its isolated temp was empty. Candidate and unchanged production PID 2191/start ticks 2753 both reported OK/read-only/non-mutating health at 1,632 messages/2,829 files; all HTTP/HTMX/static/security/rejection checks passed. Direct post-log review reconfirmed both listeners, process identities, equal current health, mode-600 pointer/log, mode-700 evidence/temp directories, and empty candidate temp. Manual antiX GUI acceptance is next; no backup, install, runit cutover, stop, or cleanup has run.
- The owner then reported `antiX candidate GUI: PASS` after manually reviewing the retained combined-migration candidate at `http://127.0.0.1:8876/` against the requested search, results/subbar, reader metadata/copy controls, splitter, Readable/Original/plain rendering, attachment/ZIP, image-confirmation/reset, narrow-layout, status, clipping, and control checks. This is owner-reported GUI acceptance; PID 15740 remains retained pending a separate read-only post-review identity gate. No production backup/install, runit action, stop, or cleanup is yet authorized or complete.
- Post-GUI audit log `notmuch-browser-operator-20260725-180917-297.log`, SHA256 `1c557593...`, passed with exact owner PASS context, clean commit, build/pointer identities, candidate PID 15740/start ticks 65744/boot ID/executable/command line/hash, loopback-only 8876, clean mode-600 log `0d4d54a...`, empty mode-700 runtime temp, and retained 14-file HTTP evidence. Production PID 2191/start ticks 2753/hash/listener remained exact; both health endpoints matched at 1,632 messages/2,829 files; root/search/status returned 200; locks were absent; candidate RSS was 17,988 KiB. No stop, cleanup, backup, install, runit action, or production change occurred. The Windows route to the VM uses antiX `192.168.254.128`; SSH listens on port 22 while candidate 8876 remains localhost-only.
- The owner then reported `Windows candidate GUI: PASS` after reviewing the retained candidate through `ssh -N -L 8876:127.0.0.1:8876 atiq@192.168.254.128`. antiX directly observed an established SSH connection from Windows `192.168.254.1` to `192.168.254.128:22` while candidate PID 15740 remained bound only to `127.0.0.1:8876`. Candidate and production health matched at 1,633 messages/2,830 files; the one-message increase since the prior gate occurred equally through normal background mail/index activity. Pointer/log hashes remained `d6e02a4d...`/`0d4d54a...`, and isolated temp remained empty. A separate post-Windows identity gate is required before any stop, cleanup, backup, install, or runit action.
- Post-Windows audit log `notmuch-browser-operator-20260725-182329-920.log`, SHA256 `b40182fc...`, passed with the owner Windows PASS, one established Windows-to-antiX SSH connection, exact retained pointer/process/start/boot/executable/command/hash/listener/log/temp/evidence identities, and localhost-only candidate 8876. Production PID 2191/start ticks 2753/hash/listener stayed exact; both health endpoints matched at 1,633 messages/2,830 files; root/search/status returned 200; locks were absent; candidate RSS was 17,184 KiB. No production change, candidate stop, cleanup, backup, install, or runit action occurred. The next gate may gracefully stop only exact candidate PID 15740 while retaining every evidence file and rechecking production.
- Candidate-stop log `notmuch-browser-operator-20260725-182556-247.log`, SHA256 `f68cb0ea...`, repeated all retained candidate and production identity/health/lock/temp checks, then sent SIGTERM only to exact PID 15740. It exited after one 0.1-second check, appended exact `shutdown=begin`/`shutdown=complete` markers, and produced final log SHA256 `5061d9f5...`. PID 15740 is absent, port 8876 is free, runtime temp is empty, and the mode-600 pointer plus all 14 HTTP evidence files remain preserved. Production PID 2191/start ticks 2753/hash/executable stayed exact and health remained read-only at 1,633 messages/2,830 files. No file was deleted and no cleanup, backup, install, or runit action ran.
- Pre-install rollback log `notmuch-browser-operator-20260725-183017-158.log`, SHA256 `b836b24b...`, created and twice verified private backup `/mail/Backups/notmuch-browser/20260725-183017-619-before-templ-runit-migration` without stopping or changing production. Its seven-item inventory SHA256 is `1b6f56c4...`, rollback script `b6582028...`, state record `8168fe64...`, total size 9,746,622 bytes, and mode-600 pointer `/mail/AppData/notmuch-browser/templ-runit-rollback-current` has SHA256 `e8d417d9...`. Exact old browser/control/index/config/startup bytes and modes are preserved; the rollback script is syntax-valid. Independent review repeated every inventory checksum and confirmed the private modes/ownership. Production PID 2191/start ticks 2753 and 1,633/2,830 health remained unchanged; candidate install and runit staging/activation did not run.
- Atomic direct-install log `notmuch-browser-operator-20260725-183445-716.log`, SHA256 `fcf9cfac...`, verified rollback evidence, paused provider-live, stopped direct index PID 2110 and browser PID 2191, atomically installed browser `c7d14e0e...`, control `4b8231a2...`, index control `1ddf93c9...`, and runit helper `8a81ab50...`, then restarted direct browser PID 51685 and index PID 51772. New loopback-only production passed health/root/HTMX search/status at 1,633 messages/2,830 files; locks cleared; provider-live PID 2130 resumed unpaused with last exit 0. Config, IceWM startup, and rollback pointer stayed exact; runit definitions/links remained absent; automatic rollback was not invoked. Direct follow-up independently matched all four installed hashes, process/control supervisor status, listener, zero temp, and provider-live state.
- Disabled per-user-runit stage log `notmuch-browser-operator-20260725-183750-568.log`, SHA256 `22c953f6...`, created only the managed browser/index definitions under `~/.runit/usersv` with both `down` markers present and both active links absent. Exact staged script/config hashes, modes, ownership, syntax, helper contracts, active user runsvdir, and singular legacy IceWM markers passed. Direct browser PID 51685/start ticks 323396 and index PID 51772/start ticks 323586 remained unchanged; health stayed read-only at 1,633 messages/2,830 files; provider-live PID 2130 remained running, unpaused, with last exit 0. The installed artifacts, config, startup, and pre-install rollback pointer stayed exact, and the runit helper's activation rollback pointer remained absent. Independent follow-up rehashed every staged file and repeated the disabled-link, helper-inspect, direct-supervisor, health, listener, and provider checks. No activation, process stop, or production interruption occurred.
- Per-user-runit activation attempt log `notmuch-browser-operator-20260725-184516-714.log`, SHA256 `f9139c6f...`, stopped at its first artifact-mode preflight because the installed browser binary is correctly mode 755 while the wrapper mistakenly expected 700. It did not reach provider-live pause or the activation helper. Failure diagnostics and independent review confirmed both active links and the activation rollback pointer absent, both legacy IceWM markers present, direct browser PID 51685/start ticks 323396 and index PID 51772/start ticks 323586 unchanged, read-only health at 1,634 messages/2,831 files, and provider-live PID 2130 running unpaused with last exit 0. No backup, process stop, service activation, rollback, or production change occurred; the replacement changes only the browser mode expectation to 755.
- Per-user-runit activation attempt 2 log `notmuch-browser-operator-20260725-184647-916.log`, SHA256 `8dd3bc36...`, passed the corrected complete preflight, paused provider-live, created verified backup `/mail/Backups/notmuch-browser/20260725-184648-before-user-runit`, and stopped the direct processes. Activation then encountered a runsvdir discovery race: `sv up` ran before the newly linked browser service had a `supervise/ok` pipe. The helper automatically restored both `down` markers and IceWM blocks, removed both exact active links, restarted direct browser PID 63971 and index PID 64002, and archived the failed pointer as `user-runit-rollback-current.failed-20260725-184650`; the wrapper resumed provider-live PID 2130 unpaused with last exit 0. Independent review verified the four backup checksums, restored startup SHA256 `16f165b0...`, absent active links/current pointer, healthy read-only 1,634/2,831 production, localhost-only 8765, and restored direct supervision. The source helper now waits up to 20 seconds for each supervisor pipe while both services remain down before removing their `down` markers and calling `sv up`.
- Runit-helper readiness-fix install log `notmuch-browser-operator-20260725-185029-854.log`, SHA256 `98381506...`, backed up the installed helper under `/mail/Backups/notmuch-browser/20260725-185030-185-before-runit-helper-readiness-fix` and atomically replaced only that helper. Installed and repository bytes now match SHA256 `f56f50ba...`, mode 700, with the bounded supervisor-wait contract and valid shell syntax; backup manifest SHA256 is `7df26b84...` and restores the prior helper `8a81ab50...`. Direct browser PID 63971/start ticks 395634 and index PID 64002/start ticks 395735 did not change, both runit links/current activation pointer remain absent, both `down` markers and IceWM blocks remain present, production is healthy/read-only at 1,635 messages/2,834 files, and provider-live PID 2130 remains running unpaused with last exit 0. No service lifecycle or provider-live pause occurred.
- Per-user-runit activation attempt 3 log `notmuch-browser-operator-20260725-185237-839.log`, SHA256 `7a0faa38...`, proved the readiness fix: activation succeeded with browser PID 70477 and index PID 70537 under user runit, exact links, loopback-only listener, unchanged 1,635/2,834 health, root/search/status 200, verified backup `/mail/Backups/notmuch-browser/20260725-185238-before-user-runit`, startup SHA256 `cd1c613c...` after removing only the two IceWM blocks, exact installed artifacts/config/pointers, and runit logs. After provider-live resumed, the wrapper incorrectly required immediate `last_exit=0`; the normal paused loop had written `paused 2026-07-25 18:52:42+0600` to the shared last-status file, so this valid transient caused the wrapper's conservative rollback. Rollback restored direct browser PID 71119 and index PID 71154, both down markers and IceWM blocks, removed both links, archived the pointer as `user-runit-rollback-current.used-20260725-185255`, and left provider-live PID 2130 running with `paused=no`. Independent review verified the backup/pointer, restored startup/helper hashes, localhost health, and direct supervision. The retry accepts only this documented transient after resume while retaining the pre-pause `last_exit=0`, running-loop, `paused=no`, and absent-lock requirements.
- Per-user-runit activation attempt 4 log `notmuch-browser-operator-20260725-185543-230.log`, SHA256 `cf5af81f...`, passed preflight and paused provider-live but exposed stale runit control state left by the prior successful-then-rolled-back cutover. Both old `supervise/ok` FIFOs still existed even though `sv status` reported `runsv not running`; the helper's FIFO-only wait therefore returned too early and `sv up` failed. Automatic rollback restored direct browser PID 74308 and index PID 74342, both down markers and IceWM blocks, removed both links, archived the pointer as `user-runit-rollback-current.failed-20260725-185546`, and resumed provider-live PID 2130 with `paused=no`. Independent review verified backup `/mail/Backups/notmuch-browser/20260725-185543-before-user-runit`, pointer SHA256 `4a91ad69...`, startup/helper hashes, localhost-only 1,635/2,834 health, and the stale-FIFO/live-runsv distinction. The source readiness loop now requires successful `sv status` rather than FIFO existence, proving a live runsv attachment while down markers remain present.
- Live-runsv readiness-helper install log `notmuch-browser-operator-20260725-185754-531.log`, SHA256 `186e0f7c...`, backed up helper `f56f50ba...` under `/mail/Backups/notmuch-browser/20260725-185754-849-before-runit-helper-readiness-fix` and atomically installed helper `0c575f41...` mode 700. Backup manifest SHA256 `85b6046e...` verifies; installed and repository bytes match and shell syntax passes. Direct browser PID 74308/start ticks 449158 and index PID 74342/start ticks 449259 remained exact, active links/current pointer stayed absent, down markers and both IceWM blocks stayed present, production remained healthy/read-only at 1,635/2,834, and provider-live PID 2130 remained unpaused with last exit 0. No service lifecycle or provider pause occurred.
- Per-user-runit activation PASS log `notmuch-browser-operator-20260725-185939-496.log`, SHA256 `0838a332...`, successfully replaced direct supervision with browser PID 79017/start ticks 473095 and index PID 79063/start ticks 473235 under the existing user runsvdir. Exact relative links, live runsv attachment, service checks/status, one process each, loopback-only 8765, unchanged 1,635/2,834 health, root/search/status 200, zero download temp, absent locks, and both runit logs passed. Backup `/mail/Backups/notmuch-browser/20260725-185940-before-user-runit` verifies with manifest SHA256 `08b3fdea...`; current pointer SHA256 is `479b7c0a...`. IceWM startup became SHA256 `cd1c613c...`, mode 700, and differs from backup only by removal of the two legacy browser/index blocks. Installed artifact/config/pre-install-pointer hashes stayed exact. Provider-live PID 2130 resumed with loop running, `paused=no`, and absent sync lock; its expected transient paused-cycle status was accepted. Rollback was not invoked. Independent audit repeated helper validation/inspect, exact PIDs/commands/listener/routes, backup/pointer/startup/artifact hashes, provider monitor state, and clean repository.
- Post-runit-cutover read-only audit log `notmuch-browser-operator-20260725-190319-083.log`, SHA256 `07457b52...`, independently bound the retained runit browser/index PIDs and start ticks, exact commands/links/listener, one process each, live service/check status, health at 1,635/2,834, root/search/status 200, zero temp, absent locks, runit logs, installed/config/startup/pointer/backup hashes, and the exact two-block-only IceWM delta. Provider-live PID 2130 and monitor PID 2217 were running, unpaused, unlocked, with last exit 0; the repository stayed clean at commit `3d46ce0...`. No lifecycle, mail, tag, or index mutation action ran. Direct follow-up repeated helper validation, PIDs, listener, health, pointer/manifest/startup hashes, and provider state.
- First strict read-only attempt log `notmuch-browser-operator-20260725-190748-261.log`, SHA256 `e1d7003b...`, paused provider-live, stopped only index PID 79063, and captured a stable before snapshot at 1,635 messages, 2,834 files, and 1,635 inbox messages. Indexed paths/content/tags were `596e34ef...`/`4928682f...`/`f328e82b...`; the 51,560-file Mailstore metadata hash was `f2a284c2...`; Maildir and download temporary counts were zero. Root/search/HTMX/status returned 200, POST returned 405, and invalid attachment/image capabilities returned 403. Before any valid capability download, the retained July validator `27331bc...` stopped because it expected audit-selected inline part 6 to remain a genuine attachment. The accepted browser correctly exposes part 6 only as an inline PNG and genuine attachments as parts 7, 8, and 9. The failure trap restarted the index as PID 90565/start ticks 525214, resumed provider-live, and retained browser PID 79017/start ticks 473095 unchanged; independent review verified live runit, localhost health, and restored background state. Evidence is preserved under `codex-output/notmuch-browser-operator/strict-readonly/20260725-190748-272`.
- Strict read-only attempt 2 log `notmuch-browser-operator-20260725-191549-402.log`, SHA256 `1ee76482...`, reproduced the exact stable 1,635/2,834/1,635 baseline and all hashes, route 200s, POST 405, and invalid-capability 403s. The corrected genuine-attachment selection advanced through both exact attachment downloads plus tampered/wrong-purpose rejection, then stopped before inline download or ZIP export because the validator searched only outer-page URLs; the accepted browser correctly places its single signed part-6 image URL inside the decoded sandboxed iframe `srcdoc`. The failure trap restarted the index as PID 101336/start ticks 573021, resumed provider-live, retained browser PID 79017/start ticks 473095, and left download temp empty. Independent review verified live runit and read-only 1,635/2,834 health. Evidence is preserved under `codex-output/notmuch-browser-operator/strict-readonly/20260725-191549-417`; no after snapshot or strict non-mutation PASS is claimed.
- The replacement strict validator now compiles and parses URLs from both the outer page and decoded iframe `srcdoc`; focused outer/inner URL and capability-deduplication tests bring the standard-library suite to 48 passing tests. Read-only discovery confirms both duplicate files expose exactly one capability for each genuine attachment part 7, 8, and 9, exactly one Save All capability, and exactly one inner inline capability for part 6. Direct notmuch output for attachment part 7 is 162,241 bytes with SHA256 `92afdde3...` in both duplicates; inline part 6 remains the audited 4,078-byte PNG with SHA256 `4814092b...`. The next operator run must execute the complete stable before/after proof with this validator.
- Strict browser non-mutation PASS log `notmuch-browser-operator-20260725-192006-234.log`, SHA256 `f96f7050...`, completed the exact batch `9d7d4bbd...`. With provider-live paused and the index loop down, root/search/HTMX/status returned 200, POST returned 405, and invalid/tampered/wrong-purpose capabilities returned 403. Genuine attachment part 7 matched direct notmuch bytes in both duplicates at 162,241 bytes/SHA256 `92afdde3...`; inline part 6 matched 4,078 bytes/SHA256 `4814092b...`; blocked/embedded/remote CSP and full-page permission reset passed. Both duplicate-specific Save All responses were valid safe three-entry Store ZIPs of 823,710 encoded bytes/823,218 decoded bytes with SHA256 `f1d4d873...`. Before and after snapshots are byte-identical with SHA256 `7f0312e7...`: 1,635 messages, 2,834 files, 1,635 inbox, indexed paths/content/tags `596e34ef...`/`4928682f...`/`f328e82b...`, 51,560 Mailstore files with metadata `f2a284c2...`, exact artifacts/config/startup/pointers/links/browser identity, and zero Maildir/download temp. Evidence manifest SHA256 is `49a8efdb...`. Browser PID 79017/start ticks 473095 never changed; index restored as PID 111776/start ticks 601483, provider-live resumed, user-runit validation passed, and final health remained read-only at 1,635/2,834. Independent review reproduced the snapshot and evidence hashes, verified restored services, and observed provider-live return to last exit 0.
- Resource-observation PASS log `notmuch-browser-operator-20260725-192843-725.log`, SHA256 `a1ca62b9...`, ran exact batch `16204ab8...` without lifecycle or mail/tag/index mutation actions. All 90 bounded concurrent localhost requests across health, root, full/HTMX search, status, static assets, and blocked/embedded message rendering returned HTTP 200 with zero errors, totaling 9,587,180 response bytes in 9,092 ms. Browser resources rose from 15,492 KiB RSS/eight threads/nine descriptors to peaks of 25,520 KiB RSS, 15 threads, and 55 descriptors, all below 65,536 KiB/64/128 limits; after three seconds it held 24,316 KiB RSS, 15 threads, and nine descriptors. CPU cost was 47 ticks. Browser PID 79017, index PID 111776, and provider PID 2130 retained their exact start identities; runit validation, loopback-only listener, read-only 1,635/2,834 health, repository/artifact/config/startup identities, absent locks, and zero temp all passed. Evidence manifest SHA256 is `d58c30ad...`; independent review reproduced it and reconfirmed healthy services.
- Controlled user-runit restart PASS log `notmuch-browser-operator-20260725-194616-350.log`, SHA256 `f98e36b6...`, ran exact batch `d7ad185c...`. After exact preflight and a verified 162,241-byte attachment download, provider-live paused, old index PID 111776 stopped, old browser PID 79017 shut down gracefully through the runit-aware control, browser PID 1965/start ticks 752934 started, and index PID 2009/start ticks 753076 started. Runit logs contain the browser's graceful shutdown/exit-0/start markers and the index's controlled signal-15 exit. The pre-restart capability became HTTP 403, the new process issued a different capability, and the fresh download again matched SHA256 `92afdde3...`; root/search/status returned 200. Health stayed read-only at 1,635/2,834; listener remained loopback-only; artifacts/config/startup/pointers and zero temp stayed exact. Provider PID 2130 resumed unpaused with last exit 0, runit validation passed, and neither rollback nor recovery ran. Evidence manifest SHA256 is `1f11eaab...`; independent review reproduced it, the log deltas, and all live identities.
- Pre-reboot readiness PASS log `notmuch-browser-operator-20260725-195437-708.log`, SHA256 `e04fee8d...`, ran exact batch `72efcf60...` without service lifecycle or mail/index mutation. It bound boot ID `dce3319f-0e3c-47f8-a831-682f9992bb56`, antiX session launcher `/etc/user_session.d/runit-user-session.sh` SHA256 `d14e756a...`, session wrapper PID 1907/start ticks 2696, runsvdir PID 1927/start ticks 2699, browser PID 1965/start ticks 752934, index PID 2009/start ticks 753076, and provider PID 2130/start ticks 2742. Exact artifacts/config/startup/rollback pointers/links, helper validation, live runsv parents, loopback-only listener, root/search/status 200, read-only 1,635/2,834 health, absent locks, and zero download temp passed. The six-file evidence manifest SHA256 `cefd3033...` reproduced independently. Mode-600 reboot pointer `/mail/AppData/notmuch-browser/templ-runit-reboot-proof-current.env` has SHA256 `dd82df4b...`; its only persistent effect is the exact pre-reboot baseline. The boot ID and all live identities remained unchanged during independent review, and no reboot command ran.
- First post-reboot attempt log `notmuch-browser-operator-20260725-200653-787.log`, SHA256 `a155f079...`, proved the boot changed to `aa4a4439-5a0e-424c-8aae-18e4b2415ce0` and passed the exact committed repository, baseline pointer, artifact, config, startup, launcher, rollback-pointer, and active-link checks. It then stopped at the helper's first health validation because browser/index were down; no verifier evidence directory, recovery, lifecycle action, or mail/tag/index mutation occurred. Independent diagnosis found the surviving antiX session wrapper PID 1898, runsvdir PID 1918, and both live runsv supervisors, but both services were `down ... normally up` with no 8765 listener. Cross-reboot logs show the browser and index initially started, then received repeated clean stop cycles through 20:04:03. This matches late `sv stop` actions from antiX `/etc/user_session.d/runit-user-session.sh` wrappers exiting while the final runsvdir survives. Provider-live PID 2105 remained running/unpaused with last exit 0, counts stayed 1,635/2,834, locks and download temp stayed clear, and the repository remained clean.
- The repository runit helper now contains a user-level repair for that antiX session race: one idempotent IceWM reconciliation block calls only the managed browser/index services, waits for `/mail` and the existing runsvdir, reasserts `sv up` when required, and demands 60 continuous healthy seconds inside a 180-second bound. It never edits `/etc`, installs dependencies, or changes mail/tags/index content. An isolated temporary-HOME test proved repair, singular markers, startup preservation/mode, repeated idempotency, and healthy validation; the full standard-library suite passes 49 tests, and all shell files pass syntax under their declared interpreters. Live helper/startup installation and current-boot recovery remain a separate guarded gate before another reboot.
- User-runit session-reconcile repair PASS log `notmuch-browser-operator-20260725-201828-121.log`, SHA256 `8586d9e2...`, ran exact batch `54a22872...`. It backed up helper `0c575f41...` and startup `cd1c613c...` under `/mail/Backups/notmuch-browser/20260725-201828-140-before-user-runit-session-reconcile`; manifest SHA256 `e474ee5b...` reproduces, and mode-600 pointer SHA256 is `229bd8d9...`. It atomically installed tested helper `598762b0...`, added exactly one reconciliation block while preserving startup mode 700 and every prior nonblank line, issued one repair for the two expected normally-up/down services, then proved 60 continuous healthy seconds. Browser PID 17403/start ticks 89928 and index PID 17456/start ticks 90069 run beneath the retained runsvdir; helper validation, loopback 8765, root/search/status 200, read-only health, absent locks, and zero temp passed. Normal provider/index activity advanced counts from 1,635/2,834 to 1,636/2,835 during the window; provider PID 2105/start ticks 2357 remained running/unpaused with last exit 0. Recovery and rollback were not invoked. Evidence manifest SHA256 `5a5549ec...`, backup, pointer, installed/source helper equality, startup-only delta, live identities, listener, health, and provider state all reproduced independently.
- Repaired pre-reboot readiness PASS log `notmuch-browser-operator-20260725-202652-189.log`, SHA256 `ed8bbec0...`, ran exact batch `0d7a8cf3...` without lifecycle or mail/tag/index mutation. It bound boot ID `aa4a4439-5a0e-424c-8aae-18e4b2415ce0`, session wrapper/runsvdir PIDs 1898/1918, browser PID 17403/start ticks 89928, index PID 17456/start ticks 90069, provider PID 2105/start ticks 2357, and browser/index runsv parents. Exact helper `598762b0...`, startup `831fe2d7...`, singular reconciliation/absent legacy markers, artifacts/config/launcher/links, v1/repair pointers and backup manifest, helper validation, loopback 8765/free 8876, root/search/status 200, read-only 1,636/2,835 health, absent locks, and zero temp passed. Seven-file evidence manifest SHA256 `c3f3fa4d...` reproduced. Mode-600 v2 pointer `/mail/AppData/notmuch-browser/templ-runit-reboot-proof-v2-current.env` has SHA256 `64935da5...` and records log offsets 2,377/1,161 bytes. Independent review reproduced all hashes/identities and confirmed the boot remained unchanged; reboot executed=no.
- Second post-reboot user-runit PASS log `notmuch-browser-operator-20260725-203802-977.log`, SHA256 `fe18ec8f...`, ran exact batch `11693b77...` and proved boot ID changed to `5c996053-b1ab-4904-aa41-a0fbd1420fa5`. The current-boot reconciliation log SHA256 `4cac162a...` recorded three repairs while antiX session traps settled, then 60 continuous healthy seconds and `status=user_runit_session_reconciled`. Fresh wrapper/runsvdir PIDs 2157/2177, browser PID 4504/start ticks 7231, index PID 4773/start ticks 7374, provider PID 2376/start ticks 3757, and exact runsv ancestry passed. Cross-reboot browser/index log deltas, helper `598762b0...`, startup `831fe2d7...`, v2/repair pointers, backup manifest, artifacts/config/launcher/links, loopback 8765/free 8876, root/search/status 200, read-only 1,636/2,835 health, and exact 162,241-byte attachment SHA256 `92afdde3...` all passed. Provider remained running/unpaused with last exit 0; locks and download temp were zero. Eleven-file evidence manifest SHA256 `4af19fb2...` reproduced independently. No lifecycle, mail/tag/index mutation, rollback, or recovery action ran in the verifier. This completes the combined migration's reboot gate.
- The prior Windows annotated-GUI revision remains uninstalled. Its dedicated result-toolbar band and detached copy-control layout were rejected at the isolated manual gate. The active revision moves the query/count/pager summary into the search shortcut subbar with HTMX out-of-band updates, places copy controls directly after their text, adds a three-letter weekday to parsed reader dates, and baseline-aligns metadata while preserving the accepted horizontal workspace and all backend behavior.
- Annotated-GUI preflight on 2026-07-18 confirmed healthy production at 1,194 messages and 2,141 files with exact installed hashes, localhost-only binding, safety configuration, lock/temp/startup invariants, and 102 GB free on `/mail`. Follow-up inventory and verification proved all seven retained July 13 rollback inventory entries exact, the restore script syntax-valid and limited to the three browser artifacts, and production unchanged. The validated filenames are `rollback-browser.sh` and `backup-inventory.sha256`; the final regenerated reconstruction guides now use those corrected names.
- Annotated-GUI source transfer passed from Windows on 2026-07-18: the local 68,359-byte, 31-member bundle matched SHA256 `a371f5cbb76a173249eac0ad4ac94fff6af1d6c576d455aae7f297256425b8ff`, SSH port 22 was reachable, SCP completed, the remote incoming archive independently returned `OK`, and it was renamed to `/home/atiq/notmuch-browser-annotated-gui-src-20260718-135325.tar.gz`. No extraction, build, or production action has run yet.
- antiX independently verified and safely extracted the annotated-GUI bundle, confirmed all 31 members were unique safe regular files, verified Go 1.26.4 plus chi v5.3.1 and x/net v0.57.0, and passed module verification, `go test -count=1 ./...`, `go vet ./...`, shell syntax, exact asset hashes, and size budgets. The staged Linux binary is 9,736,457 bytes with SHA256 `1db5b2a0175e29d753a62b24f99be8375d4c97b8435130c86f35c0555b9011e0`; production remained the prior attachment/image release and port 8876 remained free.
- The first annotated-GUI isolated run started the exact staged binary successfully on `127.0.0.1:8876`, passed health, loopback-listener, security-header, and exact static-asset checks, then stopped safely before GUI assertions because the harness required a historical July 15 Message-ID that is no longer guaranteed in the live inbox index. The application emitted no error and the cleanup trap ran. The replacement harness must discover current Cc/attachment/duplicate samples read-only instead of hard-coding mailbox content.
- Read-only dynamic discovery then scanned all 1,194 current message metadata records and produced a mode-600 sample file containing a current Cc message, an attachment message with three indexed files, and a duplicate message with three indexed files. The historical fixed ID count is zero, confirming harness drift rather than an application regression. Port 8876, prior isolated temp, production temp, Maildir temp, staged hash, and production health all remained clean.
- The first dynamic-sample isolated rerun again started cleanly, passed health/listener/security/assets and the first eleven GUI assertions, then the harness incorrectly treated a message-count-derived offset as a stable final `notmuch show` page while live mbsync/indexing and show pagination semantics can invalidate that assumption. Cleanup correctly stopped port 8876, causing the later browser connection refusal. The corrected gate must pause for manual inspection immediately after startup and test disabled Next with a stable single-ID query instead of deriving a live final offset.
- The corrected isolated GUI gate then started the exact staged binary successfully on `127.0.0.1:8876`, passed health/listener startup, and paused for manual review. The owner entered `FAIL, need some GUI modification`; the trap stopped the isolated process and freed the port. Source bundle SHA256 `a371f5c...` and staged binary SHA256 `1db5b2...` are superseded and were never installed. Production remains the validated attachment/image release.
- The replacement GUI2 revision is implemented and locally validated. It moves the query/count/range/pager cluster into the search shortcut subbar, updates that external slot through HTMX out-of-band swapping, removes the dedicated result-toolbar band, keeps all four copy controls immediately after their text, renders parsed reader dates with a weekday, and baseline-aligns the metadata grid. No route, dependency, notmuch command, security control, or mail/index behavior changed.
- GUI2 browser QA passed at 1440x900, 1024x768, and 390x844 with no horizontal overflow. The desktop summary remained right-aligned, the mobile summary wrapped below shortcuts, live HTMX search updated both the external summary and result body, all four clipboard checks returned the exact required values, and the reader date/metadata baseline checks passed. The disposable listener was stopped and port 8876 returned to free state.
- Minimal bundle `notmuch-browser-gui2-src-20260718-160432.tar.gz` contains 31 unique safe regular files, is 68,653 bytes, and has SHA256 `605453250348370000cf90394f01d778545286a8c2bb4042c25820066ea7bdfd`. A fresh extraction passed `go mod verify`, `go test -mod=readonly -count=1 ./...`, `go vet -mod=readonly ./...`, both shell syntax checks, and a stripped Windows build. CSS/app JS/HTMX are 21,892/5,476/51,238 bytes, totaling 78,606 bytes. The bundle has not been transferred to antiX and production remains unchanged.
- GUI2 Chunk 1 revalidated healthy antiX production at 1,196 messages and 2,143 files: installed hashes, browser/index/mbsync state, health/security/listeners, safety config, four ignore entries, locks/temp files, startup markers, rollback inventory, and capacity all matched. Its single blocked result was a validation-script error that demanded `evolution/local-maildir`; the exact established `new.ignore` basename is `local-maildir`, and it was present. A small read-only correction gate is required before source transfer; production was not changed.
- GUI2 Chunk 1 FIX1 closed that harness mismatch successfully. The exact four basename exclusions matched, `/mail/Mailstore/evolution/local-maildir` exists and has zero paths among the 2,143 indexed files, all installed production hashes stayed exact, health remained read-only at 1,196/2,143, only localhost port 8765 listened, port 8876 was free, and locks/download/Maildir temp were zero. The fresh preflight is complete; exact Windows bundle verification and transfer are next, without extraction or build.
- GUI2 Chunk 2A verified and transferred only the final bundle from Windows. Local and antiX size 68,653, SHA256 `605453250348370000cf90394f01d778545286a8c2bb4042c25820066ea7bdfd`, and the 31-member ordered inventory matched; SSH was reachable, incoming/final remote checks returned `OK`, and `/home/atiq/notmuch-browser-gui2-src-20260718-160432.tar.gz` is mode 600. Nothing was extracted, built, installed, stopped, restarted, or indexed. Independent antiX extraction/test/vet/temporary-build validation is next.
- GUI2 Chunk 2B independently confirmed all 31 archive members safe and exact, extracted without symlinks, passed GUI2 source contracts, exact asset hashes/budgets, module pins, `go test`, `go vet`, and shell syntax under antiX Go 1.26.4. The temporary stripped Linux binary is `/home/atiq/codex-runs/notmuch-browser-gui2-src-20260718-160432/notmuch-browser`, 9,736,457 bytes, SHA256 `472f7921118853bc22c55f097b56438998880448a929a5cb6441fefd9cf553cd`; staged controls retain their expected hashes. Production remained exact and healthy at 1,196/2,143, port 8876 free, and locks/temp zero. An isolated port-8876 validation with an immediate manual GUI pause is next.
- GUI2 Chunk 3 then started that exact temporary binary on `127.0.0.1:8876`; health, loopback binding, exact assets, HTMX out-of-band toolbar updates, stable paging, metadata/date/copy rendering, attachment/Save All, duplicate selection, and all 24 owner GUI checks passed. The validation harness exited silently at its route/rejection section before printing individual results, and cleanup correctly stopped the isolated process and emptied temporary state. Production was not changed. Bundle SHA256 `60545325...` and binary SHA256 `472f7921...` are superseded and must not be installed.
- The active replacement classifies selected-HTML image references before building attachments: normal CID/relative inline resources remain renderable through signed capabilities but disappear from Attachments and Save All; explicit attachments remain downloadable even when referenced. Embedded images stay confirmation-gated and remote images retain the second warning. Typography uses locally installed Inter for subjects, Aporetic Sans Mono for Message-IDs and plain text, and Aptos with system fallbacks for unstyled HTML. No font file will be committed, served, or included in a bundle.
- Body Images Chunk 1 passed the fresh antiX read-only preflight on 2026-07-18 with zero audit failures. Production remained exact and healthy at 1,196 messages and 2,143 files, browser/index/mbsync loops were running, only `127.0.0.1:8765` listened, port 8876 was free, both locks and all browser/Maildir temporary-file counts were zero, all four exclusions and both notmuch safety settings matched, all IceWM markers were singular, and the retained July 13 rollback inventory and script verified. `/mail` had 102 GB free. Inter Variable, Aporetic Sans Mono Nerd Font, and Aptos were absent; the next transfer may stage the approved Inter and Aporetic files for user-local installation, while Aptos remains on the declared fallback unless its Microsoft license is separately confirmed.
- Body Images Chunk 2A then verified and transferred the exact 73,310-byte no-font source bundle plus the owner-supplied Inter Variable and Aporetic Sans Mono Nerd Font files from Windows into isolated antiX staging. Local hashes and sizes matched, the archive had 31 members and zero font payloads, SSH port 22 was reachable, guarded remote incoming-hash verification passed, and final staged modes/sizes were 600 and exact. Aptos was deliberately not transferred because its installation license is not confirmed. No extraction, font installation, build, service action, index action, or production/mail mutation occurred.
- Body Images Chunk 2B reproved exact production hashes, read-only health at 1,196/2,143, localhost-only port 8765, and free port 8876; independently verified all three staged hashes/sizes/modes; safely extracted the exact 31 regular members; and verified all three frontend assets and the 78,989-byte budget. It then stopped before Go tests, build, or font installation because the harness incorrectly required literal `go 1.25` while the valid module directive is `go 1.25.0`. This is a validation-script defect, not a source failure. A small FIX1 must resume from the already verified extraction and accept the semantic Go 1.25 floor.
- Body Images Chunk 2B FIX1 confirmed that neither the temporary binary nor font destination existed and that both operational locks were absent, then stopped at its first production hash comparison. The production binary printed the correct established SHA256 `50e6b1ad...38f37e`; the FIX1 script's expected literal accidentally omitted `6f` after `...6d`. No test, build, font, service, index, or mail action ran. FIX2 only needs to replace that one incorrect literal in the saved batch, syntax-check it, and rerun the otherwise unchanged FIX1.
- Body Images Chunk 2B FIX2 was skipped without changing state. The owner's normal launcher replaces `~/codex-runs/codex-batch.sh` with each supplied chunk before execution, so the self-patching FIX2 inspected its own `OLD_HASH` and `NEW_HASH` variables and correctly found both strings once. It exited before patching or running the prior workflow. FIX3 must therefore be a self-contained resume script that directly validates the existing extracted source instead of attempting to patch a previous batch file.
- Self-contained Body Images Chunk 2B FIX3 passed under antiX Go 1.26.4. It reproved the exact 31-file no-font source, module pins, 78,989-byte frontend budget, module verification, Go tests, vet, both shell syntax checks, and a stripped temporary build. The mode-700 Linux binary is 9,756,937 bytes at `/home/atiq/codex-runs/notmuch-browser-body-images-chunk2b-20260718-185644/notmuch-browser` with SHA256 `47d763294e0687f4949b4b6d00147e89af4e9b6a9e3862ab666e7678c80e1c67`. Inter Variable and AporeticSansMonoNerdFont were atomically installed mode 600 under `~/.local/share/fonts/notmuch-browser`, retained exact source hashes, and resolve through fontconfig to those files; Aptos remains absent and resolves to Noto Sans fallback. Production artifacts, read-only 1,196/2,143 health, localhost listener, free 8876, absent locks, zero browser/Maildir temp, and source hashes remained unchanged. Isolated validation of only this exact temporary binary is next.
- Body Images Chunk 3 started the exact temporary binary as PID 47881 on only `127.0.0.1:8876` with isolated temporary storage. Health, executable identity, dual loopback listeners, all exact static assets, no served fonts, root security headers, full/HTMX search, status, all invalid-capability 403s, all POST 405/Allow-GET checks, and unchanged production passed. Its pre-manual candidate finder then rejected all reviewed queries because it required a signed `/inline-image` capability; that is too narrow for data-URI or HTML-embedded images that need no capability route. The cleanup stopped PID 47881, freed 8876, and left isolated temp empty, explaining the later browser connection refusal. No manual gate or production/mail/index change occurred. The replacement must pause immediately after basic startup checks and let the owner select the real message manually.
- Body Images Chunk 3 FIX1 then reached the manual gate correctly with PID 53348 healthy on `127.0.0.1:8876`; exact binary/fonts/production, health/listeners, static assets, no served fonts, security, full/HTMX search, status, and rejection checks all passed. The owner's launcher is non-interactive, so `input()` immediately received EOF despite printing the prompt. Cleanup again stopped the isolated process, freed 8876, and left temp empty. The next attempt must deliberately retain a detached isolated process after the batch exits, record a private pointer, accept the owner's manual result through the Codex conversation, and run a separate post-validation cleanup chunk.
- Body Images Chunk 3 FIX2 successfully retained PID 57241 on `127.0.0.1:8876` after the non-interactive batch exited, with exact binary/fonts/production, read-only 1,196/2,143 health, loopback listeners, exact assets, search/HTMX, and invalid-capability checks passing. Private mode-600 pointer `/mail/AppData/notmuch-browser/body-images-isolated-current.json` records the process/run/temp/log. Manual review failed checks 12, 13, and 20 plus additional rendering quality: the red Mustang logo and some data-embedded icons render, but several signature resources remain broken placeholders or filename links after remote mode, including social and certification images; large blank body areas and oversized sender HTML typography/layout are also visible. Remote permission cannot repair relative local references. The genuine ZIP remains the sole attachment in the reviewed message, so attachment exclusion appears directionally correct. Privacy-limited MIME/reference diagnostics are required before source edits, followed by verified stop/cleanup of retained PID 57241.
- The retained-pointer audit then confirmed pointer SHA256 `7ac41147...`, mode 600, PID 57241, candidate SHA256 `47d76329...`, process start ticks, boot ID, and loopback-only port 8876 all still match. Production remains healthy and exact on loopback port 8765 at 1,196 messages/2,143 files. The owner has separately supplied local Aptos and Iosevka SS14 archives and approved user-local installation; both archives require safe member/license/family/style inspection first, and no font payload may enter Git, the source bundle, or served frontend assets.
- The owner-local font archive audit passed without installation. Aptos archive SHA256 `6528fd12...` has 28 valid static TTFs and its Microsoft EULA; Iosevka SS14 archive SHA256 `e8d34231...` has 54 valid static TTFs. Both ZIPs passed CRC, safe-path, duplicate, size, and fontconfig metadata checks; private report SHA256 is `f12a40df...`. The resource-efficient installation target is four core styles per family rather than all 82 files, with the Aptos EULA retained locally and no font copied into repository or browser assets.
- The atomic user-local font installation then passed. Aptos and Iosevka SS14 Regular, Italic, Bold, and Bold Italic all retained exact audited hashes and resolve through fontconfig to the intended subdirectories under `~/.local/share/fonts/notmuch-browser`; eight TTFs total 9,925,356 bytes. The Aptos EULA was preserved, install report SHA256 is `fdaff735...`, and both production 8765 and retained candidate 8876 remained healthy at 1,196 messages/2,143 files. No browser restart, application artifact change, mail/index mutation, or font redistribution occurred.
- The privacy-limited MIME diagnostic then produced nine records with zero errors and proved all ten `cid:` references in each affected image-bearing duplicate match ten inline PNG MIME parts exactly. Neither raw MIME nor notmuch JSON contains `Content-Location`, so filename/location matching is not the defect. The two-slot `/inline-image` handler instead returns immediate 429 responses to the remaining requests in the browser's ten-image burst, explaining why only a few logos render. Office page-level margins up to 70.85pt/1in also explain excessive whitespace. Private report SHA256 is `7c037a5c...`. PID 57241 was revalidated and stopped; port 8876, pointer, and isolated temp are gone, while production remains healthy/read-only at 1,196/2,143.
- Windows transfer of replacement bundle `d9943dfe...` passed exact local size/hash, ordered 31-member inventory, duplicate/path/no-font checks, SSH reachability, guarded remote incoming verification, and final remote `sha256sum -c`. The mode-600 final archive is `/home/atiq/notmuch-browser-body-images-queue-fix-src-20260718-204012.tar.gz`. Production remained healthy/read-only at 1,196 messages/2,143 files and port 8876 stayed free. Nothing was extracted, built, installed, stopped, restarted, or indexed.
- The first antiX replacement verify/build attempt independently passed the archive size/hash/mode, all production hashes and health/listeners, four local font resolutions, exact 31-member safe extraction, and zero symlink/font checks. It then stopped on a harness-only quoting mismatch: the Python assertion demanded backslash-escaped quotes around `Segoe UI`, whereas the Go raw string correctly contains ordinary quotes. No module/test/vet/race/build/pointer step ran and production remained unchanged. FIX1 can securely resume from `/home/atiq/codex-runs/notmuch-browser-body-images-queue-fix-build-20260718-205222/src` after comparing every extracted byte with the archive.
- Queue-fix antiX build FIX1 passed completely. It revalidated every byte of all 31 extracted files against bundle SHA256 `d9943dfe...`, confirmed zero symlinks and zero source fonts, passed the corrected queue/download/iframe/sender-style contracts, `go mod verify`, Go tests, vet, shell syntax, and the antiX race test under Go 1.26.4. The mode-700 temporary Linux binary is `/home/atiq/codex-runs/notmuch-browser-body-images-queue-fix-build-20260718-205222/notmuch-browser`, 9,756,937 bytes, SHA256 `f12882ab30278c5a95b41ad7ec921362f84dd472ae4badf53af6f013e7673458`. Private mode-600 pointer `/mail/AppData/notmuch-browser/body-images-queue-fix-build-current.json` has SHA256 `081b9232...`. Production remained unchanged and healthy/read-only at 1,196 messages/2,143 files, only port 8765 listened, and browser/Maildir temporary-file counts stayed zero. Isolated port-8876 burst and manual rendering validation is next; no production backup or installation is authorized yet.
- Queue-fix isolated validation then passed every automated gate with retained PID 121748 on loopback-only `127.0.0.1:8876`: exact candidate/build/archive/production identities, local fonts, routes/assets/security, blocked/embedded/remote modes, ten unique signed CID URLs, ten concurrent HTTP 200 image responses in at most 200 ms, zero 429 responses, one genuine ZIP attachment, and zero isolated/production temporary files. Manual screenshot `gui4.png` nevertheless rejected the preserved Word/Outlook typography as oversized serif text compared with the more readable `gui3.png` reference. Bundle `d9943dfe...` and Linux binary `f12882ab...` are therefore superseded for installation while retained as successful queue-fix evidence. The approved replacement defaults to Office-only Aptos 10.5pt Readable rendering and offers the current sanitized sender formatting through an Original toggle; production remains unchanged.
- The Readable/Original replacement is implemented and locally validated. `GET /message` now resolves `display=readable|original`, defaults invalid or missing values to Readable, preserves display/image/duplicate state according to the approved privacy rules, and keeps full-page image permission blocked. Strong Word/Outlook markers are detected with the existing bounded tokenizer; only Office HTML in Readable mode receives the local Aptos 10.5pt/1.35 override, while Original remains sanitized sender formatting and non-Office HTML keeps its current styling. Focused URL-state, Office-detection, normalization, fallback, CSP, attachment, and existing GUI tests pass. Browser inspection at 1440x900, 1024x768, and 390x844 found no horizontal overflow and confirmed the compact segmented control. No route, dependency, notmuch command, font payload, mail/index behavior, or production artifact changed.
- Outlook Readable Cleanup Chunk 1A then completed a read-only retained-candidate audit with zero failures. Mode-600 pointer SHA256 `fd5ddf65...` identified PID 121748, binary SHA256 `f12882ab...`, boot ID `dddcf62a-7fa2-4d3f-9819-78b3f70d2658`, process start ticks 3591448, the exact isolated command line, and empty isolated temp path. PID 121748 alone listened on `127.0.0.1:8876`; production PID 2405 alone listened on `127.0.0.1:8765`; both health endpoints were read-only at the same live 1,198-message/2,145-file state. All three installed production hashes remained exact and both temporary directories were empty. No process, pointer, file, service, mail, tag, or index state changed during this audit.
- Cleanup Chunk 1B revalidated every pointer/process/build/temp/production identity with zero pre-signal failures, sent only SIGTERM to exact PID 121748, observed the process exit and port 8876 become free, and reverified production before removing anything. It then removed only the empty mode-700 isolated temp directory and mode-600 isolated pointer. Candidate binary `f12882ab...` and build pointer `081b9232...` remain preserved as evidence. Final production PID 2405, all three installed hashes, localhost-only port 8765, read-only 1,198/2,145 health, and empty production temp remained exact; no mail/index operation ran. Replacement bundle transfer is now authorized.
- The first Outlook Readable Windows transfer attempt passed exact local size/SHA256, all 32 ordered members, zero duplicate/unsafe/font members, SSH reachability, and all three remote production hashes. It printed valid read-only production health at 1,198/2,145, then the pre-transfer harness exited before SCP because its shell wildcard required JSON keys in the wrong order (`ok`, then `read_only`, then `mail_mutation`); actual deterministic JSON places `mail_mutation` earlier. No incoming or final archive was written and no remote application state changed. The replacement must parse health structurally with Python before retrying the guarded transfer.
- The corrected Outlook Readable transfer parsed health structurally and passed. Windows reverified the exact 77,445-byte SHA256 `533235a5...`, 32 members, and zero duplicate/unsafe/font members. Remote preflight proved incoming/final paths absent, all three production hashes exact, read-only 1,198/2,145 health valid, and port 8876 free. SCP completed; antiX independently matched incoming size/hash, 32 members, and zero duplicates before atomically finalizing `/home/atiq/notmuch-browser-outlook-readable-src-20260718-215557.tar.gz` mode 600 with the same hash. Production hashes/health remained exact, port 8876 stayed free, and no extraction, build, service, mail, or index operation ran.
- antiX independently audited and extracted the Outlook Readable bundle into `/home/atiq/codex-runs/notmuch-browser-outlook-readable-build-20260718-222714`: all 32 ordered members were regular, safe, normalized uid/gid/modes, byte-identical, link-free, and font-free. Module pins, source contracts, exact asset hashes and 79,904-byte budget, `go mod verify`, tests, vet, race test, both shell checks, and stripped build passed under Go 1.26.4/notmuch 0.39. The mode-700 Linux binary is 9,769,225 bytes with SHA256 `e80fb11dc1e4dffe51e65f754ecd03ca94903a16f6294394f685c853bb0f5cd4`; audit report SHA256 is `fd78e9a...`; mode-600 build pointer SHA256 is `e088115d...`. All four local fonts resolve. Production hashes/read-only 1,198/2,145 health stayed exact, port 8876 stayed free, and no install/restart/mail/index operation ran.
- Standalone fail-closed operator harness `scripts/notmuch_browser_outlook_readable_isolated_validate.sh`, SHA256 `d0b3ce6c...`, now binds the next isolated gate to build pointer `e088115d...` and Linux binary `e80fb11d...`. It verifies production and candidate identities, exact assets, routes/security, full-page image reset, Readable/Original state propagation, one genuine ZIP attachment, ten concurrent CID HTTP 200 responses with zero 429s, and empty temporary state before retaining the exact loopback-only candidate through a private pointer. Failure cleanup can signal only the PID whose executable and process-start ticks match; an existing pointer is never removed. The script has LF endings, passes Bash parsing, and all four embedded Python programs compile, but it has not yet run on antiX.
- The harness is committed and pushed in `28e1355`, but unauthenticated GitHub raw, GitHub raw-proxy, and API requests all return 404 because the repository is not publicly readable. The reviewed transfer path is therefore an exact one-file Windows SCP from the local checkout, followed by independent antiX SHA256/mode/syntax verification; no clone or repository setup on antiX is required.
- The one-file Windows SCP gate passed. Local and antiX copies are both 31,267 bytes with SHA256 `d0b3ce6c...`; antiX finalized `/home/atiq/codex-runs/notmuch-browser-outlook-readable-isolated-validate-28e1355.sh` mode 700 after proving LF-only content, Bash syntax, and all four embedded Python programs. Production artifacts and read-only health remained exact at 1,198/2,145, port 8876 stayed free, and no candidate, install, restart, mail, or index operation ran. A mistyped SSH password was retried successfully before final verification and had no state impact.
- The first Outlook Readable isolated attempt bound exact PID 54952/start ticks 4198056 and passed build/archive/production identities, four font resolutions, candidate read-only 1,198/2,145 health, loopback listener, exact static assets, root/search/HTMX/status routes, three invalid-capability 403s, one 404, and all eight POST 405/Allow-GET checks. It then stopped before display, attachment, CID, report, or pointer retention because the operator harness searched the outer HTML-attribute representation of `srcdoc` for literal `font-src 'none'`; at that layer the inner CSP apostrophes are correctly entity-escaped. This is a harness parsing defect, not a candidate CSP failure. The wrapper returned 1 and identity-limited cleanup was attempted, but process/port/temp/pointer/production cleanup still requires a separate read-only audit before replacing the harness.
- The follow-up read-only audit proved PID 54952 gone, zero processes using binary `e80fb11d...`, port 8876 free, the private pointer absent, and production exact/read-only at 1,198/2,145 with one loopback listener and zero temporary files. The preserved failed run contains only private logs/state. Cleanup left only the exact mode-700 isolated temp directory, which is empty at zero bytes; this single residue caused `audit_failures=1` and may be removed only after rechecking those identities. Replacement harness SHA256 `cfd2ce4f...` is 31,961 bytes and parses the inner `srcdoc` with a second standard-library `HTMLParser`, reads the decoded CSP meta value structurally, and checks forbidden elements/Office style IDs from parsed tags rather than unescaping or substring-scanning sender content. Bash syntax, all four embedded Python programs, and an extracted double-escaped `srcdoc` regression pass locally.
- The guarded empty-residue cleanup then rechecked the gone PID, zero candidate processes, free port 8876, absent pointer, exact realpath/mode/owner, zero entries/bytes, preserved failed-run evidence, and production identities/health/temp before invoking only `rmdir` on that exact directory. Removal passed; evidence remained; production stayed at read-only 1,198/2,145 with the same loopback listener and zero temp. No signal, service restart, mail, or index operation ran. The failed isolated state is now fully clean, so only newly named replacement-harness transfer is authorized.
- The corrected one-file transfer passed. Windows and antiX matched 31,961 bytes and SHA256 `cfd2ce4f...`; antiX independently passed LF/Bash/four-Python-block checks and extracted the actual structured parser definitions to reproduce the doubly escaped `srcdoc` regression. New mode-700 path is `/home/atiq/codex-runs/notmuch-browser-outlook-readable-isolated-validate-9cd83a5.sh`. Old failed harness `d0b3ce6c...` and failed-run evidence remain exact, old isolated temp is absent, port 8876 is free, production remains read-only at 1,198/2,145 with zero temp, and no candidate or production action ran. The second isolated automated attempt is authorized using only the corrected script.
- Isolated attempt 2 started exact PID 68939/start ticks 4321854 and repeated all identity/font/health/listener/asset/route/security/rejection passes at 1,198/2,145. Structured iframe CSP parsing passed the former failure point, then the gate stopped because the real duplicate-1 HTML did not contain the `notmuch-browser-office-readable` style ID. The current implementation injects that ID only when bounded detection finds explicit Word/Outlook markers, so this is no longer safely classifiable as a harness escaping bug: either the real body intentionally falls outside the approved Office-only normalization or the detector lacks a privacy-safe legacy Outlook indicator. No attachment/CID/report/pointer gate ran. Identity-limited cleanup was attempted but requires another independent audit; a content-private marker/count diagnostic is required before any source or harness change.
- The attempt-2 audit proved PID 68939 gone, zero candidate processes, free 8876, absent pointer, exact production/read-only 1,198/2,145 health, and zero production temp. Only its exact mode-700 isolated directory remains, empty at zero bytes. Privacy-limited duplicate-1 diagnostics printed no content/path/value and proved a 7,650-byte HTML body with ten unique CID images, zero Office/MSO/Word/VML/generator markers, zero Office classes/styles/namespaces/comments, no legacy `<font>` tags, and existing Aptos/sans-serif declarations. Eighteen `X-MS-Exchange-*` headers identify transport, not authoring client. The Go Office-only detector is therefore correct and remains unchanged. Final policy-aware harness is 33,092 bytes, SHA256 `c660f120...`; it requires Office override-vs-Original divergence when present, otherwise exact sanitized Readable/Original parity. Bash, four Python blocks, structured `srcdoc`, both valid policy branches, and both rejection branches pass extracted-code tests.
- The guarded attempt-2 residue cleanup then rechecked the gone PID, zero candidate processes, free 8876, absent pointer, exact candidate/production hashes, exact temporary-directory realpath/type/mode/owner/emptiness, and preserved failed-run evidence. It removed only that empty directory with `rmdir`. Production PID 2405 remained the sole loopback listener on 8765 with read-only 1,198/2,145 health and zero temporary files before and after; no process signal, restart, mail, or index operation ran. Transfer of only the final policy-aware validator `c660f120...` under a new filename is now authorized.
- The final policy-aware validator one-file transfer passed. Windows, remote incoming, and final antiX copies all matched 33,092 bytes and SHA256 `c660f120...`; LF endings, Bash syntax, and all four embedded Python blocks compiled. Final mode-700 path is `/home/atiq/codex-runs/notmuch-browser-outlook-readable-isolated-validate-e7a2c13.sh`. Candidate process count stayed zero, 8876 remained free, and exact production artifacts/read-only 1,198/2,145 health/zero temp remained unchanged. Before execution, antiX must independently extract and exercise the actual structured parser and both display-policy branches from this final file.
- The independent antiX policy regression extracted the actual parser and `validate_display_policy` definitions from final file `c660f120...`, proved structured inner `srcdoc`/CSP parsing, accepted Office override divergence and exact non-Office sanitized parity, and rejected both inverse-invalid cases. All four embedded Python blocks and Bash syntax passed. Port 8876 remained free, pointer/temp remained absent, and exact production/read-only 1,198/2,145 health/zero temp was unchanged. Exact isolated validator execution is now authorized through a non-closing wrapper.
- Final isolated automated validation passed and retained exact PID 87785/start ticks 4481004 on only `127.0.0.1:8876`. Candidate and production matched read-only 1,198/2,145 health. Exact assets/routes/security/rejections passed; the real body was correctly classified `non-office-sanitized-parity`; display/duplicate state passed; ten signed CID images returned ten 200 responses, zero 429, maximum 99.2 ms; exactly one genuine ZIP/Save All remained; and isolated/production temp returned zero. Mode-600 pointer SHA256 is `84e47fda...`, automated report SHA256 is `c7f0a4da...`, and production service/mail/index state was untouched. Manual same-viewport Readable/Original visual comparison is now required while retaining PID 87785.
- The owner completed the antiX Firefox 100% manual comparison and explicitly reported PASS. This accepts the real non-Office Readable/Original parity, embedded-image rendering, sole genuine ZIP attachment, controls/layout, and current visual result. PID 87785 remains intentionally retained only for identity-audited cleanup; production has not been backed up or changed yet.
- The post-manual read-only audit then revalidated pointer `84e47fda...`, PID 87785/start ticks 4481004, exact command/listener/build/archive/report identities, report `c7f0a4da...`, ten HTTP-200 CIDs, one genuine attachment, empty isolated temp, and candidate health. Production PID 2405/exact hashes/loopback listener/read-only 1,198/2,145 health/zero temp also remained exact. No signal or removal occurred. Identity-matched SIGTERM and exact empty-temp/pointer cleanup are now authorized before backup.
- On 2026-07-19 the owner re-evaluated the retained candidate with screenshots `gui5.png` and `gui3.png` before running cleanup. Functional acceptance remains useful evidence, but typography acceptance and installation authorization are superseded: `gui5` keeps oversized signature text and wraps/expands the legal block, whereas embedded-image dimensions are already close to the compact reference. Root cause is the exact non-Office classification: current Readable equals Original and the fallback defaults can be overridden by sender styles. The previously issued stop-and-clean chunk was not run. PID 87785 remains retained for one privacy-limited style/structure diagnostic; production remains unchanged.
- A separate five-line target screenshot was compared locally against available font files. Inter Variable Regular at approximately 15px was the strongest raster match (Dice 0.961 first line, 0.968 `BR`, 0.932 `Agnes`), well ahead of the main alternatives on those glyphs. Because antialiasing and clipping prevent absolute identification from pixels alone, the retained antiX diagnostic must still inspect declared source/sanitized typography without exposing message content before the Readable stack is finalized.
- The first privacy-limited typography diagnostic attempt stopped at its initial listener gate on 2026-07-19 06:43:15: pointer and candidate-binary hashes matched, but 8876 no longer had exactly one listener. No source/sanitized extraction, diagnostic report, signal, removal, service action, mail operation, or index operation occurred. The retained candidate may have exited overnight or the boot/session may have changed. A read-only PID/process/port/pointer/temp/evidence/production audit is required before restart or cleanup.
- The replacement self-cleaning typography diagnostic passed on 2026-07-19 at 06:55 +06:00. It started exact candidate `e80fb11d...` as PID 44156/start ticks 324089 only on `127.0.0.1:8876`, matched production's read-only 1,199-message/2,146-file health, emitted no addresses/body/IDs/filenames/HTML/capabilities/paths, then identity-stopped the candidate, freed 8876, and removed its temporary directory. Production artifacts/service remained unchanged with zero temp; no mail or index operation ran. Preserved report SHA256 is `eb82bc35c516435833d807075c1cdb47e1a7eaa65e82d2d1173d98c5d5ad9667`.
- The diagnostic proves the exact source and sanitized structure are stable: four flat tables, 21 cells, seven paragraphs, 24 spans, and ten CID images whose dimensions remain exactly preserved after rewrite. The source fixes all seven paragraphs at `16px`; 18 spans request an unavailable sender family, while smaller signature spans explicitly use `7pt` (9), `8pt` (4), `9pt` (3), and `10pt` (2). Current Readable adds only the page default and preserves all 56 inline styles, explaining the oversized serif-like result. The approved replacement applies universal Readable Inter `15px`/`1.4` prose, compact `9pt`/`1.25` table typography, preserves explicit smaller spans and image dimensions, caps table width, and leaves Original unchanged. Office-only bundle `533235a5...` and candidate `e80fb11d...` are superseded for installation.
- The first local computed-style check caught a second root cause before packaging: the iframe contained the Readable style marker but exposed zero active stylesheets, and its paragraph still computed as Times New Roman 16px. Because `srcdoc` inherits the embedding page CSP, parent `style-src 'self'` blocked both viewer and sanitized sender inline CSS. The approved correction adds `'unsafe-inline'` only to `style-src`; scripts remain non-inline, the iframe remains sandboxed without script/same-origin permission, its own CSP remains `default-src 'none'`, and untrusted mail HTML remains escaped inside `srcdoc`. A restarted browser fixture must prove active 15px/1.4 Inter Readable styling and unchanged Original before packaging.
- The corrected local browser gate passed. Readable exposed two active iframe stylesheets and computed exactly to the local Inter stack at 15px with 21px line height; Original had no Readable marker and retained the existing Aptos 10pt fallback at 13.3333px/18px. Both modes had equal scroll/client width and zero browser warnings/errors. Module verification, Go tests/vet, 77.1% browser-package coverage, both shell syntax checks, all 46 Python tests with two expected Windows skips, and the 79,904-byte frontend budget passed. Windows `-race` is unavailable because CGO is disabled and remains an antiX gate.
- Deterministic no-font bundle `notmuch-browser-readable-typography-src-20260719-071305.tar.gz` contains 32 unique safe regular members, is 76,778 bytes, and has SHA256 `50e368dcabcf453c471c2b2d774200f9d80fb2fd366952efe12ea99fd83574f1`; a second archive build was byte-identical. Clean extraction matched every source byte and passed modules/tests/vet/shell/build. With `-buildvcs=false`, root and clean builds were identical at 10,033,664 bytes and SHA256 `3ab0970637e647dee7e51651cfe5b7162e3c742b2055bab07c945b4b6a80c18b`; the earlier root/clean mismatch was only dirty Git metadata. No antiX or production state was contacted. The next step is a guarded Windows-to-antiX transfer, then independent Linux build/race and isolated real-message validation.
- The universal-Readable transfer preflight then passed from Windows PowerShell 7.6.3. Local bundle size/hash/count matched exactly at 76,778 bytes, `50e368dc...`, and 32 unique members with zero unsafe/font paths; SSH port 22 was reachable. antiX proved the final path absent, zero incoming residue, port 8876 free, read-only production health at 1,199 messages/2,146 files, zero download-temp files, and exact installed hashes `50e6b1ad...` browser, `1b1277b0...` control, and `df07cd07...` index control. No transfer, extraction, build, service, mail, or index action ran. Guarded transfer of only the exact archive is next.
- Windows then reverified and transferred only the exact universal-Readable bundle to `/home/atiq/notmuch-browser-readable-typography-src-20260719-071305.tar.gz.incoming-50e368dc`. Remote size and SHA256 matched 76,778 bytes and `50e368dc...`; SCP staging mode is 664 with owner/group `atiq`. All three production hashes remained exact, health remained read-only at 1,199/2,146, port 8876 remained free, and download temp remained zero. The archive is deliberately neither finalized nor extracted. antiX must independently validate all tar members before changing only this incoming file to mode 600 and atomically renaming it to the final path.
- The first antiX archive-finalization attempt stopped safely at its last pre-mutation inventory-hash assertion. Incoming identity and all three production hashes passed; production health remained read-only 1,199/2,146 with zero temp. Before the failing line, Python had already proved 32 unique regular files, safe paths, zero fonts, uid/gid zero, and exact 0644/0755 member modes. No chmod, rename, extraction, build, service, mail, or index operation ran. Root cause is validator-only: Windows `Sort-Object` used culture-aware ordering to derive `9b2aa4f...`, while the antiX Python validator uses code-point ordering. Local Python reproduces the validator's 1,256-byte inventory and correct SHA256 `b505b3e1b5778919d32f127c80c08536b70a283a31150e9b231457d77e508f9c`. The corrected chunk must recheck all state and use that hash before finalization.
- Corrected antiX archive finalization passed. Python code-point inventory SHA256 matched `b505b3e1...`; all 32 members were unique regular files with safe paths, zero links/fonts, uid/gid zero, and exact 0644/0755 modes. Only the validated incoming archive was changed to mode 600 and atomically renamed to `/home/atiq/notmuch-browser-readable-typography-src-20260719-071305.tar.gz`; final size/hash are 76,778 bytes and `50e368dc...`. Production hashes and read-only 1,199/2,146 health matched before and after, temp remained zero, and 8876 remained free. No extraction, build, service, mail, or index operation ran. Independent extraction/test/vet/race/shell/build is next.
- Independent antiX extraction and build validation passed for bundle `50e368dc...`. A new mode-700 run safely extracted the exact 32 regular members and matched every extracted byte to the archive; `go mod verify`, `go test -count=1 ./...`, `go vet ./...`, `go test -race -count=1 ./...`, both shell syntax checks, source contracts, and the 79,904-byte frontend budget all passed under Go 1.26.4 and notmuch 0.39. Inter Variable, Aptos, AporeticSansMonoNerdFont, and Iosevka SS14 resolved locally. The temporary Linux binary is `/home/atiq/codex-runs/notmuch-browser-readable-typography-build-20260719-071305/notmuch-browser`, 9,761,033 bytes, SHA256 `e68c33d1e369f8023e5b7fc619055b0ef7fa537a5013226176aac6a9f2435647`; its private build report SHA256 is `c28f953f6b45051ee35d77fd34118078679f1f697e24f5ea5348749b89e99b5f`. Zero candidate processes remained, 8876 stayed free, and production artifacts/read-only 1,199/2,146 health/zero temp remained exact. No install, restart, mail operation, or index operation occurred. Retained isolated startup and real-message validation are next.
- Standalone retained-start harness `scripts/notmuch_browser_readable_typography_isolated_start.sh` was added so antiX can receive only one 12,796-byte LF script rather than paste a fragile multi-hundred-line batch. SHA256 is `6aa02ac861d7202eb2764ae835983bd8bc2a6fbff2d8d0facae3b035bef0d304`; Bash syntax and all four embedded Python programs pass locally. The harness pins the archive/build/production identities, refuses stale pointers/processes/port use, uses identity-bound failure cleanup, starts only on `127.0.0.1:8876`, validates base routes/assets/CSP/rejections/temp/production, writes a private retained pointer, and deliberately leaves the exact candidate running for the next gate. It does not install or restart production or run mail/index commands. Because the private repository's unauthenticated raw URL returns 404, transfer uses SCP from the Windows checkout rather than antiX `curl`.
- The first retained-start harness transfer attempt stopped safely before SCP. Windows matched the local 12,796-byte LF script and SHA256 `6aa02ac...`, SSH port 22 was reachable, and antiX matched the exact candidate binary, build report, and installed production browser. The wrapper then rejected production health because its shell `case` expression incorrectly required JSON keys in `ok`, `read_only`, `mail_mutation` order; Go's encoded map placed `mail_mutation` earlier. PowerShell threw `Remote preflight failed`, so no script transfer, finalization, execution, candidate process, service change, mail operation, or index operation occurred. The corrected transfer preflight parses JSON structurally with Python and must first reconfirm absent incoming/final paths, absent pointer, and free 8876.
- Corrected local transfer helper `scripts/notmuch_browser_readable_typography_harness_transfer.ps1` is 6,324 bytes with SHA256 `09103ad2a05f3310d223704cb25c4f98a0a72cf5f1c6c62ec1b24cd69270cb02`. PowerShell parsing and both embedded remote Bash blocks pass locally. It replaces key-order matching with Python JSON decoding, rechecks exact candidate/build/production identities, zero exact candidate processes, absent incoming/final/pointer paths, clean production temp, and free 8876 before SCP; after transfer it verifies exact bytes/hash/LF, Bash syntax, all four embedded Python blocks, production health, and still leaves the harness unfinalized and unexecuted.
- The follow-up audit proved antiX had rebooted: current boot ID differs from the retained pointer, PID 87785 is absent, zero exact candidate processes run, and 8876 is free. Pointer `84e47fda...` is an exact stale record; its mode-700 temp is empty/non-mount; report `c7f0a4da...` and all run evidence remain intact; and the failed diagnostic created no directory/report. Production restarted normally as PID 2393 with exact artifacts, loopback-only 8765, read-only 1,199/2,146 health, and zero temp. No signal or removal occurred. Exact stale pointer/empty-temp cleanup is now authorized before a self-cleaning diagnostic-specific restart.
- Guarded stale cleanup then reconfirmed every identity and removed only the empty old temp with `rmdir` plus stale pointer with `unlink`. No process signal was needed. Candidate binary, report/log evidence, build pointer, and source bundle remain exact. Production PID 2393/exact artifacts/loopback-only 8765/read-only 1,199/2,146 health/zero temp passed before and after. A new diagnostic-specific candidate may now run on 8876 with automatic identity-bound stop/temp cleanup and no persistent pointer.

## What Is Intentionally Undecided

- Package manager for any future dependency family outside the approved browser workflow
- Database or storage layer beyond the existing notmuch sidecar index
- Any future mail-mutating UI actions; reply, forward, compose, delete, and tag editing remain explicitly out of scope
- Branching model beyond keeping changes commit-ready

## Active Working Context

- The repository is in setup plus first application implementation mode.
- Do not install unapproved Python, Node, npm, pip, Docker, devcontainer, or other dependencies. The approved notmuch browser build exception is Bun 1.3.14 with the exact lockfile dependencies.
- The transport and converter utilities remain Python 3 standard-library-only unless the owner approves another change. The notmuch browser uses templ v0.3.1020, chi v5.3.1, x/net v0.57.0, vendored HTMX 2.0.10, and Tailwind CSS/CLI 4.3.3 through Bun; the module floor is Go 1.25. templ and Tailwind are build-time only. Production embeds the generated HTML/CSS/JavaScript/Lucide/HTMX assets in one Go binary. User-level antiX setup helpers remain POSIX shell.
- Do not delete files without explicit approval.
- End each working session by committing changes with a clear self-explanatory message and pushing to the configured remote.
- Do not perform non-routine remote operations without explicit approval.
- Keep all documentation and future scripts portable across Windows 11 and Debian Linux.

## Completed Setup

- Git repository exists.
- End-of-session commit and push workflow approved.
- Initial documentation structure exists.
- Project memory files created or updated:
  - `AGENTS.md`
  - `README.md`
  - `docs/PROJECT_STATE.md`
  - `docs/DECISIONS.md`
  - `docs/TODO.md`
  - `docs/WORK_VALIDATION_LEDGER.md`
  - `docs/IDEAS.md`
  - `docs/WORKFLOW.md`
  - `docs/CHANGELOG.md`
  - `docs/BETTERBIRD_PROFILE_TRANSPORT_GUIDE.html`
  - `docs/BETTERBIRD_PROFILE_TRANSPORT_GUIDE.txt`
  - `docs/MAILDIRPP_ARCHIVE_TRANSPORT_GUIDE.html`
  - `docs/MAILDIRPP_ARCHIVE_TRANSPORT_GUIDE.txt`
  - `docs/BETTERBIRD_DELTA_MAILDIRPP_TRANSFER_GUIDE.html`
  - `docs/BETTERBIRD_DELTA_MAILDIRPP_TRANSFER_GUIDE.txt`
  - `docs/EVOLUTION_FLATPAK_ANTIX_GUIDE.html`
  - `docs/EVOLUTION_FLATPAK_ANTIX_GUIDE.txt`
  - `docs/MBSYNC_ANTIX_GUIDE.html`
  - `docs/MBSYNC_ANTIX_GUIDE.txt`
  - `docs/NOTMUCH_ASTROID_ANTIX_GUIDE.html`
  - `docs/NOTMUCH_ASTROID_ANTIX_GUIDE.txt`
  - `docs/NOTMUCH_BROWSER_ANTIX_GUIDE.html`
  - `docs/NOTMUCH_BROWSER_ANTIX_GUIDE.txt`
  - `docs/NOTMUCH_GO_BROWSER_SERVICE_GUIDE.html`
  - `docs/NOTMUCH_GO_BROWSER_SERVICE_GUIDE.txt`
  - `.gitignore`
  - `.gitattributes`
  - `go.mod`
  - `go.sum`
  - `cmd/notmuch-browser/main.go`
  - `internal/notmuchbrowser/config.go`
  - `internal/notmuchbrowser/handlers.go`
  - `internal/notmuchbrowser/notmuch.go`
  - `internal/notmuchbrowser/server.go`
  - `internal/notmuchbrowser/templates.go`
  - `internal/notmuchbrowser/notmuch_test.go`
  - `internal/notmuchbrowser/static/app.css`
  - `internal/notmuchbrowser/static/htmx.min.js`
  - `internal/notmuchbrowser/static/htmx.LICENSE.txt`
  - `internal/notmuchbrowser/styles/app.tailwind.css`
  - `internal/notmuchbrowser/templates/layout.html`
  - `internal/notmuchbrowser/templates/search.html`
  - `internal/notmuchbrowser/templates/message.html`
  - `internal/notmuchbrowser/templates/status.html`
  - `internal/notmuchbrowser/templates/partials/results.html`
  - `src/betterbird_profile_transport.py`
  - `src/maildirpp_transport.py`
  - `src/betterbird_maildirlite_to_maildirpp.py`
  - `src/betterbird_post_archive_delta.py`
  - `scripts/evolution_flatpak_icewm_launcher_setup.sh`
  - `scripts/mbsync_provider_inbox_setup.sh`
  - `scripts/notmuch_browser_control.sh`
  - `scripts/notmuch_browser_index_control.sh`
  - `tests/test_betterbird_profile_transport.py`
  - `tests/test_maildirpp_transport.py`
  - `tests/test_betterbird_maildirlite_to_maildirpp.py`
  - `tests/test_betterbird_post_archive_delta.py`

## Current Implementation Notes

- Horizontal bundle `44398af...`, annotated-GUI bundle `a371f5c...`, and GUI2 bundle `60545325...` plus their temporary binaries were all superseded before production installation. GUI2 remains useful validation history because all 24 manual checks passed, but its route harness exited silently and the owner approved body-resource/font changes before installation. None of these historical artifacts may be installed.
- The failed body-image artifact `notmuch-browser-body-images-src-20260718-183128.tar.gz`, temporary binary SHA256 `47d76329...`, queue-fix bundle `d9943dfe...`, and queue-fix Linux binary `f12882ab...` are superseded and must not be installed. The active replacement is deterministic 32-member no-font bundle `notmuch-browser-outlook-readable-src-20260718-215557.tar.gz`: 77,445 bytes, SHA256 `533235a5d16d43a504c7bd8e598127606076144b87c970e68f4fa7d0110a0e3c`. A byte-identical second build proved archive reproducibility; inventory checks found zero duplicate, absolute, traversal, link, or font members. Clean extraction passed module verification, Go tests/vet, both shell syntax checks, and a 10,037,760-byte stripped Windows build at SHA256 `791cdf28...`; the full Python suite passed 46 tests with two expected Windows skips. Browser-package coverage is 77.8%. Deterministic Tailwind regeneration matched exactly; CSS/app JS/HTMX are 23,190/5,476/51,238 bytes, total 79,904. Retained queue-fix PID 121748 must first be identity-checked and stopped through its private pointer. Fresh transfer, antiX verification/Linux build, isolated ten-CID and Readable/Original validation, production backup/install, antiX/Windows acceptance, read-only proof, and reboot proof remain required one reviewed chunk at a time.
- GUI2 local browser QA passed at 1440x900, 1024x768, and 390x844. Desktop and mobile placement, live HTMX OOB summary replacement, exact pager state, inline copy proximity, four exact clipboard values, weekday date rendering, Cc rendering, metadata baselines, horizontal workspace behavior, and zero horizontal overflow were verified. The preview listener was stopped afterward.
- Horizontal-layout Chunk 1 passed on antiX at 2026-07-15 07:39 +0600. The installed attachment/image release retained its exact three artifact hashes; browser PID 2396, index PID 2301, and mbsync PID 2316 were healthy; only `127.0.0.1:8765` listened; health remained read-only with 828 messages and 1,508 files; safety config and four `new.ignore` entries were exact; locks and all Maildir/download temporary files were absent; both IceWM marker pairs occurred once; the verified rollback pointer remained usable; and `/mail` had 103 GB free. No source transfer, build, service stop, index refresh, or production change occurred.
- Horizontal-layout Chunk 2A passed from Windows PowerShell 7.6.3. The local source bundle matched SHA256 `44398af72673cf2019822b74bd81e37d9f513d8fa4b713f671e3224e23fc930f`, all 30 ordered members matched, antiX SSH port 22 was reachable at `192.168.254.128`, and SCP exited 0 to `/home/atiq/notmuch-browser-horizontal-src-20260715-073049.tar.gz`. AntiX must still independently verify the transferred bytes and build in an isolated directory before any service change.
- Horizontal-layout Chunk 2B passed on antiX from run `/home/atiq/codex-runs/notmuch-horizontal-chunk2b-20260715-074839`. The transferred 65,788-byte bundle independently matched its exact hash and safe 30-member inventory; Go 1.26.4 module verification, tests, vet, and a stripped temporary Linux build passed. The temporary binary is 9,724,169 bytes with SHA256 `5d8ace286cd0027738e6310eb85057d42ee46eea575db0fdf67e5fdd5a134fdb`; control hashes stayed exact. Asset sizes matched 20,712-byte CSS, 5,380-byte app JavaScript, 51,238-byte HTMX, and 77,330 bytes total. Production PID 2396 and index PID 2301, exact artifact hashes, 828/1,508 health counts, localhost binding, locks, and zero temporary files were unchanged; port 8876 remained free. Nothing was installed or started from the new build.
- Horizontal-layout Chunk 3 passed on antiX from run `/home/atiq/codex-runs/notmuch-horizontal-chunk3-20260715-075307`. Temporary PID 68334 served only `127.0.0.1:8876` with an isolated download directory; health/security/static/HTMX/message/405/403 checks passed. The owner entered `PASS` after all ten real UI checks, including results above the reader, initial 35/65 sizing, message features, pointer drag, reload persistence, Up/Down/Home/End controls, and the unchanged sub-860px layout. The process then stopped, port 8876 became free, and isolated/Maildir temp stayed zero. Production artifact hashes and PIDs remained unchanged. Counts rose together from 828/1,508 to 830/1,510 because the intentionally running mbsync/index loops received two messages during the manual gate; this is live background activity, not browser mutation.
- The July 14 guide deferral ended after the combined replacement passed staged antiX build/port-8876 checks, atomic production rollout, antiX and Windows UI confirmation, strict read-only proof, resource/restart gates, and the repaired reboot proof. The final July 25 guides supersede that release.

- The transport utility is designed to run `pack` on Fedora against `~/Betterbird-Email`, write split `betterbird-profile.tar.gz.partNNNN` files with `manifest.json` and `inventory.jsonl`, then run `verify-archive`, `unpack`, and `verify-tree` on antiX.
- The transport utility defaults to whole-profile scope, Python stdlib gzip compression level 6, and 1900 MiB part files.
- The transport utility refuses active-looking Betterbird lock markers, output directories inside the source tree, non-empty restore destinations, unsafe archive paths, unsupported special files, and symlinks unless explicitly overridden.
- The DIY guide in `docs/BETTERBIRD_PROFILE_TRANSPORT_GUIDE.html` and `.txt` is the operator-facing runbook for the safe pack, transfer, verify, unpack, verify-tree, and conversion sequence.
- In all transport commands, `--manifest` points to `manifest.json`.
- `inventory.jsonl` stays beside `manifest.json` and is read automatically by the transport script.
- The converter is designed to run on antiX after the Fedora Betterbird profile is restored to `/mail/import-staging/betterbird-maildir`.
- Default mode is `--dry-run`; `--copy` refuses unmarked or unsafe sources and writes through Maildir `tmp` before atomic rename.
- Duplicates are kept, but duplicate `Message-ID` and content hashes are logged.
- The full Betterbird maildir-lite archive was converted on Fedora into canonical Maildir++ at `/home/atiq/Evolution-Mailstore/betterbird-archive-maildirpp`. Converter copy and full hash verification succeeded with 48,720 copied `cur` messages, 0 `new` messages, 0 target `tmp` files, and 0 verify errors.
- The owner reported Fedora Evolution GUI validation of the converted archive: `mail_tagindustries_com_sg.Inbox` opens with 14,279 emails, messages open, HTML renders correctly, and no error popup is shown.
- `src/maildirpp_transport.py` is the next transport path for the already-converted archive. It defaults to source `/home/atiq/Evolution-Mailstore/betterbird-archive-maildirpp`, destination `/mail/Mailstore/evolution/local-maildir`, split gzip tar parts named `maildirpp-archive.tar.gz.partNNNN`, and Maildir++ layout checks before pack and after restore.
- Fedora manual transport preflight on 2026-07-01 validated the copied scripts in `~/codex-runs` by SHA256, compile, help, and import-location checks. It also stopped Evolution mail services and inspected `/home/atiq/Evolution-Mailstore/betterbird-archive-maildirpp` successfully: 29 Maildir folders, 28 dot folders, 48,720 `cur`, 0 `new`, 0 `tmp`, no symlinks, no special files, and `status=ok`.
- Fedora pack on 2026-07-01 succeeded using the manually copied transport script. It wrote `/home/atiq/maildirpp-archive-export-20260701-215204` with 21 `maildirpp-archive.tar.gz.partNNNN` files, `manifest.json`, and `inventory.jsonl`; export size was 38G, archive size was 40,613,836,495 bytes, archive SHA256 was `23292983c9ffb3590197a534aeddd03590a2b8d9f913732573fa1637b71045fe`, and `pack_exit=0`.
- Fedora `verify-archive` on 2026-07-01 succeeded for `/home/atiq/maildirpp-archive-export-20260701-215204/manifest.json`: 21 parts, 40,613,836,495 archive bytes, `status=ok`, and `verify_archive_exit=0`.
- The Maildir++ archive guide now documents the transfer path for that verified export: run a Fedora preflight proving the USB and `/mnt/hgfs/Win11Host_Shared4VM` have enough real free space and actual write permission, copy the export and two transport scripts to USB, verify the archive on the USB, optionally copy and verify the same export in the Win11 host share, copy the USB package to `/mail/import-staging/maildirpp-archive-export-20260701-215204` on antiX, then run antiX-side `verify-archive` before any unpack. A 50 GB free USB drive is acceptable for the verified 38G split export if the byte-level preflight and write probes pass; it is not acceptable for the original uncompressed 59G Maildir++ tree.
- Fedora diagnostics on 2026-07-03 showed `/mnt/hgfs/Win11Host_Shared4VM` exists but was not mounted as a VMware shared folder: `findmnt -T` resolved it to Fedora `/` on `btrfs`, user `atiq` could not write, and `sudo` could write only into the Fedora filesystem. The guide now requires a host-share mount identity check before the Win11 host-share copy path is used.
- A later Fedora HGFS mount attempt on 2026-07-03 found `vmhgfs-fuse` and `vmware-hgfsclient` installed, but `vmware-hgfsclient` exited 1 with no visible shares and `sudo vmhgfs-fuse .host:/ /mnt/hgfs ...` failed with `Error -107 cannot open connection`; the host-share path still resolved to Fedora `/` on `btrfs` and user write remained denied. Use the USB transfer path unless VMware shared folders are fixed in the Fedora VM configuration and the mount identity/write probes pass.
- A subsequent Fedora check on 2026-07-03 showed the VMware share `Win11Host_Shared4VM` became visible and `/mnt/hgfs/Win11Host_Shared4VM` resolved to `fuse.vmhgfs-fuse`, but user `atiq` still could not create the write-test directory there. The Win11 host-share copy remains blocked until the user write probe succeeds; do not copy the 38G export there yet.
- A Fedora remount attempt with uid/gid ownership options on 2026-07-03 still left `/mnt/hgfs/Win11Host_Shared4VM` unusable: `mount_exit=0`, but the mounted options still showed `user_id=0,group_id=0`, `ls` returned `Permission denied`, and the user write probe failed. Continue to treat the Win11 host-share path as unsafe for the 38G copy unless a later probe shows normal-user read/write success.
- A later direct named-share remount on 2026-07-03 made the Win11 host-share path usable for the next copy attempt: `sudo /usr/bin/vmhgfs-fuse .host:/Win11Host_Shared4VM /mnt/hgfs/Win11Host_Shared4VM -o subtype=vmhgfs-fuse,allow_other` succeeded, `findmnt` reported `fuse.vmhgfs-fuse`, `ls -ld` showed `atiq atiq` ownership with `drwxrwxrwx`, `host_share_mount_ok=yes`, and normal-user `mkdir` and file-write probes both exited 0. The host-share copy path may be used only if the same mount identity and normal-user write probes still pass immediately before copying; the USB path remains independently safe.
- Fedora-to-Win11 host-share copy on 2026-07-03 succeeded for `/mnt/hgfs/Win11Host_Shared4VM/maildirpp-archive-export-20260701-215204`: scripts were copied with expected SHA256 values, `rsync` transferred 40,626,264,258 bytes, `copy_exit=0`, `host_manifest_exit=0`, `host_inventory_exit=0`, `host_part_count=21`, export size was 38G, and host-share `verify-archive` returned `status=ok` with 21 parts and 40,613,836,495 archive bytes. This is now a verified Win11 host-side backup or alternate transfer package; it has not yet been copied into antiX staging.
- antiX USB-to-staging transfer finished successfully across 2026-07-03 and 2026-07-04. The USB was mounted at `/media/atiq/New Volume`, `/mail` had 200G available, the transport scripts copied into `~/codex-runs` with expected SHA256 values, USB-side `verify-archive` returned `status=ok`, `rsync` copied 40,626,264,258 bytes into `/mail/import-staging/maildirpp-archive-export-20260701-215204`, `copy_exit=0`, `staging_manifest_exit=0`, `staging_inventory_exit=0`, `staging_part_count=21`, staging size was 38G, and antiX staging `verify-archive` returned `status=ok` with 21 parts, 40,613,836,495 archive bytes, and `staging_verify_archive_exit=0`.
- antiX restore of the converted Maildir++ archive succeeded on 2026-07-04. The staged archive was reverified before unpack with `status=ok`, then unpacked into `/mail/Mailstore/evolution/local-maildir` with `unpack_exit=0`, 115 directories, 48,721 regular files, 62,430,783,277 regular bytes, 29 Maildir folders, 48,720 `cur` files, 0 `new` files, and 0 `tmp` files. Restored-tree `verify-tree` returned `status=ok` and `verify_tree_exit=0`; final `inspect` returned 29 Maildir folders, 28 dot folders, 48,720 `cur`, 0 `new`, 0 `tmp`, 1 metadata file, 62,430,783,277 regular bytes, `status=ok`, and `inspect_exit=0`.
- A separate Betterbird post-conversion delta was selected on Fedora on 2026-07-04 by comparing the current Betterbird source against the original conversion state. The owner chose to include all 248 candidates, including 8 same-Message-ID but different-content variants, to prioritize data preservation over deduplication.
- The Fedora delta conversion target `/home/atiq/Evolution-Mailstore/betterbird-delta-maildirpp-20260704` was created as canonical Maildir++ and validated with converter SHA256 verification: 248 copied messages, 248 verified messages, 0 skipped/errors, 248 `cur`, 0 `new`, 0 `tmp`, 418M apparent target size, and folder counts Inbox 241, Sent 4, Local_Folders.November2025 2, Trash 1.
- The owner validated the Fedora delta target in Evolution Flatpak as a separate `Maildir-format mail directories` account: Inbox 241, Sent 4, November2025 2, Trash 1, messages opened, HTML rendered, attachments were visible, and no error popup appeared.
- The Fedora delta target was packed with `maildirpp_transport.py` into `/home/atiq/maildirpp-delta-export-20260704-151448`. Fedora `pack` and `verify-archive` succeeded with 1 part, export size 286M, archive bytes 299,189,312, and `status=ok`.
- The delta export was copied to the Fedora HGFS/Win11 host share at `/mnt/hgfs/Win11Host_Shared4VM/maildirpp-delta-export-20260704-151448` after the share resolved to `fuse.vmhgfs-fuse` and normal-user write probes passed. Host-share `verify-archive` succeeded with 1 part, 299,189,312 archive bytes, and `status=ok`.
- The owner then manually transferred the delta export to antiX through Google Drive as `/home/atiq/Downloads/maildirpp-delta-export-20260704-151448.zip`; antiX ZIP inspection succeeded with SHA256 `65ff42dd1897dad29c09a6cb666bd019c917d52031860dc63c729daa431d8f44`, exactly one top-level export folder, and expected `manifest.json`, `inventory.jsonl`, and `maildirpp-archive.tar.gz.part0001`.
- The antiX delta package was extracted to `/mail/import-staging/maildirpp-delta-export-20260704-151448` and verified with `verify-archive` before unpack. It was restored to `/mail/Mailstore/evolution/betterbird-delta-maildirpp-20260704`; `unpack`, `verify-tree`, `inspect`, independent counts, and Evolution Flatpak visibility all succeeded with 248 `cur`, 0 `new`, 0 `tmp`, and folder counts Inbox 241, Sent 4, Local_Folders.November2025 2, Trash 1.
- The owner validated the restored antiX delta target in Evolution as a separate `Maildir-format mail directories` account: Inbox 241, Sent 4, November2025 2, Trash 1, messages opened, HTML rendered, attachments were visible, and no error popup appeared.
- The Betterbird delta archive must remain separate from `/mail/Mailstore/evolution/local-maildir` and `/mail/Mailstore/mbsync/provider-live` unless a later merge is explicitly planned, backed up, and tested.
- `src/betterbird_post_archive_delta.py` is the current future-facing delta helper. It uses `/home/atiq/Evolution-Mailstore/betterbird-archive-maildirpp/.conversion-state.sqlite` as the baseline for the original main archive/export `maildirpp-archive-export-20260701-215204`, expects 48,720 copied baseline rows by default, and never uses email `Date:` headers as the cutoff.
- The dynamic post-main-archive helper selects every current Betterbird source file absent from the main baseline, whether the current count is 249, 500, or another later value. It first excludes exact `(source_path, size, mtime_ns)` matches, then relative-path matches when the source root changed, then SHA256 matches to avoid falsely selecting old mail after path or mtime changes.
- The dynamic helper stages selected mail under `/home/atiq/Evolution-Mailstore/post-main-archive-work/RUN_ID/staging-betterbird-maildir`, plans converter targets as `/home/atiq/Evolution-Mailstore/betterbird-post-main-archive-maildirpp-RUN_ID`, refuses to overwrite existing staging/target directories, refuses missing baseline state, reports `status=no_post_archive_mail` when there is no delta, and blocks over 10,000 selected messages unless explicitly overridden.
- The previous 248-message delta remains validated historical data. Future post-main-archive aggregate runs supersede older post-main-archive delta Evolution accounts after the newer aggregate is validated. Disable or remove the older 248-message delta account in Evolution UI to avoid duplicate search results; do not delete old delta files until backup and cleanup are explicitly planned.
- The Betterbird delta Maildir++ guide now documents the dynamic "Post-Main-Archive Aggregate Delta" workflow. It begins with a RUN_ID-based Fedora audit, then mail-process shutdown, staging, converter dry-run, copy/hash verification, pack/verify, optional Google Drive ZIP, antiX staging, antiX restore/inspect, and Evolution GUI validation.
- Fedora dynamic aggregate run `20260704-205827` succeeded through pack/verify on 2026-07-04. It selected 249 post-main-archive messages from the current Betterbird source, all `cur`, 0 `new`, 436,492,704 selected bytes, with folder split Inbox 242, Sent 5, and Local_Folders/November2025 2. It staged 249 `cur`, converted and SHA256-verified 249 messages into `/home/atiq/Evolution-Mailstore/betterbird-post-main-archive-maildirpp-20260704-205827`, then packed and verified `/home/atiq/maildirpp-post-main-archive-export-20260704-205827` as 1 part, 299,207,989 archive bytes, 286M export, `status=ok`.
- The owner confirmed the old 248-message delta's `Trash: 1` difference is expected because that Trash email was manually deleted from Betterbird before the new dynamic aggregate run. Therefore aggregate `20260704-205827` should be treated as a current-source post-main-archive snapshot, not as a strict superset of the older 248-message delta. Do not use old-delta hash equality as a blocker for this run; keep old delta files until backup and cleanup are explicitly planned if the intentionally deleted Trash message may ever be needed.
- Fedora Batch 7A visibility check for aggregate `20260704-205827` succeeded. The host and Evolution Flatpak sandbox both saw 249 `cur`, 0 `new`, and 0 `tmp`; host folder counts were `.mail_tagindustries_com_sg.Inbox` 242, `.mail_tagindustries_com_sg.Sent` 5, and `.Local_Folders.November2025` 2.
- The owner validated dynamic aggregate `20260704-205827` in Fedora Evolution as a separate `Maildir-format mail directories` account: Inbox 242, Sent 5, November2025 2, Trash 0, messages opened, HTML rendered, attachments were visible, and no error popup appeared.
- The Betterbird delta Maildir++ HTML guide CSS was corrected after screenshot review so `pre code` command blocks no longer inherit the pale inline-code background. Brave rendering validation confirmed command text is visible on the dark code block and inline code still uses the pale style.
- The Betterbird delta Maildir++ HTML guide `Start Here` section now renders Batch 1 through Batch 9 as separate command blocks with `Copy Batch 1` through `Copy Batch 9` buttons. Each button copies only the command text inside that batch's shell block, not headings, explanations, or fenced-code markers.
- The Maildir++ archive guide now uses the validated antiX restore chunk based on `~/codex-runs/maildirpp_transport.py` instead of fragmented repo-relative restore examples. This prevents the earlier variable-only no-op pattern and keeps pre-unpack archive verification, destination-empty refusal, unpack, `verify-tree`, and final `inspect` in one logged operation.
- The converted Betterbird archive must stay separate from mbsync `provider-live`; it is a local read/archive Maildir++ account, not a live IMAP sync tree.
- Evolution Flatpak 3.60.2 from Flathub was validated on antiX 26 runit with zzzFM/IceWM. It is installed as a user Flatpak, granted `/mail:create`, and its app directory is symlinked from `~/.var/app/org.gnome.Evolution` to `/mail/AppData/flatpak-evolution/appdir`.
- Evolution Flatpak desktop launching should use `scripts/evolution_flatpak_icewm_launcher_setup.sh` on antiX. The validated setup creates `~/.local/bin/evolution-flatpak-mail`, adds marked IceWM `~/.icewm/personal` and `~/.icewm/toolbar` entries, adds a marked keyring startup block to `~/.icewm/startup`, and keeps wrapper-level keyring fallback.
- The validated IceWM launcher uses the official Flatpak-exported icon at `~/.local/share/flatpak/exports/share/icons/hicolor/scalable/apps/org.gnome.Evolution.svg`. The tested antiX VM showed SHA256 `dcda7580deebec635ff3d560795a45dd41e62ce3e7d311a170b46b4cf2a0cbb2`.
- The verified Evolution test account uses `BackendName=maildir` and reads a Maildir++ test tree at `/mail/Mailstore/evolution/test-maildir`. Selecting `MH-format mail directories` is a known wrong path because it can show folder names while hiding Maildir `cur`/`new` messages.
- The first approved mbsync path is APT `isync` on antiX/Debian, not a source build, unless target VM package discovery shows an unsuitable version.
- The first mbsync test used the isolated target `/mail/Mailstore/mbsync/provider-inbox-test`, with state in `/mail/AppData/isync/state/provider`, logs in `/mail/Logs/mbsync`, and `~/.config/isyncrc` reading `~/.config/isync/provider.pass` via `PassCmd`.
- antiX validation on 2026-06-27 confirmed `isync 1.5.1`, successful IMAP login to `mail.tagindustries.com.sg`, 144 INBOX messages pulled into `/mail/Mailstore/mbsync/provider-inbox-test`, post-Evolution mbsync succeeded, and Evolution Flatpak created a `BackendName=maildir` source pointing at the test Maildir path.
- The first mbsync channel is INBOX-only and local-preserving: `Sync PullNew`, `Create Near`, `Remove None`, and `Expunge None`. `mbsync --dry-run` hit an isync 1.5.1 assertion on the empty test Maildir, so validation proceeded with logged real pulls after confirming the target was empty and deletion-safe policy was intact.
- The antiX mbsync config/log snapshot was saved under `/mail/Backups/mbsync` before production promotion. Empty remote folders `FARSUK` and `BASUNDHARA` were verified empty and removed from the IMAP server at the owner's request.
- Production mbsync `provider-live` was validated on 2026-06-27 with the exact remote folders `INBOX`, `Drafts`, `Trash`, `spam`, `Sent`, `Junk`, and `Archive`. It uses `/mail/Mailstore/mbsync/provider-live`, state in `/mail/AppData/isync/state/provider-live`, logs in `/mail/Logs/mbsync-live`, `PipelineDepth 1`, and `UseNamespace yes`.
- The current production mbsync config keeps normal folders receive-only in channel `provider-live` with explicit `Patterns "INBOX" "Drafts" "Trash" "spam" "Junk" "Archive"`, `Sync PullNew`, `Create Near`, `Remove None`, and `Expunge None`. Sent mail is handled by a narrow channel `provider-live-sent-upload` mapping only `Sent <=> Sent` with `Sync PullNew PushNew`, `Create None`, `Remove None`, and `Expunge None`.
- The first `provider-live` pull downloaded 163 messages: 151 INBOX, 4 Sent, and 8 Trash. A second pull had zero delta and no duplicate flood. After Evolution SMTP testing and Gmail replies, a post-SMTP pull reached 170 messages: 158 INBOX, 4 Sent, and 8 Trash. `tmp` remained empty throughout.
- Evolution Flatpak reads the production `provider-live` tree as `BackendName=maildir` with path `/mail/Mailstore/mbsync/provider-live`. The owner verified opening/searching production messages and successfully sent two messages by SMTP to Gmail; Gmail replies were pulled back into `provider-live`.
- Evolution's production account `TAG-Mustang_mbsync-Live` now stores sent copies in `folder://ca9e0701d552924b5b13afa4b29213e130bba0bf/Sent`, which corresponds to `/mail/Mailstore/mbsync/provider-live/.Sent`.
- Sent upload was validated on 2026-06-27. A controlled Sent-only upload changed remote `INBOX.Sent` from 4 to 5 messages with `Far: +1`; a second Sent sync had zero delta. A later automatic loop test saved one new local Sent message and uploaded it to remote Sent, reaching local Sent 6 and remote `INBOX.Sent` 6 with `MaxPushedUid 6`.
- The production auto-sync loop uses `~/.local/bin/mbsync-provider-live-loop` and `~/.local/bin/mbsync-provider-live-control`, defaults to a 180-second post-sync delay, writes one daily auto log under `/mail/Logs/mbsync-live`, targets `provider-live-group`, supports `status`, `pause`, `resume`, `sync-now`, `logs`, `cleanup-logs`, `clear-stale-lock`, `stop-loop`, and `start`, and starts from a marked IceWM startup block. Final audit confirmed repeated automatic group runs with exit `0`, no lock left behind, and `tmp` still empty.
- The verified IceWM startup marker is uppercase `# BEGIN CODEX MBSYNC PROVIDER LIVE AUTOSYNC`; the startup block runs `~/.local/bin/mbsync-provider-live-control start`.
- The provider-live auto-sync scripts now include validated log retention: auto logs older than 2 days are compressed if `gzip` is available, auto logs older than 30 days are deleted, manual/validation logs are kept for 90 days, and `status` reports `/mail/Logs/mbsync-live` disk usage. The antiX fake-log proof compressed an old auto log, deleted expired auto/manual logs, kept a recent manual log, and removed fake leftovers.
- The provider-live auto-sync scripts were further hardened on antiX on 2026-06-27 for slow or large syncs. The loop remains sequential, so a sync that takes longer than 180 seconds delays the next pass rather than overlapping it. Generated scripts default to `MBSYNC_PROVIDER_LIVE_SYNC_TIMEOUT_SECONDS=3600`, wrap `mbsync` with `timeout` when available, write lock metadata, expose `sync_timeout_seconds`, sync age, sync channel, sync pid, and pid liveness in `status`, and add `clear-stale-lock` for non-running stale locks. The antiX proof created a fake 10-minute-old lock with pid `99999999`, showed `sync_age_seconds=600` and `sync_pid_alive=no`, cleared it successfully, then validated `sync-now` and the restarted loop with exit `0`, `sync_lock=absent`, and all provider-live `tmp` folders empty.
- On 2026-07-11, the helper-generated controls gained persistent dynamic intervals: `interval`, `set-interval VALUE`, and `reset-interval`, with 60 seconds through 24 hours accepted and the mode-600 override stored under `/mail/AppData/isync/provider-live-loop`. The antiX rollout was validated end to end: `30s` was rejected with exit 2, a `2m` override was stored with mode 600, manual and automatic group syncs exited 0, the running loop logged live changes from 120 to 60, 60 to 120, and 120 to 180 seconds, and final state returned to the original `3m` default with loop running, paused=no, lock absent, and provider-live `tmp=0`. Installed loop/control hashes are `06063c0c80219120e99344fc9a0d847c8e092f686e9c56ea502bf46d986111b6` and `8892bc03b3c462ae6f56cd029c6beae54f37df785072affe04c1e217dd59d3d2`; rollback backup is `/mail/Backups/mbsync/20260711-111637-before-dynamic-interval`.
- The 2026-07-11 provider-live archive Chunk 1A preflight observed a later operator-selected persistent interval of 60 seconds. mbsync remained healthy with loop pid 10451, last exit 0, no lock, 912 root-INBOX messages, 1.2G total provider-live usage, and every Maildir `tmp` empty; the shorter interval does not block archive monitoring or transactions.
- A 2026-07-11 missing-mail diagnostic found the installed provider-live system healthy: loop running, not paused, lock absent, repeated exit `0`, valid IMAPS TLS, 103G free under `/mail`, local arrivals through 00:30, and INBOX state updated at 00:33. The owner then identified the cause: another POP client had `leave a copy on server` disabled, so it removed messages before IMAP/mbsync could retrieve them. The POP client must retain server copies or be changed to IMAP; already removed messages must be preserved/exported from that client.
- The generated provider-live control script now verifies that a saved loop PID belongs to `mbsync-provider-live-loop` before reporting the loop as running or sending a stop signal. Stale loop PID files are shown as `stale_loop_pid` and ignored for process killing. The timeout wrapper uses GNU `timeout --kill-after=60s` when supported and falls back to plain `timeout` otherwise.
- The provider-live archive controller was installed and hash/mode-validated on antiX on 2026-07-11. Its archive root `/mail/Mailstore/evolution/provider-live-archive` and `.Inbox` hierarchy are canonical empty Maildirs with mode 700; archive and tmp file counts are zero. Engine/transport/control modes are 700/600/700, mbsync and notmuch configs were hash-unchanged, existing services remained healthy, and the archive monitor remains intentionally stopped. The threshold is 15,000 immediate root-INBOX files, monitor interval is 900 seconds, and all transfer/cleanup actions require explicit run-ID approvals. Normal mbsync remains receive-only and Sent remains the only `PushNew` channel; the engine refuses any policy drift before snapshot or cleanup.
- Evolution source verification completed for the new account `TAG-Mustang Local Archive`: exactly one source uses `BackendName=maildir` and `/mail/Mailstore/evolution/provider-live-archive`; it is non-default, has no SMTP backend or archive-owned Sent folder, and its transport backend is `none`. The existing `TAG-Mustang_mbsync-Live` Sent destination remains unchanged. The archive is still empty and Evolution was stopped before the next notmuch step.
- notmuch scope enrollment for `provider-live-archive` completed on antiX on 2026-07-11. Before/after counts stayed at 650 unique messages and 1,176 indexed files: 929 provider-live files, 247 approved small-delta files, zero files from the empty new archive, and zero files from the 59G Betterbird `local-maildir`. Config/tag/path backups were created at `/mail/Backups/notmuch/provider-live-archive-scope-20260711-191741` and their inventory hashes verified. The index loop and browser restarted cleanly; mbsync, notmuch, and archive transaction locks were absent. The archive monitor was intentionally left stopped at that gate and started only after the separate validation below.
- The 15-minute provider-live archive threshold monitor and IceWM startup block were installed and validated on antiX on 2026-07-11. Repeated startup installation left exactly one marked block with mode 700. The simulation was non-persistent; the real check recorded 915 messages below the 15,000 threshold. A subsequent reboot/login started a new monitor as pid 2392 through IceWM, completed a fresh check, and left alert/transaction/sync/refresh state clear. Archive/tmp remained empty, configs stayed unchanged, and mbsync/notmuch services remained healthy. The monitor rollout is complete.
- A manual desktop-popup test and discovery audit on 2026-07-12 found a healthy IceWM session D-Bus and `notify-send 0.8.6`, but initially no owner/service/process/startup entry for `org.freedesktop.Notifications`. Debian APT `dunst 1.12.2-1` was then installed and current-session validated: its D-Bus service exists, one Dunst process owns the notification name, exactly one marked IceWM block starts before the archive monitor, and the owner saw the critical test popup. Mail/config state and related services remained unchanged. Reboot persistence remains the final notification gate.
- Archive runs target every INBOX message at the stable cutoff. A full provider-live hard-link snapshot and copied mbsync state/config are retained under `/mail/Backups/provider-live-archive`; a portable split gzip package on a verified USB/HGFS filesystem is mandatory. Monthly archive copies preserve message bytes and Maildir flags while using new names without mbsync `U=` metadata. Approval 2 requires copy, Evolution, notmuch, same-disk snapshot, and external-package verification plus a one-message mbsync canary.
- Local validation on 2026-07-11 passed 19 provider-live archive tests and Python/shell syntax checks; the full suite passed 46 tests with 2 expected Windows skips. Chunk 1B confirmed safe Evolution/notmuch state and no active locks, but found no real USB/HGFS mount. External-target validation now rejects `/media/$USER` when its nearest mount is `/`, preventing the root filesystem from being mistaken for external backup. Archive-account registration, notmuch scope enrollment, the running 900-second monitor, and post-reboot IceWM autostart are validated on antiX. Runtime validation remains required for a real external package and the first live mbsync cleanup canary at threshold. This historical/current-VM gate kept the 59G Betterbird archive excluded; the approved fresh-VM workflow now restores and indexes the exact verified package instead.
- `scripts/mbsync_provider_inbox_setup.sh` now includes production helper commands for the validated setup: `production-layout`, `write-production-config`, `production-list`, `production-sync`, `production-status`, `write-autosync`, `install-autosync-startup`, `autosync-status`, `autosync-preflight`, `refresh-autosync`, `autosync-stale-lock-proof`, `autosync-validate`, and `autosync-log-rotation-proof`.
- `docs/MBSYNC_ANTIX_GUIDE.html` and `.txt` now include a clear `Start Here: Fresh Setup` sequence before the production reference material. Fresh antiX runs start with the INBOX validation helper sequence, then production `provider-live`, then Evolution GUI checks, then auto-sync validation. The production mbsync config block is explicitly labeled reference-only so it is not mistaken for a terminal command.
- `docs/WORK_VALIDATION_LEDGER.md` is now the durable record for all future terminal-guided and validation-heavy work, regardless of topic. It records chunk/action, output source, result, notes, and follow-up without storing secrets or massive raw logs. Historical entries are summarized from confirmed project memory because earlier exact pasted-output pairs were not preserved.
- notmuch 0.39 is now configured on antiX as a sidecar pilot index. The config is at `~/.config/notmuch/default/config`, the disposable database is `/mail/SearchIndex/notmuch/default`, the mail root is `/mail/Mailstore`, `maildir.synchronize_flags=false`, and `index.decrypt=false`.
- The notmuch pilot currently indexes only `/mail/Mailstore/mbsync/provider-live` and the restored Betterbird delta scope. It produced 457 unique messages and 867 files after the 2026-07-05 refresh. On 2026-07-09, the browser hit stale indexed Maildir `new/` paths after provider-live changed; a locked `notmuch-browser-control refresh-index` run detected 12 file renames, added 102 new messages, and updated the pilot index to 559 unique messages and 1,025 indexed files. The 59G archive remains ignored because a later audit showed `/mail/Mailstore/evolution/local-maildir` at 48,564 `cur` instead of the earlier verified 48,720, and the delta count also drifted from 248 to 247.
- Astroid `0.16+20240629-1` was purged on 2026-07-05 after its settings were backed up to `/mail/Backups/notmuch/astroid-retired-20260705-232014`. Its user config/cache/helper paths were removed, and the Astroid-only compatibility symlinks under `/mail/SearchIndex/notmuch/default` were removed after being verified as symlinks.
- Evolution Flatpak must not be used as a raw notmuch file opener. `xdg-open` on a selected Maildir file opened Evolution's wrong `Import Data - Berkeley Mailbox (mbox)` flow instead of a normal message viewer or reply/forward workflow. Evolution remains the normal read/reply/send mail client for its configured Maildir accounts.
- The validated notmuch GUI path is the read-only localhost browser viewer at `~/.local/bin/notmuch-browser-pilot`, launched on `http://127.0.0.1:8765/`. It now uses single-email mode: search results are individual messages, `/message?id=<message-id>` opens one selected email body, exact `id:<message-id>` search works, and the Status page reports `Viewer mode: single-email`. This viewer is read/search/view only; reply/send stays in Evolution.
- notmuch backup/restore/reindex helper validation has passed for the pilot scope. Corrected Chunk 8B-FIX1 passed shell/Python syntax checks, tag backup/restore, exact `id:` search, indexed path stability, and zero `tmp` changes. Chunk 8C then moved the old database to `/mail/Backups/notmuch/reindex-db/default-20260705-084026`, rebuilt only `/mail/SearchIndex/notmuch/default`, restored tags from `/mail/Backups/notmuch/notmuch-tags-before-reindex-20260705-084026.batch-tag`, preserved 444 messages and 846 files at that time, kept the pilot Maildir file list unchanged, kept `tmp` folders empty, resumed mbsync, and relaunched the browser viewer.
- `docs/NOTMUCH_BROWSER_ANTIX_GUIDE.html` and `.txt` now document the active browser-only path, including Astroid purge, single-email viewer installation, shim removal, daily browser-viewer start/stop commands, status checks, CLI search examples, tag backup/restore, pilot-safe reindex, and the deferred full-archive expansion preflight. The older `docs/NOTMUCH_ASTROID_ANTIX_GUIDE.html` and `.txt` are retained only as retired historical guides.
- The current source revision is the Go chi/templ-backed service in `cmd/notmuch-browser` and `internal/notmuchbrowser`. It keeps `GET /`, `/search`, `/message`, `/status`, and `/healthz`, plus signed `GET /attachment`, `/attachments.zip`, and `/inline-image` routes. It executes notmuch with argument arrays, uses `show --format=raw --part=N --duplicate=N --decrypt=false` for decoded MIME bytes, renders compiled templ components, embeds compiled local Tailwind/HTMX/application assets, refuses non-localhost binds, and checks the established notmuch safety configuration at startup.
- The attachment/image revision keeps Maildir, notmuch tags, and the notmuch database non-mutating. A random startup HMAC key binds capabilities to purpose, Message-ID, selected duplicate, MIME part, filename, and media type. Individual/ZIP output is bounded and staged under mode-700 `/mail/AppData/notmuch-browser/download-tmp` as mode-600 files, then removed after each response/error; stale owned files older than 24 hours are cleaned at startup. Download concurrency is one, inline-image concurrency is two, and health/status/control output reports the temporary-file contract.
- Untrusted HTML is reserialized with pinned `golang.org/x/net/html v0.57.0` under 32 MiB document and 8 MiB token limits. Scripts, events, nested frames, objects, embeds, forms, foreign objects, refresh/CSP/base metadata are removed; CID references are rewritten to signed local image URLs. The sandboxed iframe starts with images blocked, embedded/data/SVG images require the first HTMX confirmation, remote HTTP(S) images require a second warning, the Go service never fetches remote images, and permission resets on reload/message/duplicate changes.
- The Frost/Sapphire UI uses a compact adaptive sidebar, dense three-column result pane with inline tags/file count, keyboard/pointer splitter, reading pane, responsive mobile stack, attachment rows, Save All, local Lucide icons, and no CDN/runtime Tailwind. Local visual QA at desktop and 390x844 mobile widths found and fixed result-pane horizontal overflow, HTMX reading-pane scroll restoration, and collapsed-navigation accessible names.
- The chi/Tailwind browser revision was installed and validated on antiX on 2026-07-09 from minimal bundle `/mail/notmuch-browser-chi-src-20260709-222603.tar.gz`, SHA256 `9a53fabf109e492723e8619dd1e07b09d210ebd30c67197c840a11d26a21dbf5`. `go test ./...` passed on antiX, the installed binary SHA256 is `78f3fce722240999ad6bcfe57866a29a4fb58cceb0852d57529fac78b0ad58bd`, `/static/app.css` served the compiled local Tailwind CSS at 10,801 bytes, `/healthz` returned OK, search and single-message rendering worked, read-only proof preserved message/file/inbox counts, Maildir `tmp=0`, indexed file hash, and tag hash, and the index loop remained healthy. After validation the index held 638 unique messages and 1,158 indexed files.
- `scripts/notmuch_browser_control.sh` is the installable antiX control source for the Go browser service. It expects the compiled binary at `~/.local/bin/notmuch-browser`, uses logs under `/mail/Logs/notmuch-browser`, stores state under `/mail/AppData/notmuch-browser`, keeps Windows access on an SSH tunnel, and makes `refresh-index` skip when `/mail/AppData/isync/provider-live-loop/lock` is present so it does not overlap the provider-live mbsync loop.
- `scripts/notmuch_browser_index_control.sh` is the source for a small optional antiX index-refresh loop. It is meant to be installed as `~/.local/bin/notmuch-browser-index-control`, starts/stops/statuses the loop, installs a neutral IceWM startup block, writes logs under `/mail/Logs/notmuch-browser`, and calls the existing locked `refresh-index` command so mbsync and notmuch refreshes do not overlap.
- Windows 11 now has native Go 1.26.5 installed in `PATH`; `go version` reports `go1.26.5 windows/amd64`. The attachment/image revision's original local validation used an isolated official Go 1.26.4 toolchain for tests, vet, coverage, and a stripped build; after the persistent installation, `go test ./...` and `go vet ./...` also passed directly under native Go 1.26.5. The unchanged 30-member transfer bundle remains 65,392 bytes with SHA256 `f56332f7746ba0a621da784135cdb1379a8e3f1310e7bfd2de9ed00ac2af5f9e`. Windows source-level validation does not replace antiX/notmuch integration, Linux binary, read-only manifest, SSH-tunnel, performance/RSS, or reboot validation.
- On 2026-07-06, antiX operator chunks inspected the pinned `kerolloz/go-installer` v3.0.0 script and installed Go 1.26.4 under `/mail/AppData/go/goroot`, with GOPATH `/mail/AppData/go/workspace` and a managed block in `/home/atiq/.profile`.
- The first antiX Go test/build chunk loaded Go successfully but stopped before validation with `status=blocked_repo_not_found`; the repository was not cloned under the searched `/home/atiq` paths. A later minimal raw-file download attempt from pinned commit `34479bdf9312b3fd714af00bd34890578bf0b18a` stopped immediately with HTTP 404, and a sparse checkout attempt failed because GitHub HTTPS password authentication is not supported. The successful path was a credential-free minimal source bundle copied to `/mail/notmuch-browser-min-src-efc2990.tar.gz`.
- On 2026-07-06, antiX validated the minimal Go browser source bundle: SHA256 matched, `scripts/notmuch_browser_control.sh` passed `sh -n`, `go test ./...` passed, and a temporary Linux binary was built at `/home/atiq/codex-runs/notmuch-browser-build-20260706-212117/notmuch-browser`.
- On 2026-07-06, antiX installed the tested binary as `/home/atiq/.local/bin/notmuch-browser` and the control script as `/home/atiq/.local/bin/notmuch-browser-control`. Install validation confirmed the notmuch safety baseline, control-script `status` and `tunnel-hint`, no mbsync/refresh locks, 457 messages, 867 indexed files, and no listener on port 8765 before service start.
- On 2026-07-06, antiX started and locally validated the installed Go browser service. It listened only on `127.0.0.1:8765`, `/healthz` returned OK/read-only/single-email mode, status/search/message/duplicate views rendered, logs were written under `/mail/Logs/notmuch-browser`, and read-only proof passed with unchanged message count, indexed file count, indexed file hash, tag hash, and Maildir `tmp` count.
- The owner manually validated the antiX browser UI at `http://127.0.0.1:8765/`: `tag:inbox` search worked, opening one email worked, body/HTML rendering, attachments, duplicate selector, and Status page looked correct. The first Windows tunnel attempt failed only because `ANTI_X_VM_IP` was used literally; the next step is to discover the actual antiX VM IP and verify SSH reachability from Windows.
- antiX IP/SSH discovery showed `sshd` running and listening on port 22. Candidate VM IPs are `192.168.254.129`, `192.168.254.128`, `192.168.4.128`, and `192.168.4.129`; the best first Windows tunnel target is `192.168.254.128` because it is the DHCP source address on the default route via `192.168.254.2`.
- Windows 11 SSH tunnel validation succeeded with `ssh -N -L 8765:127.0.0.1:8765 atiq@192.168.254.128`. With the tunnel open, `http://127.0.0.1:8765/` worked from Win11 and the owner validated `tag:inbox` search, opening an email, body/HTML rendering, attachments, duplicate selector, and Status page through the tunnel.
- After the chi/Tailwind install, the owner manually confirmed both antiX local browser access and Windows tunnel access: `search/results/message/body/html/status look right`.
- The 2026-07-12 attachment/image revision has been transferred, built, isolated-tested, synthetic-fixture-tested, backed up for rollback, atomically installed, and validated for exact production attachment/ZIP bytes. Strict read-only, manual antiX UI, Windows tunnel, controlled reboot, and final guide gates all passed by 2026-07-14.
- Attachment/image rollout Chunk 1A passed on antiX with Go 1.26.4, notmuch 0.39 raw-part/duplicate support, the required safety configuration, both mbsync and refresh locks absent, production browser/index loops healthy, listener restricted to `127.0.0.1:8765`, and isolated test port 8876 free. The pre-rollout comparison baseline is 657 unique messages, 1,189 indexed files, 657 inbox messages, 49,905 Maildir message files, zero Maildir `tmp` files, indexed-path SHA256 `9baefe9cdeeb689d15fe5bac9560035dc6afbae9bef86385f7c4ce6b0bc7622e`, tag SHA256 `870afb45d3098d2c9933fc13d3efe7e54bd8a0181bbf6406d791249b10838aba`, and Maildir-name SHA256 `4156e002a069e4d3dd088da1ee453788ee5fcf7e1ac12367c62439615d276e88`. The future download-temp directory was absent, `/mail` had 103 GB free, and no source, installed binary, mail, tag, or index mutation occurred.
- Attachment/image rollout Chunk 1B completed after correcting the audit helper's argv construction, exact-message duplicate query, and body-less metadata recognition. The final read-only run saw 658 messages and 1,191 files before and after, scanned 80 attachment-tagged messages, found 468 attachment candidates, a four-file duplicate Message-ID, a real PNG/CID message, and a remote-image message. Direct notmuch raw-part probes for duplicate 1 and duplicate 2 each returned the same 4,078 decoded bytes with SHA256 `4814092b854d37e6cfa9057a2d6ce65fcccd07f3a9c626e95c43b5c693cbc563`. Real data-image, SVG, and unsupported-image samples were absent from the bounded scan and remain assigned to the synthetic fixture gate. Maildir `tmp` stayed zero, both locks stayed absent, and the private sample map is `/mail/AppData/notmuch-browser/attachment-rollout-audit.json`, mode 600, SHA256 `70add48c9930988aa06945b073da84db59444baa3c4a816f6b77120b108a8650`.
- Attachment/image rollout Chunk 2A/2B transferred and independently verified the unchanged 30-member source bundle, safely extracted exact regular-file inventory, verified module pins and asset budgets, passed `go mod verify`, module download, `go test -count=1 ./...`, and `go vet ./...` under antiX Go 1.26.4, and built temporary Linux binary `/home/atiq/codex-runs/notmuch-browser-attachment-src-20260712-164424/notmuch-browser`, 9,724,169 bytes, SHA256 `50e6b1adbcf32201496f6d6f5a3d53c060d0c39e1831ca1b5ecac3d48838f37e`. Closing checks proved all installed production hashes unchanged, production healthy on `127.0.0.1:8765`, the temporary binary not running, port 8876 free, both locks absent, Maildir `tmp=0`, and download-temp still absent. No production install or restart has occurred.
- Attachment/image rollout Chunk 2C-1 started temporary PID 40217 from the verified binary on exactly `127.0.0.1:8876`. The expanded health contract, root/search/status, HTMX fragment, security headers, static asset byte counts, invalid-capability 403 responses, POST 405 guard, mode-700 empty download-temp directory, stable 658-message/1,191-file counts, zero Maildir `tmp`, unchanged installed binary hash, and simultaneous production health on `127.0.0.1:8765` all passed. The temporary server remains intentionally running under mode-600 `/mail/AppData/notmuch-browser/isolated-8876.pid` only for the next capability/download/image gate; it is not installed or supervised.
- Attachment/image rollout Chunk 2C-2 used the private production sample to prove exact attachment and inline-image bytes against direct notmuch for duplicate 1 and duplicate 2: 4,078-byte PNG SHA256 `4814092b854d37e6cfa9057a2d6ce65fcccd07f3a9c626e95c43b5c693cbc563`. Tampered and wrong-purpose capabilities returned 403; blocked, embedded, and remote iframe CSPs plus full-page permission reset passed. Save All for both duplicates produced identical four-entry Store-mode ZIPs, 827,904 archive bytes and 827,296 decoded bytes, SHA256 `b82ceaa72c582851c5e2a53a5148862cc193a2f14aeedc42d9fb07372d4559f4`, with safe unique names and the selected part present. Counts stayed 658/1,191, Maildir `tmp=0`, installed production unchanged, and download-temp returned to zero files. The temporary server was stopped, isolated PID file removed, and port 8876 is free.
- Attachment/image rollout Chunk 3A created a fully separate synthetic Maildir/config/database under `/home/atiq/codex-runs/notmuch-browser-synthetic-20260713-063457`, containing one unique Message-ID and two indexed duplicate files. Each duplicate exposes 13 MIME parts: PNG/SVG CID resources, data and remote HTML images, repeated `report.txt` attachments, duplicate-specific PDF/text/TIFF payloads, and one attached `message/rfc822` part whose filename is not exposed by notmuch. Malicious HTML elements are intentionally present for sanitizer tests. Raw-part audit proved inline resources identical across duplicates and attachment/message payload isolation. Manifest SHA256 is `3df54297997852ebdb6e3d0a6d27db5f08f0107e7815dcc23b83f8cd25be8e36`; mode-600 fixture audit SHA256 is `64ed5bd73259203c1f928fa9b4c2c98b1da1ed81a64c56c697916ee54ee1d435`; the mode-600 current pointer records the root. Production messages/files/path/tag/mail-name hashes, Maildir `tmp`, installed binary, and health remained unchanged.
- Attachment/image rollout Chunk 3B-1 started the verified binary against only the synthetic config/index on `127.0.0.1:8876` and a local request canary on `127.0.0.1:8899`. Health reported exactly one message/two files and synthetic paths. Blocked, embedded, remote, full-reload reset, and duplicate-switch reset policies passed; scripts/events/frames/objects/embeds/forms/base/foreignObject/refresh metadata were removed, CID/data/inline-SVG behavior was correct, seven attachment capabilities per duplicate included two collision-prone `report.txt` names, TIFF was download-only, attached message was included, and exactly two PNG/SVG inline capabilities existed. Rendering all modes caused zero canary requests, proving the Go service does not fetch remote images. Synthetic temp stayed empty and production remained exactly 658/1,191 with Maildir `tmp=0`. Synthetic PID 51940 and canary PID 51925 remain intentionally running for the final synthetic download/ZIP/SVG/canary gate.
- Attachment/image rollout Chunk 3B-2 proved all seven synthetic attachment routes per duplicate matched direct raw-part hashes, PNG/SVG inline bytes were exact, and raw SVG active content was constrained by sandbox/default-none CSP, same-origin resource policy, and nosniff. TIFF and attached-message downloads were exact; repeated names became `report.txt` and `report (2).txt`; both seven-entry ZIPs used Store mode, safe unique paths, and only the selected duplicate's exact parts. Duplicate ZIP hashes differed (`b07159f6...` versus `67a45b85...`). The Go service made no remote request; one controlled request to `/remote-canary.png` was issued only after remote CSP validation and was the canary's sole request. Synthetic temp/status returned to zero, production stayed 658/1,191 with Maildir `tmp=0` and unchanged installed hash, both isolated PIDs stopped, PID files were removed, and ports 8876/8899 are free. The synthetic fixture remains as private validation evidence but is not enrolled in production.
- Attachment/image rollout Chunk 4A created verified rollback backup `/mail/Backups/notmuch-browser/20260713-064920-before-attachment-images` before any stop or install. It contains the exact old browser SHA256 `78f3fce7...`, old control `2e5ca1fd...`, unchanged index control `df07cd07...`, notmuch config, IceWM startup, captured preinstall hashes/counts, checksum inventory, and a syntax-checked rollback script. The new binary is `50e6b1ad...`, new control `1b1277b0...`, and new index control remains `df07cd07...`. Mode-600 pointer `/mail/AppData/notmuch-browser/attachment-rollback-current` identifies the backup. Production remained running, healthy, localhost-only, lock-free, 658/1,191, Maildir `tmp=0`, and download-temp empty.
- Attachment/image rollout Chunk 4B atomically installed binary `50e6b1ad...`, control `1b1277b0...`, and index control `df07cd07...` without invoking rollback. An active provider-live sync was observed, auto-sync was paused, the index loop stopped, both locks cleared, and the old browser stopped before replacement. New production PID 59997 passed the expanded health contract, localhost listener, root/search/status, blocked and embedded production message rendering, and exact 20,775/5,371/51,238-byte assets. Counts stayed 658/1,191; path/tag/mail-name hashes, config, IceWM startup, Maildir `tmp=0`, and download-temp zero were unchanged. Index PID 60144 restarted, provider-live PID 2327 returned to its original unpaused state with last exit 0, and all locks were absent. The rollback backup/pointer remain retained until all post-install, Windows, and reboot gates pass.
- Attachment/image rollout Chunk 5 validated the installed production PID 59997 at `127.0.0.1:8765`. Individual attachment bytes for duplicate 1 and duplicate 2 exactly matched direct notmuch output: 4,078 bytes with SHA256 `4814092b854d37e6cfa9057a2d6ce65fcccd07f3a9c626e95c43b5c693cbc563`; inline-image bytes also matched. Tampered and wrong-purpose capabilities returned 403, blocked/embedded/remote CSP modes and full-page permission reset passed, and both selected duplicates produced valid four-entry Store-mode ZIPs of 827,904 bytes with SHA256 `b82ceaa72c582851c5e2a53a5148862cc193a2f14aeedc42d9fb07372d4559f4`. External `unzip -t` reported all four files OK. Client and server temporary files returned to zero. Messages/files stayed 658/1,191; indexed-path, tag, and Maildir-name hashes remained `92d6168d...`, `29cebcff...`, and `a082a943...`; Maildir `tmp=0`; browser and index loop remained healthy and lock-free. The production validator SHA256 was `27331bc88fadba12c17fad035d27eabf2f09b065239ba5c75d7d230fc0701911`.
- Attachment/image rollout Chunk 6 attempt 1 stopped safely before snapshots or browser operations. Under `set -o pipefail`, the initial `notmuch-browser-index-control status | grep -q` probe misclassified the running index loop because `grep -q` closed the pipe after its match while the status producer still had output. The script therefore did not stop index PID 60144 and correctly failed its later `loop=stopped` gate. Its cleanup trap resumed provider-live mbsync; no mail/index validation operation or mutation occurred. The corrected probe captures complete status before matching it.
- Attachment/image rollout Chunk 6 FIX2 corrected index detection, paused mbsync, stopped index PID 60144 despite a harmless disappearing-`/proc` warning, confirmed both locks absent, and captured a stable baseline: 658 messages, 1,191 files, 658 inbox, indexed-path SHA256 `92d6168d...`, indexed-content SHA256 `d3ff2de6...`, tag SHA256 `f0709895...`, 49,917 Mailstore files, Mailstore-metadata SHA256 `494c1730...`, zero Maildir/download temp, and expected installed/config/startup hashes. Root/search/status returned 200. The run then stopped before attachment operations because the production validator requires the base URL argument and was called without it. Cleanup restarted the index loop as PID 75157 and resumed mbsync. No after snapshot or mutation claim was made.
- Chunk 6's URL-argument retry reproduced the same stable baseline and 200 responses, then showed that the production validator also requires an audit-path argument at `sys.argv[2]`. It stopped before attachment operations, restarted the index loop as PID 79968, and resumed mbsync. The next step is to inspect every validator argument and preserve the complete strict-proof script under a separate stable filename before another retry.
- The Chunk 6 validator-argument audit recovered the complete strict-proof script from `/home/atiq/codex-runs/codex-batch.sh.before-validator-arg-20260713-071905` to separate `/home/atiq/codex-runs/notmuch-browser-chunk6-fix2-full.sh`; both have SHA256 `fb466957d308e349722364882a97c71dd49481d19f5c469bd838f9f18b4203e2` and pass shell syntax. Validator SHA256 remains `27331bc...`. Inspection proved it requires exactly three positional arguments: production base URL, private audit JSON path, and the current run directory. No service or mail operation ran during this audit.
- Chunk 6 FIX3 created patched strict script `/home/atiq/codex-runs/notmuch-browser-chunk6-fix3-full.sh`, SHA256 `62572ef62d911040a3683a24703a8137a524fce61d55da53e324aba166d25fcd`, and correctly supplied all three validator arguments. The stable 658/1,191 baseline and all prior hashes were reproduced. The validator passed exact duplicate 1/2 attachment bytes, tampered/wrong-purpose 403s, blocked/embedded/remote CSP, permission reset, inline-image bytes, and both four-entry 827,904-byte ZIPs, then printed `status=python_capability_validation_complete`. The harness stopped before its own temp checks and after snapshot because it incorrectly expected the outer Chunk 5 shell line `download_tmp_after_requests=0` in validator output. Cleanup restarted index PID 91544 and resumed mbsync; strict no-drift completion remains pending.
- Attachment/image rollout Chunk 6 FIX4 completed the strict stable-state proof using script SHA256 `d33e097b8ddd53352378de2f782bb51c53c48d0c6196b1865a1f089ef3dbda6e`. With mbsync paused and the index loop stopped, before/after snapshots were exactly identical: 658 messages, 1,191 files, 658 inbox, indexed paths `92d6168d...`, indexed content `d3ff2de6...`, tags `f0709895...`, 49,917 Mailstore files, Mailstore metadata `494c1730...`, config/startup/installed artifact hashes, Maildir `tmp=0`, and download temp zero files/bytes. The full exact-byte/capability/CSP/inline-image/four-entry ZIP validator passed between snapshots. `strict_snapshot_match=yes`, `mail_and_index_mutation=no`, and `download_temp_cleanup=verified`. Browser PID 59997 remained healthy; index restarted as PID 96511; mbsync PID 2327 resumed with `paused=no`; both locks were absent and final health stayed read-only at 658/1,191.
- Chunk 7 antiX validation is complete. Terminal checks passed before and after the manual-test pause: browser PID 59997 stayed localhost-only and healthy at 658/1,191, index PID 96511 and mbsync PID 2327 remained running, mbsync stayed unpaused, both locks were absent, and download temp stayed at zero files/bytes. The owner explicitly confirmed all 18 manual UI checks passed, covering search, duplicate switching, individual download, four-file Save All ZIP, blocked/embedded/remote confirmations and resets, HTML/plain bodies, divider, sidebar, narrow layout, status readability, and absence of overlap/clipping.
- Chunk 8 Windows-tunnel validation is complete. antiX detected VM IP `192.168.254.128`; an established SSH connection from Windows `192.168.254.1` reached antiX port 22 while the browser remained bound only to `127.0.0.1:8765`. Browser PID 2400 and index PID 2313 stayed running; health remained read-only and stable during the gate at 827 messages/1,507 files; both locks were absent; download temp was zero before/after. The increased counts and changed PIDs since Chunk 7 reflect normal intervening mail/index activity or a later session, not within-gate drift. The owner explicitly confirmed all 14 Windows UI checks passed, including duplicate switching, individual/ZIP downloads, image confirmations and resets, body/layout/status behavior, and absence of visual defects.
- Chunk 9A established a verified controlled pre-reboot baseline. Boot ID was `41d0fe2f-7ac1-4a06-be9a-178dea473f31`; installed binary/control/index hashes matched expected; exactly one browser PID 2400 and one index-loop PID 2313 were running; mbsync PID 2324 was running unpaused with last exit 0; each neutral browser/index IceWM marker appeared exactly once and no `CODEX NOTMUCH` marker existed. Listener was localhost-only, health contract passed at 827/1,507, locks were absent, and Maildir/download temp were zero. Mode-600 pre-state `/mail/AppData/notmuch-browser/chunk9-pre-reboot.env` has SHA256 `7df896cf...`. The system is ready for the controlled reboot/post-login proof.
- Chunk 9B completed the controlled reboot proof. Boot ID changed to `8dea0ad8-f226-4e70-b6a1-6ade9459dea3`; at 92.62 seconds uptime, IceWM had auto-started exactly one browser PID 2185 and one index-loop PID 2097 on the first readiness check, with mbsync PID 2130 running unpaused. Startup markers/hashes remained exact, listener remained localhost-only, expanded health and root/search/status passed at 827/1,507, and temp was zero. The full exact-byte/capability/CSP/inline-image/four-entry ZIP validator and strict before/after proof then passed with paths `3c92fde3...`, indexed content `4dbecb47...`, tags `ec65d984...`, 50,233 Mailstore files, metadata `bce6b835...`, and no drift. Index restarted as PID 7074; browser remained PID 2185; mbsync resumed; final locks and Maildir/download temp were zero. Final HTML/TXT guide regeneration and static validation subsequently passed.
- IceWM startup integration uses the neutral markers `# BEGIN NOTMUCH BROWSER SERVICE` and `# END NOTMUCH BROWSER SERVICE`. The block in `/home/atiq/.icewm/startup` runs `~/.local/bin/notmuch-browser-control start` in the background, passed idempotency checks, and subsequently passed the Chunk 9B reboot/login autostart proof.
- Post-reboot proof passed: after rebooting antiX, `~/.local/bin/notmuch-browser-control status` showed the Go browser running as pid 2121, and `/healthz` returned OK/read-only/single-email mode with 457 messages and 867 files. The read-only Go notmuch browser service is now validated end to end for IceWM-session auto-start and Win11 SSH-tunnel access.
- A 2026-07-09 repair run explained a later browser error after provider-live changed behind the notmuch database: before refresh, `tag:inbox` search failed because 12 indexed files under `/mail/Mailstore/mbsync/provider-live/new/` no longer existed. `refresh-index` ran without mbsync overlap, processed 174 files, detected 12 renames, added 102 new messages, left Maildir `tmp` count at 0, and restored `/healthz` plus `/search?q=tag%3Ainbox` behavior with 559 messages and 1,025 files.
- The user-level notmuch browser index-refresh loop was installed and validated on 2026-07-09 as `/home/atiq/.local/bin/notmuch-browser-index-control`. It runs every 60 seconds, skips while the provider-live mbsync lock or notmuch refresh lock is present, calls the existing locked `notmuch-browser-control refresh-index`, writes `/mail/Logs/notmuch-browser/notmuch-browser-index-loop.log`, and starts from neutral IceWM markers `# BEGIN NOTMUCH BROWSER INDEX REFRESH SERVICE` / `# END NOTMUCH BROWSER INDEX REFRESH SERVICE`. Validation showed the loop running as pid 14998, a no-new-mail refresh completed successfully, `/healthz` remained OK with 559 messages and 1,025 files, the browser search endpoint returned HTML, and Maildir `tmp` count stayed 0.
- `docs/NOTMUCH_GO_BROWSER_SERVICE_GUIDE.html` and `.txt` now reproduce the validated attachment/image revision from the exact 65,392-byte, 30-member source bundle. They document dependency pins, source members, staged build, state-preserving automatic rollback, localhost and health gates, 18-item antiX and 14-item Windows acceptance, portable strict read-only proof, controlled reboot, stale-index repair, exact validated hashes, expected output, and rollout troubleshooting. The HTML has 19 offline copy controls with no missing targets or external resources.
- Windows validation passed for syntax and portable unit tests. The copy-preserves-Maildir-flags converter test is skipped on Windows because `:2,` filenames are Linux Maildir-specific and invalid on Windows filesystems.
- Next dynamic delta validation must transfer the verified aggregate export `/home/atiq/maildirpp-post-main-archive-export-20260704-205827` to antiX, restore it as a separate Evolution Maildir account, and validate message counts, message opening, HTML rendering, attachments, and absence of error popups.

## Open Questions

- Which operating environments must the finished project support beyond Windows 11 and Debian Linux?
- Should the project use a conventional branching model, trunk-based work, or simple local commits until the direction is clearer?
- What, if any, non-mutating browser capability should follow only after the attachment/image revision is fully antiX-validated?

## Next Actions

1. Keep the provider-live archive monitor running and inspect `provider-live-archive-control status` periodically; no transaction is required below 15,000.
2. At threshold, run only `estimate` first and do not clean up until all external-backup, Evolution, notmuch, and mbsync-canary gates pass.
3. On a new VM, restore the full 59G Betterbird archive only from the exact verified package and require 48,720 indexed file paths; do not copy the current drifted tree. The existing VM remains unchanged until a separate guarded rollout is approved.
4. Keep the mbsync and notmuch index loops monitored during normal antiX use.
5. Consider credential and flag-sync changes only as separate controlled work.

## Cross-Machine Restore Instructions

Use Git to move the repository between machines once a remote is connected.

General restore flow:

1. Install Git on the target Windows 11 or Debian Linux machine.
2. Clone the repository from the chosen remote.
3. Open the repository root.
4. Read `AGENTS.md`, `README.md`, `docs/PROJECT_STATE.md`, `docs/TODO.md`, `docs/DECISIONS.md`, and `docs/WORK_VALIDATION_LEDGER.md`.
5. Run `git status --short --branch` before making changes.
6. Do not install dependencies until the project type and dependency policy are decided.

## Notes for Future Codex Sessions

- Start by reading the required memory files listed in `AGENTS.md`.
- Treat this file as the high-level state snapshot.
- Use `docs/WORK_VALIDATION_LEDGER.md` as the durable per-step record when reviewing command chunks or pasted terminal output.
- Keep documentation current as work proceeds.
- Prefer small, reviewable changes.
- Ask before dependency installation, deletion, or remote pushes.
- Preserve portability between Windows 11 and Debian Linux.
